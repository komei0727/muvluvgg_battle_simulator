steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --tag
      - ${_IMAGE}
      - .

images:
  - ${_IMAGE}

# ユーザー指定SAではGoogle所有の既定log bucketを使用できないため、
# build logはCloud Loggingへ保存する。
options:
  logging: CLOUD_LOGGING_ONLY
