# UIテスト戦略

## 1. 目的

UIの見た目だけでなく、API DTO変換、battleUnitId単位の集計、未知イベントへの耐性、GitHub Pages配備を継続的に検証する。

テストは実装詳細ではなく、[01_UI要求・画面設計.md](./01_UI要求・画面設計.md) の受け入れ条件と本書のTest IDへ紐づける。

## 2. テストレベル

| レベル            | 対象                                                            | 実行頻度              |
| ----------------- | --------------------------------------------------------------- | --------------------- |
| Pure unit         | validator、request mapper、summary projector、formatter、filter | PRごと                |
| Component         | slot、dialog、form、summary、tabs、error表示                    | PRごと                |
| Contract fixture  | 実OpenAPI/response fixtureとの互換                              | PRごと                |
| E2E mock API      | browser上の主要フロー                                           | PRごと                |
| E2E deployed API  | Pages相当originからCORSを含む縦切り                             | mainまたは手動        |
| Visual regression | 採用モックとの主要viewport比較                                  | UI変更PR              |
| Accessibility     | 自動検査＋keyboard/screen reader手動                            | PR自動＋release前手動 |

## 3. Pure unit tests

### Draft validation

- `UI-UT-VAL-001`: 味方0体を拒否する。
- `UI-UT-VAL-002`: 敵0体を拒否する。
- `UI-UT-VAL-003`: 1体と5体を受理し、6体を拒否する。
- `UI-UT-VAL-004`: memory 0件と6件を受理し、7件を拒否する。
- `UI-UT-VAL-005`: turnLimit 1、99を受理し、0、100、小数、空を拒否する。
- `UI-UT-VAL-006`: Catalogに存在しない定義IDを持つunit/memoryを拒否する。
- `UI-UT-VAL-007`: 適性外配置はwarningでありerrorにしない。
- `UI-UT-VAL-008`: レベルリンクONでリンクレベルが空・0・小数のとき`LEVEL_LINK_INVALID`を出す。1以上の整数は受理する。
- `UI-UT-VAL-009`: リンク中の枠の空・0・小数のレベルを`UNIT_LEVEL_INVALID`にしない。「リンクから外す」枠は従来どおり検証する。
- `UI-UT-VAL-010`: 陣営の強化トグルOFF、またはリンクOFFのときはリンクレベルを検証しない（入力値は保持したまま送信対象から外れる）。
- `UI-UT-VAL-011`: モードと陣営の編成プール（`PLAYABLE`／`EXERCISE_ENEMY`）に合わない枠を`UNIT_POOL_MISMATCH`で拒否する。
- `UI-UT-VAL-012`: 統計実行の実行回数1・2,000を受理し、0・負数・小数・2,001・空を`RUN_COUNT_OUT_OF_RANGE`で拒否する。単一実行では実行回数を検証せず、シードは形を問わない。

### Request mapping

- `UI-UT-REQ-001`: FRONT/REAR、column 0～2をそのままAPI positionへ変換する。
- `UI-UT-REQ-002`: Catalog適性 `BACK`をrequestの `REAR`と混同しない。
- `UI-UT-REQ-003`: 空slotを除外する。
- `UI-UT-REQ-004`: 同じunitDefinitionIdを複数slotへ出力する。
- `UI-UT-REQ-005`: unitsをFRONT column順、REAR column順へ安定化する。
- `UI-UT-REQ-006`: UI専用fieldやunknown propertyを出力しない。
- `UI-UT-REQ-007`: request array indexから元slotKeyへの逆引きを保持する。
- `UI-UT-REQ-008`: プレビューrequestが戦闘requestと同じ編成・強化指定を持ち、`turnLimit`と`options`を持たない。ターン上限が未入力でも組み立てられる。
- `UI-UT-REQ-009`: レベルリンクONで全枠の`enhancement.level`がリンクレベルへ解決される。強化入力を一度も開いていない枠（`enhancement === undefined`）も対象で、解決済みレベルが200以外ならギア0件の`enhancement`を出力する。
- `UI-UT-REQ-010`: `linkExcluded`の枠だけが枠の個別レベルで出力され、同じ陣営の他の枠はリンクレベルのままになる。
- `UI-UT-REQ-011`: リンクOFFでは各枠の個別レベルをそのまま出力し、リンクON・OFFのいずれでも`levelLink`・`linkExcluded`をrequestへ出力しない。解決済みレベル200かつギア0件の枠は`enhancement`を出力しない。
- `UI-UT-REQ-012`: リンクONでもリンクレベルが空・0・小数のときはリンクを適用せず、各枠の値（強化入力を持たない枠は200）へフォールバックする。
- `UI-UT-REQ-013`: 戦術演習の単一実行が、draftに残っている`logLevel`に依らず常に`options.logLevel: "DETAILED"`で送る。
- `UI-UT-REQ-014`: 一括評価リクエストが単一実行と同じ編成部分を候補1件へ載せ、`options`も`turnLimit`も持たない。敵ちょうど1体・敵メモリー0件の制約とslot対応表は単一実行と同じ。
- `UI-UT-REQ-015`: 編成署名が同じ編成に対して安定し、実行回数・シードでは変わらず、味方・敵いずれの編成変更でも変わる。

### Summary projection

- `UI-UT-SUM-001`: `unitSummaries`のdamageDealt／damageTaken／healingDoneをそのまま読み、イベント列からは集計しない。
- `UI-UT-SUM-004`: 同じunitDefinitionIdの異なるbattleUnitIdを別行として保つ。
- `UI-UT-SUM-005`: サーバーが返した0をそのまま表示する。
- `UI-UT-SUM-007`: combatStatusとHPを`finalState`ではなく`unitSummaries`から解決する。
- `UI-UT-SUM-008`: イベント列を一切読まないため、未知eventが表へ影響しない。
- `UI-UT-SUM-010`: initialState roster順を維持する。
- `UI-UT-SUM-016`: `finalState`を持たないresponse（3/3の`SUMMARY`形）でも表を描画する。
- `UI-UT-SUM-017`: 対応する`unitSummaries`行が無いrosterユニットを0埋めし、warningを返す。
- `UI-UT-SUM-018`: 陣営の振り分けを`unitSummaries[].side`ではなくroster側の`side`で決める。

`UI-UT-SUM-002`／`003`／`006`／`009`／`011`〜`015`は、クライアント側イベント集計
（`DAMAGE_APPLIED`／`HEAL_APPLIED`／`HEALING_TRANSFERRED`のアダプタ）そのものを検証して
いたため、Issue #464 のサーバー集計移行で退役した。集計セマンティクスの証跡はAPI側
（`UT-UNIT-SUMMARY-001`〜`006`、`IT-REL-004-LOG-LEVEL-005`）が引き継いでいる。

### 演習結果の投影（Issue #538）

- `UI-UT-EXR-001`: ブレイクの発生源ユニットをCatalogの`displayName`で解決する。
- `UI-UT-EXR-002`: Catalog未取得、またはCatalogに無い定義IDの発生源を定義IDのまま表示する。
- `UI-UT-EXR-003`: `sourceUnitDefinitionId`を持たないブレイクを「メモリー効果」として表示する（`R-MEM-04`。この項目より前にデプロイされたAPIの応答も同じ経路を通る）。

### 統計実行の統計量（Issue #540）

統計の定義の正本は `tools/exercise-lab`（`stats.py` / `optimize/fitness.py`）であり、
`features/exercise-stats` はその移植である。期待値は
`tools/exercise-lab/scripts/export_ui_parity_fixture.py` が同ツールの実装から書き出した
`__fixtures__/python-parity.ts` を読む。TS側が重みを比の積で持つ分だけ丸めの順序が違うため、
一致は相対許容差で見る（`test-parity.ts`）。

- `UI-UT-STS-001`: count／mean／median／stdev／min／max／p05／p25／p75／p95と95%信頼区間がPython実装と一致する。
- `UI-UT-STS-002`: n=1でstdevと信頼区間を`null`にし、全run同値の0（ばらつきが無い）と区別する。
- `UI-UT-STS-003`: 空のスコア・完了理由・ブレイク回数を契約違反としてthrowする。
- `UI-UT-STS-004`: `ALLY_DEFEATED`の割合がPython実装と一致し、未知の終了理由もコードのまま内訳へ数える。
- `UI-UT-STS-005`: ブレイク回数分布を回数の昇順で返し、平均を併せて出す。
- `UI-UT-STS-006`: ヒストグラムのビン数がSturges（1〜60へクランプ）でPython実装と一致し、等幅ビンが全試行を覆う。
- `UI-UT-STS-007`: E[best-of-5]がPython実装と一致し、n<kの部分結果では標本最大へ落ちる。
- `UI-UT-STS-008`: 日次ベスト分位点（75%・90%保証ライン、日次ベスト中央値）がPython実装と一致する。
- `UI-UT-STS-009`: 有効サンプル数 n(2k−1)/k² と信頼可否（≥10）がPython実装と一致する。
- `UI-UT-STS-010`: 日次ベスト分布曲線が経験CDFの`F(x)^k`であり、標本の外へ外挿しない。
- `UI-UT-STS-011`: ユニット別与ダメージのmean／median／p25／p75／min／maxと寄与率（平均÷全体平均合計）を出し、総ダメージ0でも0除算しない。試行ごとに列数が違う応答は拒否する。
- `UI-UT-STS-012`: ユニット別ブレイク回数の平均と0／1／2／3+の内訳、および味方の枠が起こしたのではないブレイクの平均残差を出す。残差には発生源ユニットを持たないブレイク（R-MEM-04）と敵の枠自身が起こしたブレイク（R-CFS-01）の両方が入るため、起源を断定した名前を付けない（`10_API設計.md`）。
- `UI-UT-STS-013`: ベストスコア上位N試行のユニット別平均と全体平均との差分を出し、同点は添字の小さい試行から採る。
- `UI-UT-STS-014`: Nが試行数以上のとき上位N集計が全体統計と一致し、差分が0になる。

### 統計実行の分割送信（Issue #541）

`tools/exercise-lab`の`runner.py`と同じ分割規約であること自体が要件なので、チャンク計画・集約は純関数として押さえる。

- `UI-UT-EVL-001`: 1,000試行が300×3＋100へ割れ、各チャンクが`<seed>#<runOffset>`を持つ。300ちょうど・1試行の境界と、非正の実行回数・チャンクサイズの拒否を含む。
- `UI-UT-EVL-002`: シード未入力のとき実行seedを自動生成し、実行ごとに異なる値になる。
- `UI-UT-EVL-003`: チャンクの応答を送信順に連結し、部分結果を埋めずに短い標本として積む。チャンク間の`catalogRevision`不一致とユニット別配列の列数変化を集約せず報告する。
- `UI-UT-EVL-004`: 指定回数がチャンクへ割れて逐次送られ（同時in-flightは常に1件）、進捗が更新される。同じseed・同じ実行回数なら同じリクエスト列になる。
- `UI-UT-EVL-005`: 部分結果を再送しない。中断は完了済みチャンクまでで確定し、完了0試行は結果なしとして失敗にする。unmountで実行中のチャンクを中断する。
- `UI-UT-EVL-006`: `404 ENDPOINT_DISABLED`（判定は`code`のみ。codeを持たない404は汎用のAPI失敗）・`429`（`Retry-After`付き）・チャンク間のCatalog切替・列数変化・seedの不一致・その他のAPI失敗で再試行せず中断する。汎用のAPI失敗は§13の種別文言・生message・violations・送信時のslot対応表を残す。
- `UI-UT-EVL-007`: 評価APIの422 pathから`/candidates/0`を落として送信時のslot・ギア枠へ対応づけ、`/runsPerCandidate`と共有の敵編成のpathはそのまま扱う。対応づかないpathは枠を推測しない。表示するpathはサーバーが返した形へ戻す。

### 統計の投影とエクスポート（Issue #542）

表示用の投影（`statistics-report.ts`）と持ち出す形（`statistics-export.ts`）は、統計量そのものを
計算しない層である。ここで押さえるのは並べ直し・列の名前づけ・列の並びであり、数値の定義は
`UI-UT-STS-*` が持つ。

- `UI-UT-STR-001`: 応答の列へ送信時の編成から名前を付け、Catalog未取得では定義IDへ落ちる。同じ定義を複数の枠へ置いた場合は枠の順番を添えて読み分けられるようにする。
- `UI-UT-STR-002`: スコア統計・日次ベスト指標・完走内訳・ブレイク回数分布を1つの投影へまとめ、要求試行数と実試行数の差を部分結果として残す。
- `UI-UT-STR-003`: キャラ別の全run統計と上位N部分集計を同じ行へ並べ、分布バーの共通スケールと、ユニット列に帰属しないブレイクの残差を出す。送信時の編成が名前を付けられない列は位置で示す。
- `UI-UT-STR-004`: 単発ヒストグラムと日次ベスト分布が同じビン・同じ縦軸（割合）に載り、日次ベスト側が経験CDFの`F(x)^k`のビン境界差分になる。全試行同値の縮退した標本も1本のビンとして出す。
- `UI-UT-CSV-001`: 連結後の標本からは復元できない試行ごとの出所（チャンク番号・チャンクseed・チャンク内試行番号）を、連結と同じ場所で残す。
- `UI-UT-CSV-002`: 生データCSVが`runs.csv`（`stats.py:RUNS_CSV_COLUMNS`）と同じ8列で始まり、ユニット別の列を後ろへ足す。seedのような任意文字列はRFC 4180でquoteする。
- `UI-UT-CSV-003`: 統計サマリJSONがexercise-labの`summary.json`と同じ再現条件・同じキーを持ち、日次ベスト指標・ユニット別統計・上位N集計を足す。
- `UI-UT-CSV-004`: 組み立てたテキストを`a[download]`で渡し、object URLを同じ操作の中で開放する。object URLを作れない環境では失敗を呼び出し側へ返す。

### M8 ダメージ内訳（DMG-010、Issue #191）

- `UI-UT-DMG-001`: `DAMAGE_APPLIED`のcalculated／タイプあり・なしシールド吸収／サブユニット吸収／HPダメージを別項目として出す。
- `UI-UT-DMG-002`: 内訳フィールドを持たないM4〜M7 fixtureを「吸収0」と断定せず表示し続ける。
- `UI-UT-DMG-003`: 値が0の吸収項目を出さない。
- `UI-UT-DMG-004`: 反射・リンク由来のダメージをヒット番号ではなく由来ラベルで表す。
- `UI-UT-DMG-005`: `DAMAGE_CALCULATED`の実効防御・防御貫通・最終ダメージを出す。
- `UI-UT-DMG-006`: 混乱倍率（R-CFS-02）を与ダメージ倍率と別項目で出す。
- `UI-UT-DMG-007`: `DAMAGE_WILL_BE_APPLIED`の貫通3割合を出す。
- `UI-UT-DMG-008`: `CRITICAL_CHECK_RESOLVED`のクランプ後実効会心率と判定結果を出す。
- `UI-UT-DMG-009`: `HIT_POINT_REDUCED`のHP変化を出す。
- `UI-UT-DMG-010`: `SHIELD_CONSUMED`のプール種別・吸収量・残量を出す。
- `UI-UT-DMG-011`: `shieldType: null`をタイプなしプールとして表し、`null`を見せない。
- `UI-UT-DMG-012`: `SUB_UNIT_DAMAGED`をインスタンス単位の耐久変化として出す。
- `UI-UT-DMG-013`: 引き寄せ（`TARGET_REDIRECT`）と肩代わり（`COVER`）を軽減率とともに読み分ける。
- `UI-UT-DMG-014`: `REFLECTED_DAMAGE_GENERATED`の元ダメージと反射ダメージを出す。
- `UI-UT-DMG-015`: `LINKED_DAMAGE_GENERATED`のリンク率とシールド適用可否を出す。
- `UI-UT-DMG-016`: `LETHAL_DAMAGE_SURVIVED`の耐えた直後HPを出す。
- `UI-UT-DMG-017`: `DAMAGE_CONVERTED_TO_HEAL`をダメージではなく回復として表す。
- `UI-UT-DMG-018`: `CONTINUOUS_DAMAGE_APPLIED`の種別と炎上2倍を出す。
- `UI-UT-DMG-018B`: `FIXED`継続ダメージのサブユニット吸収を内訳へ出し、保存則を成立させる。
- `UI-UT-DMG-019`: R-DOT-04の付与時攻撃力上限到達を明示する。
- `UI-UT-DMG-020`: `EFFECT_MERGED`の効果量・残り期間の採用結果を出す。
- `UI-UT-DMG-021`: 必須項目を欠くダメージpayloadをgeneric fallbackへ落とす。
- `UI-UT-DMG-022`: `finalState.units[].shields`の3プールを読み、契約違反を不明として落とす。
- `UI-UT-DMG-023`: `subUnits`をプール合計へ合算せずインスタンスのまま保持する。
- `UI-UT-DMG-024`: `shields`/`subUnits`を持たないfixtureを「なし」ではなく不明として扱う。
- `UI-UT-DMG-025`: shapeが壊れたsubUnit要素だけを落とし、残りを表示する。

### DAMAGE_CALCULATEDの計算過程（DMG-012、Issue #488）

- `UI-UT-DMG-026`: ダメージ計算の全項目をR-DMG-01の乗算順で、等倍・ゼロ・0%でも省略せず出す。貫通3割合の0%表示とダメージ無効の不成立表示を含む。
- `UI-UT-DMG-027`: DMG-012以前に録取したpayloadをgeneric fallbackへ落とさず、欠けた項目だけを文から消す。ダメージ無効は項目が無いとき「なし」と断定しない。
- `UI-UT-DMG-028`: 攻撃力0を監査要約から落とさない。有利属性でない場合を「有利属性なし」として属性倍率1の理由を示す。

### 状態遷移delta（DMG-010、Issue #191）

- `UI-UT-DLT-001`: `EntityCollectionDelta`の追加・削除エントリを件数だけでなく名指しする。
- `UI-UT-DLT-002`: 更新エントリの変わったfieldだけを`before → after`として出す。
- `UI-UT-DLT-003`: サブユニットを自身のID項目（`subUnitInstanceId`／`subUnitDefinitionId`）で名指しする。
- `UI-UT-DLT-004`: 公開projectionに差分が現れない更新を「表示可能な変更項目なし」と明示する。
- `UI-UT-DLT-005`: IDが読めない未知shapeをcompact JSONへ退避し、件数へ畳まない。

### Catalog/filter

- `UI-UT-CAT-000`: 一覧API responseの必須shapeと定義ID重複を検証する。
- `UI-UT-CAT-001`: displayNameとdefinitionIdを大文字小文字を区別せず検索する。
- `UI-UT-CAT-002`: attribute/roleを組み合わせてfilterする。
- `UI-UT-CAT-005`: imageUrl欠落を許容する。
- `UI-UT-CAT-006`: Catalog revisionとETagを一覧API response/headerから保持する。
- `UI-UT-CAT-007`: 304で直前のin-memory Catalogを維持する。
- `UI-UT-CAT-008`: 一覧取得失敗または契約違反で編成を有効にしない。

`UI-UT-CAT-003`・`UI-UT-CAT-004`（選択可否のfilterと表示反映）は`REF-023`（Issue #352）のCapability廃止で退役した。IDは欠番のままとする。

### 入力の永続化（Issue #425）

- `UI-UT-PST-001`: localStorageが無い環境・アクセスがthrowする環境で、読み書き・削除が例外を投げずに縮退する。
- `UI-UT-PST-002`: 保存できない書き込み（`QuotaExceededError`）を握り潰し、呼び出し側へ失敗を伝播しない。
- `UI-UT-PST-003`: JSONとして壊れた値、`schemaVersion`が異なる値、shapeが契約外の値をすべて破棄し、初期状態へフォールバックする。固定長配列（ユニット枠6・メモリー枠6・ギア枠9）の長さ不一致も破棄する。
- `UI-UT-PST-004`: 保存したdraftを枠位置・空枠・強化入力ごと同値へ復元する。
- `UI-UT-PST-005`: 味方draftから手持ちデータを導出し、未入力（`""`）のレベル・学園レベルを書き戻さない。敵側の入力を書き戻さない。
- `UI-UT-PST-005B`: 同じユニットが複数枠にある場合、直近に編集した枠の値だけを記録し、未編集の同一ユニット枠で上書きしない。記録後は無関係なdraft変更で値が揺れない。
- `UI-UT-PST-006`: 導出結果が前回と等しい場合に同一参照を返す。
- `UI-UT-PST-007`: 手持ちデータからユニットのレベル・ギアをプリフィルし、記録が無ければ既定値（レベル200・ギア9枠すべて空）を返す。
- `UI-UT-PST-008`: `UNKNOWN_DEFINITION` violationの`slotKey`だけをユニット枠・メモリー枠から取り出し、他の違反コードを拾わない。
- `UI-UT-PST-009`: Catalogに存在しないユニットの手持ちデータエントリだけを取り除き、残りを保持する。
- `UI-UT-PST-010`: 保存済みdraftの`logLevel: "DIAGNOSTIC"`を`DETAILED`として復元し、draft全体を破棄しない（Issue #464）。
- `UI-UT-PST-011`: `levelLink`／`linkExcluded`を持たない保存データ（`schemaVersion`は現行のまま）を破棄せず、既定値（リンクOFF・リンクレベル200・除外なし）で復元する。手持ちデータと両モードのdraftキーの**すべて**で成立させる（draft側は陣営の`levelLink`と枠の`linkExcluded`の双方）。
- `UI-UT-PST-012`: 味方draftから`levelLink`と`linkExcluded`を手持ちデータへ書き戻し、プリフィルで枠へ復元する。敵側のリンク設定は書き戻さない。
- `UI-UT-PST-013`: `exerciseExecution`を持たない保存draft（`schemaVersion`は現行のまま）を破棄せず、既定値（単一実行・100回・シード空）で復元し、保存済みの`logLevel`はそのまま残す。実行モード・実行回数・シードは往復で保たれ、未知の実行モードを持つ保存データは破棄する。

### API/error

- `UI-UT-API-001`: 200 successを正規化する。
- `UI-UT-API-002`: error body付き422をfield violationへ変換する。
- `UI-UT-API-003`: 429/503のRetry-Afterを安全にparseする。
- `UI-UT-API-004`: 504をturn limit resultへ変換しない。
- `UI-UT-API-005`: AbortErrorを利用者キャンセルとUI timeoutで区別する。
- `UI-UT-API-006`: fetch TypeErrorをnetwork/CORS候補として扱い、断定しない。
- `UI-UT-API-007`: 必須shape欠落の200をcontract mismatchへ変換する。
- `UI-UT-API-008`: unknown event/type/optional propertyを許容する。
- `UI-UT-API-009`: プレビュー200を`units`ごとの開始時ステータスへ正規化し、必須shape欠落をcontract mismatchとして扱う。
- `UI-UT-API-010`: `finalState`を持たない200を受理する（3/3の`SUMMARY`形）。
- `UI-UT-API-011`: `finalState`が届いた場合はinitialStateとのroster対応を従来どおり検証する。
- `UI-UT-API-012`: `unitSummaries`の欠落・非array・必須項目欠落・型/値域違反を拒否する。
- `UI-UT-API-013`: `unitSummaries`がrosterのユニットを覆っていない200を拒否する。
- `UI-UT-API-014`: `unitSummaries`が同じ`battleUnitId`を重複して持つ200を拒否する。
- `UI-UT-API-015`: rosterに無い`battleUnitId`の`unitSummaries`行を拒否する。
- `UI-UT-API-016`: `unitSummaries`の並び順がinitialState.unitsと異なる200を受理する（API契約は配列順も定めるが、UIは`battleUnitId`で結合するため順序違反だけでは表示が壊れず、最小validatorは拒否しない）。
- `UI-UT-API-017`: 演習の`breaks[].sourceUnitDefinitionId`を任意項目として受理し、載っている場合だけ非空文字列を要求する。
- `UI-UT-API-018`: 一括評価の200を受理し、6配列の長さが`completedRuns`と一致しない・ユニット別の列数が試行間で違う・与ダメージ列とブレイク列の幅が違う・候補が1件でない応答を契約違反として拒否する。完了0試行のチャンクは受理する。
- `UI-UT-API-019`: 一括評価POSTが`no-store`／`credentials: "omit"`／`X-Request-Id`で送られ、404の`ENDPOINT_DISABLED`・429の`Retry-After`・中断を呼び出し側が読み分けられる形で返す。

## 4. Component tests

- `UI-CT-001`: 空slotに完全なaccessible nameがある。
- `UI-CT-002`: 選択済みslotに名前、属性、role、画像fallbackが出る。
- `UI-CT-003`: slot操作でdialogが開き検索inputへfocusする。
- `UI-CT-004`: dialog Escapeで閉じ、元slotへfocusが戻る。
- `UI-CT-005`: unit dialogの検索・filter・解除・選択が動作する。
- `UI-CT-007`: 5体設定済みで空slotから6体目を確定できない。
- `UI-CT-008`: submit中にbutton連打してもAPI clientが1回だけ呼ばれる。
- `UI-CT-009`: cancelでAbortControllerが呼ばれる。

`UI-CT-006`（unavailable itemの選択不可と理由表示）は`REF-023`（Issue #352）で退役した。IDは欠番のままとする。

- `UI-CT-010`: 旧executionの遅延成功が新executionを上書きしない。
- `UI-CT-011`: 成功後、サマリ見出しへfocusが移る。
- `UI-CT-012`: draft変更後、前回結果へdirty indicatorが表示される。
- `UI-CT-013`: event/transition/JSON tabをkeyboardで切り替えられる。
- `UI-CT-014`: unknown eventをgeneric rowとして表示する。
- `UI-CT-015`: image errorでfallbackへ切り替わる。
- `UI-CT-016`: API field violationが該当slotとsummaryに表示される。
- `UI-CT-017`: finalStateのeffectを種別・category・残りDurationとともに一覧表示する。
- `UI-CT-018`: `category: STATUS_ABNORMALITY`のeffectを`statusKind`付きで表示し、次点(`isEffective: false`)を採用中と区別する。
- `UI-CT-019`: `effects`を持たないfixtureで「効果なし」と断定せず不明として表示する。
- `UI-CT-020`: 空の`effects`配列を「効果なし」として表示する。
- `UI-CT-021`: 有利な`statusKind`（STEALTH等）をAPIの`category`どおりBUFFとして表示し、状態異常にしない。
- `UI-CT-022`: `finalState`のシールドプールとサブユニットインスタンスをユニット状態タブへ表示する。
- `UI-CT-023`: 全プール0・空配列を「シールドなし」「サブユニットなし」として表示する。
- `UI-CT-024`: `shields`/`subUnits`を持たないfixtureで「なし」と断定せず不明として表示する。
- `UI-CT-025`: `shields`だけを持つレスポンスで、`subUnits`側だけを不明として表示する。
- `UI-CT-026`: `subUnits`だけを持つレスポンスで、`shields`側だけを不明として表示する。
- `UI-CT-027`: モードタブが通常戦闘と戦術演習をkeyboardで切り替えられ、`aria-selected`が現在モードを示す。
- `UI-CT-028`: 戦術演習モードで敵メモリー枠とターン上限入力が表示されず、敵の6枠のうち別の枠を選んでも2体目にならず1体が移る。
- `UI-CT-029`: 戦術演習のリクエスト変換が`turnLimit`を含めず、敵1体・敵メモリー0件のペイロードを生成する。
- `UI-CT-030`: 演習成功レスポンスから総スコア、ブレイク回数、終了理由、ブレイク履歴が表示される。ブレイク0回でも成立する。
- `UI-CT-031`: 演習イベント（スコア加算、ブレイク、復活）がタイムラインへ表示され、未知の演習detailsでもクラッシュしない。
- `UI-CT-032`: モード切替後も各モードのdraftと直近結果が保持され、旧モードの遅延成功が他モードを上書きしない。
- `UI-CT-033`: 強化トグルOFF（既定）のsubmitで、API clientが受け取るリクエストに`enhancement`プロパティが含まれない（後方互換）。
- `UI-CT-034`: 強化トグルONで学園レベル9項目を編集でき、0・小数・空が送信前検証でエラー表示される。
- `UI-CT-035`: 選択済みユニット枠からユニット強化ダイアログが開き、レベルとギア9枠（stat・tier・grade。空枠と同一statの重複を許容）を編集できる。
- `UI-CT-036`: 陣営の強化トグルOFF時はユニット強化ダイアログを開けず、トグルONを促す表示が出る。
- `UI-CT-037`: `enhancement`配下の422 field violationが該当する学園レベル・レベル・ギア入力へ表示される。
- `UI-CT-038`: 選択済みユニット枠のhoverとfocusで開始時ステータスが表示され、slot buttonから`aria-describedby`で参照される。
- `UI-CT-039`: 強化指定の変更でプレビューが再取得され、表示値が新しい応答へ追随する。強化トグルOFFの陣営には強化なしの値が出る。
- `UI-CT-040`: プレビュー取得が失敗しても戦闘実行buttonが有効なままで、送信前検証の違反が増えない。
- `UI-CT-041`: ギアのstatを選ぶと、種別・ランクの選択肢へその組み合わせの上昇値が併記され、割合補正（`%`）とポイント加算（`%pt`）が表記で区別される。
- `UI-CT-042`: tier・gradeの片方だけが選ばれている間は、もう一方の軸を跨いだ範囲が選択肢へ示される。
- `UI-CT-043`: 一覧応答が`gearEffects`を持たない場合、および表に無いstatを選んだ場合に、ランク名だけの表示へフォールバックする。

- `UI-CT-044`: 前回セッションの編成（両陣営の配置・メモリー・強化入力・ターン上限）が再mountで復元される。
- `UI-CT-045`: Catalogから消えた定義を含む保存データで、該当枠だけが空になり残りの入力が復元される。
- `UI-CT-046`: 味方枠へ配置したユニットへ手持ちデータのレベル・ギアがプリフィルされ、外して置き直しても再入力を要さない。同じユニットを敵枠へ置いてもプリフィルされない。
- `UI-CT-047`: localStorageへの書き込みが常に失敗する環境でも、編成入力と送信が動作する。
- `UI-CT-048`: 「編成をクリア」がdraftだけを初期化して手持ちデータを残し、「保存した育成データをクリア」が手持ちデータと味方育成入力だけを消してユニット配置・敵側入力を残す。
- `UI-CT-049`: 同じユニットを複数の味方枠へ配置した状態で片方を編集しても、未編集の枠の値で保存が上書きされない。
- `UI-CT-050`: ドラッグ&ドロップで同一陣営の空き枠へ移動・選択済み枠と入れ替えでき、ユニット単位の強化入力がユニットと一緒に移る。
- `UI-CT-051`: 陣営を跨ぐdrop・自分自身へのdrop・disabled中のdragは編成を変更しない。空き枠はdrag元にならない。
- `UI-CT-052`: 「移動」buttonからキーボードだけで移動元の選択→配置ができ、Escapeで中止できる。進行状態が`aria-live`で通知される。

- `UI-CT-053`: 戦術演習の敵選択ダイアログが`EXERCISE_ENEMY`だけを表示し、開催中バッジが出る。
- `UI-CT-054`: 通常戦闘の両陣営と戦術演習の味方の選択ダイアログが`PLAYABLE`だけを表示する（`category`不在は`PLAYABLE`扱い）。
- `UI-CT-055`: 開催終了（`exerciseActive: false`）の演習ユニットもバッジ付きで選択でき、そのまま送信できる。
- `UI-CT-056`: 誤ったプールのユニットが枠に残っている（保存draftの復元）と`UNIT_POOL_MISMATCH`で送信が止まる。
- `UI-CT-057`: 戦術演習のプレビューrequestに`mode: "TACTICAL_EXERCISE"`が載り、通常戦闘では載らない。
- `UI-CT-058`: 初期表示が戦術演習タブになる。
- `UI-CT-059`: 戦術演習の編成が再mountで復元され、通常戦闘のdraftと混ざらない。
- `UI-CT-060`: 戦術演習の敵を置いた枠の座標がそのままリクエストの`position`へ載る（前衛左上に固定されない）。

ログ方針刷新2/3（Issue #464）。

- `UI-CT-061`: ログレベルの選択肢が`SUMMARY`と`DETAILED`の2つだけで、退役した`DIAGNOSTIC`を選べない。
- `UI-CT-062`: `SUMMARY`実行の結果では詳細タブを出さず、詳細ログで実行し直せる案内へ置き換える。
- `UI-CT-063`: `DETAILED`実行では5タブすべてを保つ。

補正前ステータスの表示（`UI-AC-034`、Issue #488）。

- `UI-CT-064`: 応答の`enhancedBaseStats`を検証済みユニットへ保持し、補正前表示へ渡せる。
- `UI-CT-065`: `enhancedBaseStats`を持たない応答も受理する（このフィールドより前にデプロイされたAPIでもプレビューが成立する）。
- `UI-CT-066`: 形が壊れた`enhancedBaseStats`は部分表示せず拒否する。
- `UI-CT-067`: プレビューは既定で補正後（開始時）のステータスを表示する。
- `UI-CT-068`: 補正前表示を選ぶと`enhancedBaseStats`の値を表示する。
- `UI-CT-069`: どちらを表示しているかを見出しで示し、2つの値を取り違えさせない。
- `UI-CT-070`: 補正前が取得できない応答では、補正後の値へ黙って差し替えず取得できない旨を示す。
- `UI-CT-071`: 表示の切替に依らず、プレビューの取得状態を報告し続ける。
- `UI-CT-072`: 1つの切替が両陣営・全枠のプレビューへ効く。
- `UI-CT-073`: `enhancedBaseStats`を返さない応答でも補正後プレビューは動き、補正前は取得できない旨を示す。

- `UI-CT-074`: `finalState`の効果を`kindKey`のグループ名ではなく定義IDで名指しする。

レベルリンク（`UI-AC-035`〜`039`）。

- `UI-CT-075`: 陣営強化パネルでレベルリンクをONにしてレベルを入力すると、送信リクエストの全ユニットの`enhancement.level`がその値になる。ユニット強化ダイアログを一度も開いていない枠も含む。
- `UI-CT-076`: リンク中はユニット強化ダイアログのレベル入力を編集できず、適用中のリンクレベルが表示される。
- `UI-CT-077`: 「リンクから外す」でその枠だけ個別編集へ戻り、レベルにその時点のリンクレベルがシードされる。「リンクへ戻す」では枠のレベルが書き換わらず、送信値だけがリンクレベルへ戻る。
- `UI-CT-078`: リンクレベルが空・0・小数のとき送信が止まり、リンク中の枠の空レベルでは止まらない。
- `UI-CT-079`: リンク中の枠のレベルに対する422（成長値を持たないユニット）が、編集できない状態のレベル入力へ表示され、「リンクから外して200を指定する」案内も同じ入力へ結びつく。
- `UI-CT-080`: 味方のリンク設定がモードを切り替えても同じ値で表示され、再mountで復元される。`levelLink`を持たない保存データでも編成入力が初期化されない。
- `UI-CT-081`: 陣営の強化トグルOFFではレベルリンクを操作できず、リクエストへ`enhancement`が出ない（`UI-CT-033`の後方互換を保つ）。

ブレイクの発生源（`UI-AC-021`、Issue #538）。

- `UI-CT-082`: ブレイク履歴が発生源をBREAK／TURN／累計スコアに続く4列目として出し、発生源ユニットを持たないブレイクを「メモリー効果」と表示する。

### 戦術演習の実行モード（Issue #539）

- `UI-CT-083`: 戦術演習モードでログレベル選択が実行モード切替へ置き換わり、通常戦闘モードでは従来どおりログレベルを選べる。統計実行を選んだときだけ実行回数・シードが現れ、入力が報告される。実行モードはモードを往復しても保たれる。
- `UI-CT-084`: 範囲外の実行回数が実行回数入力へ`aria-invalid`と文言として出て実行できない。値を直すと統計実行のまま実行できる。

### 統計実行（Issue #541）

- `UI-CT-085`: 実行中は進捗バー（完了試行数／要求試行数）と現在チャンクを`aria-live="polite"`で出し、「中断して結果を見る」を押せる。完了後は集計へ入った試行数・要求試行数・seedを出し、中断を失敗として扱わない。失敗は`role="alert"`で種別文言・生message・violationsの順に出す。完了後に編成が変わると「変更前の条件」と示し、Catalog revisionが変わった結果は要約を出さず再読込を促す。
- `UI-CT-086`: 統計実行の実行ボタンが指定回数を300試行ずつのチャンクへ割って`<seed>#<runOffset>`で逐次送り、進捗・中断・集計試行数を表示する。`404 ENDPOINT_DISABLED`はこの環境では利用できない旨として出て、422の`/runsPerCandidate`は実行回数入力へ出る。実行中のロックも422の枠表示も演習の統計実行に閉じ、通常戦闘・単一実行へは漏れない。完了後に編成を変えると結果へ「変更前の条件」が出る。単一実行では評価APIを呼ばない。

### 統計可視化パネル（Issue #542）

- `UI-CT-087`: 演習統計サマリがスコア統計・日次ベスト指標・スコア分布・完走内訳・ブレイク回数分布と再現条件（seed・チャンクサイズ・Catalog revision）を出す。有効サンプルが10未満のときだけ実行回数を増やす警告を出し、要求より短い標本には部分結果を示す。
- `UI-CT-088`: スコア分布チャートが`role="img"`と説明を持ち、同じビンを`<table>`としても出す。inline style属性を1つも持たない（CSPが`style-src 'self'`）。全試行同値の標本でも図と表が成立する。
- `UI-CT-089`: キャラ別統計が編成順の行で全run平均・寄与率・上位N平均・分布バーを出し、上位Nの切り替えが再集計だけで効く。試行数がNに満たないときは実数を添え、ユニット列に帰属しないブレイクを脚注に出す。
- `UI-CT-090`: 全run生データCSVを`a[download]`で渡し、行数が完了試行数と一致する。統計サマリJSONをクリップボードへコピーできる。
- `UI-CT-091`: 統計実行の完了で演習サマリ・演習詳細が統計パネルへ差し替わり、単一実行では従来の表示のままになる。ユニット列の名前は送信時の編成から付き、実行後に編成を編集してもパネルは残る（`StatisticsRunFeedback`が「変更前の条件」を示す）。

APIはrequest mocking libraryで差し替え、component testから実networkへ接続しない。localStorageはtest間で毎回クリアする（`src/test/setup.ts`）。

## 5. Contract fixtures

fixtureを次に分ける。

```text
apps/ui/src/test/fixtures/
├── m4.5-catalog.json
├── m4-success-minimal.json
├── m4-success-duplicate-definition.json
├── success-unknown-event.json
├── error-invalid-command.json
├── error-capacity.json
└── malformed-success.json
```

- 手作業でAPIの巨大responseを再現せず、backend contract testまたは実OpenAPI fixtureから更新する。
- fixtureへ日時やrandom IDの不要な変動を入れない。
- backend wire DTOに変更があったPRはUI fixture testも実行する。
- OpenAPI型生成を導入した場合、生成差分がcleanであることをCIで確認する。

## 6. E2E scenarios

### Mock API E2E

- `UI-E2E-001`: page load → 味方unit選択 → 敵unit選択 → turn設定 → submit → summary表示。
- `UI-E2E-002`: memory dialogの全itemが選択できる。
- `UI-E2E-003`: API 422で該当unit slotを強調する。
- `UI-E2E-004`: API 503で前回結果を保持し再試行操作を表示する。
- `UI-E2E-005`: events → transitions → JSONを切り替え、JSONをcopyする。
- `UI-E2E-006`: `/muvluvgg_battle_simulator/` 配下でreloadしてasset 404がない。
- `UI-E2E-007`: 390px viewportで編成3枠の意味と主要操作が維持される。
- `UI-E2E-008`: keyboardだけでunit選択から結果tabまで操作する。
- `UI-E2E-009`: HEAL列がfixtureの実回復量と一致し、付与された効果・状態異常をイベントとユニット状態の双方から辿れる。
- `UI-E2E-010`: DAMAGE列が実HPダメージと一致し、calculated・シールド吸収・サブユニット吸収・HPダメージをイベント要約、シールド／サブユニット状態、状態遷移deltaの3か所から混同せず辿れる。
- `UI-E2E-011`: ドラッグ&ドロップで同一陣営内の移動・入れ替えができ、陣営を跨ぐdropは編成を変更しない。
- `UI-E2E-012`: キーボードだけでユニット枠の移動ができる（`UI-NFR-001`の拡張）。
- `UI-E2E-013`: 戦術演習の敵選択が演習専用ユニットだけを開催中／開催終了バッジ付きで出し、通常戦闘・演習味方の選択には現れない。
- `UI-E2E-014`: 戦術演習の敵を後衛の枠へ置き直して実行すると、その座標がリクエストの`enemyFormation`へ載る。
- `UI-E2E-015`: 戦術演習で実行モードを統計実行へ切り替えると実行回数・シードが現れ、400試行が300＋100のチャンクへ割れて`<seed>#<runOffset>`で送られ、集計試行数が表示される。単一実行へ戻すと単発実行に戻る。

- `UI-E2E-016`: 統計実行の完了で演習統計サマリとキャラ別統計が現れ、上位Nの切り替えが評価APIを呼び直さずに効き、全run生データCSVをダウンロードできる。

戦術演習の縦切り（`e2e/tactical-exercise.spec.ts`）のブレイク履歴の検証には発生源列を含める（Issue #538）。visual baselineは通常戦闘の画面だけを撮っており演習の結果画面を含まないため、この列の追加では貼り替えを要さない。

同じ理由で、実行モード切替（Issue #539）もvisual baselineの貼り替えを要さない。baselineの3枚はいずれも通常戦闘モードで撮っており、演習の実行パラメータは写っていない。演習の縦切りからはログレベルの切替（`selectDetailedLogLevel`）を外す — 単一実行は常に`DETAILED`で送るため、切り替える対象そのものが無い。

### Deployed API E2E

- `UI-E2E-LIVE-001`: Pages相当originからOPTIONS preflightが成功する。
- `UI-E2E-LIVE-002`: 最小1対1をPOSTし200を表示する。
- `UI-E2E-LIVE-003`: `X-Request-Id`をresponse headerから読める。
- `UI-E2E-LIVE-004`: 一覧API Catalog revisionと戦闘API response revisionを比較する。
- `UI-E2E-LIVE-005`: 一覧APIのETag/304と選択可否を確認する。

Live testは乱数結果の具体値を固定しない。schema、完了、unit対応、集計不変条件を検証する。

## 7. Visual regression

採用HTMLモックから次の視覚的不変条件を抽出する。

- dark systematic theme
- 味方blue、敵pink
- desktopで左右編成
- FRONT/REAR各3枠
- parameter barと主要submit CTA
- outcome strip
- 味方・敵summary table
- 3つのdetails tab

1440×900と390×844を最低baselineとする。動的時刻、Battle ID、画像取得を固定して撮影する。

統計実行の可視化パネル（Issue #542）は既存3枚に写らない画面なので、1440×900の統計結果画面を4枚目のbaselineとして追加する。統計量はすべてmock APIの決定的な生値から出るため、追加のマスクは要らない。

pixel完全一致だけを合否にせず、意図した変更ではbaselineをレビュー更新する。HTMLモックをPlaywright testの実装依存として読み込まない。

## 8. Accessibility tests

### 自動

- axe等による重大/深刻violation 0件
- accessible name欠落
- form label関連
- dialog name/modal
- tab role関連
- table header
- contrast（自動判定可能範囲）

### 手動

- keyboard focus順
- focus visibility
- dialog focus trapと復帰
- screen readerでslot、validation、実行状態、summaryを理解できるか
- 200% zoom
- reduced motion
- Windows High Contrastまたはforced colorsで主要操作を判別できるか

## 9. CI品質ゲート

PRで必須：

1. format check
2. typecheck
3. lint
4. unit/component test（カバレッジ計測・下限検証を含む）
5. UI build
6. E2E mock API
7. accessibility自動検査

mainで追加：

1. backend既存full quality gate
2. Pages artifact build
3. deploy
4. deployment smoke test

Live API E2EはAPI環境の可用性に依存するため、初期は別jobとし、必須化の可否を運用実績から決める。失敗を無視するのではなくjob statusを残す。

## 10. Coverage方針

- line coverageの数値達成だけを目的にしない。
- request mapper、validator、summary projector、error normalizerはbranch coverage 100%を目標とする。
- componentは主要利用者操作とfailure stateを網羅する。
- CSSとAPI contract fixtureをcoverage対象から除外してよい。
- snapshot testだけでcomponent挙動を保証しない。

計測は`@vitest/coverage-v8`で行い、`mise run ui:test:coverage`（PR CIと同等）が下限を検証する。

- 対象は`src/**/*.{ts,tsx}`全体。テストファイル・`src/test/`・`src/main.tsx`・型宣言のみのファイルは除外する。
- 下限はAPI側（`apps/api/vitest.config.ts`）と同水準のlines、functions、branches、statements各80%。導入時の実測baseline（lines 90%／functions 94%／branches 85%／statements 90%）は下限を上回っており、80%は退行ガードとして機能する。
- 下限はbaselineと重要度を確認しながら段階的に引き上げる。退行を許容するための引き下げは行わない。

## 11. 完了条件

- `UI-TEST-001`: `UI-AC-*`の各項目が最低1つのautomated testまたは明示したmanual testへ対応する。
- `UI-TEST-002`: request座標変換とsummary集計がpure unit testで固定される。
- `UI-TEST-003`: unknown event fixtureが正常表示される。
- `UI-TEST-004`: GitHub Pages base pathをE2Eで検証する。
- `UI-TEST-005`: CORSを含む実API最小縦切りをrelease前に確認する。
- `UI-TEST-006`: 主要2viewportのvisual reviewが完了する。
- `UI-TEST-007`: keyboardとscreen readerの手動確認記録がある。
