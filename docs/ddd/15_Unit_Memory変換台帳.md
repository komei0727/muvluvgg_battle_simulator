# Unit / Memory 変換台帳

## 目的

Issue #47（[Catalog] M2前提として残UnitとMemoryの基礎Catalogを整備する）の一環として、`raw/units/`・`raw/memories/` の全件を棚卸しし、production Catalog 候補（`catalog-src/`）への変換状況を機械的に追跡する。

前提文書: [`14_Catalog定義スキーマ.md`](./14_Catalog定義スキーマ.md)

## 件数サマリ

| 区分            | 総数 | 済み                                                                                                                                                                     | 未変換 |
| --------------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| `raw/units/`    | 69   | 69（代表10 + Issue #47先行バッチ12 + Issue #55 Batch A 8 + Issue #59 Batch B 8 + Issue #57 Batch C 8 + Issue #56 Batch D 8 + Issue #58 Batch E 8 + Issue #60 Batch F 7） | 0      |
| `raw/memories/` | 32   | 32（Issue #47先行バッチ6 + Issue #178 M7-007 静的補正バッチ6 + Issue #176 M7-008 所属・動的効果バッチ20）                                                                | 0      |

代表10ユニットは Issue #41 / #46 で production Catalog（`catalog-src/units/`）へ昇格済み。Issue #47 では「先行バッチ」として指定された12ユニット・6 Memory を同水準（`unit.json`/`skills.json`/`effects.json` フル変換）で追加した。Issue #55（Issue #54 Batch A）では既存キャラクターの別バージョンを中心とした8ユニットを同水準で追加した。Issue #59（Issue #54 Batch B）では同一キャラクターの複数バージョン整合性に注意が必要な8ユニット（生駒葵・リリー・ラヴォア・一条白奈・綺羅クララの各2バージョン）を同水準で追加した。Issue #57（Issue #54 Batch C）では所属/チーム系Memoryとの将来整合性に注意が必要な8ユニット（桃園める・シエナ・クラーク・リュシー・ムーアクロフト・朽葉ラミの各2バージョン）を同水準で追加した。Issue #56（Issue #54 Batch D）では戦術/前衛寄りの8ユニット（御剣冥夜・篁唯依・オルガ・ヴォルコワ・榊野ヒイロ・珠瀬壬姫・ノエル・アルエ・タリサ・マナンダル・姜小花）を同水準で追加した。Issue #58（Issue #54 Batch E）では支援/制御/イベント色の強い8ユニット（鳴滝七彩・姫川泉花（クリスマスコーデの参謀）・姫川泉花（自称腹黒の深謀策士）・樋向心香・大賀真桜・榊千鶴・ミリアム・ヘイワード・ルナ・メロウ）を同水準で追加した。Issue #60（Issue #54 Batch F）では残っていた最後の7ユニット（エレーナ・パステルコワ・タチアナ・ドロズドヴァ・ロージー・ヒューズ・波瀬うるう・レイヴェル・ブライトリーフ・ナージャ・ヴォルコワ・ジュリー・ステイシー）を同水準で追加し、`raw/units/` の全件変換が完了した。いずれも本Issueで初変換のキャラクターのため、`characterId`・`unitDefinitionId` を新規採番した。ナージャ・ヴォルコワの `characterId`（`CHAR_NADYA_VOLKOVA`）は、姓を共有する既存ユニット `UNIT_OLGA_VETERAN`（`CHAR_OLGA_VOLKOVA`）とは別キャラクターとして区別した。

`raw/` は `.gitignore` 対象（ローカルにのみ存在するスクレイピング元データ）であり、CI環境には存在しない。そのため `raw/units/`・`raw/memories/` の総数（69・32）はこの台帳上で手動管理し、`catalog-src/` 側の変換済み件数（69ユニット・32メモリー）のみを `apps/api/src/infrastructure/catalog/source/catalog-src-inventory.test.ts` でCI検証する。

`unitDefinitionId` は `UNIT_<キャラクター名>_<衣装・バージョンを表す語>` の形式へ統一している（例: `UNIT_EVIE_ECO`、`UNIT_EVIE_KYONSHI`）。キャラクター名のみのID（例: 旧`UNIT_EVIE`）は使わない。

### Issue #106: 台帳外の合成Unit（`UNIT_CI_SMOKE_TEST`）

`catalog-src/units/UNIT_CI_SMOKE_TEST/` は `raw/units/` のキャラクター変換ではない。追加時点では`capabilities.json`の30件中29件が`PLANNED`であり（Issue #166後の現在値は[`17_残作業対応表.md`](./17_残作業対応表.md)を正本とする）、実在キャラクターUnitは1件も`selectable`にならず、Cloud Run CI/CDのpost-deploy simulation smoke test（`docs/運用手順.md`「Cloud Run deploy」）が実行不能だった問題への対応として、未実装Capabilityを参照しない合成Unit（Active 1・EX 1・Passive 1、`metadata.tags: ["INTERNAL","SMOKE_TEST","TEMPORARY"]`）を追加した（PR #112レビュー、2026-07-15）。Issue #166では実装済み`CAP_SKILL_RUNTIME_COUNTER`のproduction lifecycle証跡として、通常のDAMAGE解決だけを行うPassiveを追加した。これによりdeploy smokeの選択可能性を維持したまま、`TurnStarted`から`PassiveActivated`、counter更新、StateDelta replayまでを実Catalog定義で検証する。

この台帳の69件（`raw/units/`変換）・`catalog-src-inventory.test.ts`の69件アサーション（`IT-CAT-INV-001`のraw/変換キャラクター数）には含めない。`catalog-src/`のunit directory総数は70件になる。実在キャラクターUnitが`IMPLEMENTED`済みcapabilityだけで`selectable`になり次第、このUnitを削除しCatalog revisionを再生成すること——削除条件・削除手順は`REL-002`（Issue #199）で追跡する。

## Unit 変換台帳

| raw/units/ ファイル                                  | unitDefinitionId           | characterId              | ステータス                                                          |
| ---------------------------------------------------- | -------------------------- | ------------------------ | ------------------------------------------------------------------- |
| 【はだけるわんぱく浴衣】ユリア・バーンズ             | `UNIT_YURIA_YUKATA`        | `CHAR_YURIA_BURNES`      | 済み（本Issue #55 Batch A）                                         |
| 【みんなを見守る山ガール】黒森ラウラ                 | `UNIT_LAURA_MOUNTAIN`      | `CHAR_KUROMORI_LAURA`    | 済み（#41/#46, 代表10ユニット）                                     |
| 【オールラウンダーな統率者】鳴滝七彩                 | `UNIT_NANAE_COMMANDER`     | `CHAR_NANAE_NARUTAKI`    | 済み（本Issue #58 Batch E）                                         |
| 【キョンシーハッカー】エヴィ・レーナルト             | `UNIT_EVIE_KYONSHI`        | `CHAR_EVIE_RENALT`       | 済み（本Issue #47 先行バッチ）                                      |
| 【ギャルインフルエンサー】フルート・メルヴィル       | `UNIT_FLUTE_INFLUENCER`    | `CHAR_FLUTE_MELVILLE`    | 済み（本Issue #55 Batch A） ※不完全変換あり→下表参照                |
| 【クリスマスコーデの参謀】姫川泉花                   | `UNIT_SENKA_CHRISTMAS`     | `CHAR_SENKA_HIMEKAWA`    | 済み（本Issue #58 Batch E） ※不完全変換あり→下表参照                |
| 【シリウスシュガーのエース】桃園める                 | `UNIT_MERU_SIRIUS`         | `CHAR_MERU_MOMOZONO`     | 済み（本Issue #57 Batch C） ※不完全変換あり→下表参照                |
| 【スタチュービューティー】ステラ・ブレーメル         | `UNIT_STELLA_STATUE`       | `CHAR_STELLA_BREMER`     | 済み（#41/#46, 代表10ユニット）                                     |
| 【ダウナーギャルな副委員長】カリナ・ジェンティーレ   | `UNIT_KARINA_DOWNER`       | `CHAR_KARINA_GENTILE`    | 済み（#41/#46, 代表10ユニット） ※不完全変換あり→下表参照            |
| 【ドメスティックなリーダー】榊千鶴                   | `UNIT_CHIZURU_DOMESTIC`    | `CHAR_CHIZURU_SAKAKI`    | 済み（本Issue #58 Batch E） ※不完全変換あり→下表参照                |
| 【ナチュラルボーンサバイバー】鎧衣美琴               | `UNIT_MIKOTO_SURVIVOR`     | `CHAR_YOROI_MIKOTO`      | 済み（#41/#46, 代表10ユニット）                                     |
| 【ノーブル・グレイス】ドロテア・カークランド         | `UNIT_DOROTHEA_GRACE`      | `CHAR_DOROTHEA_KIRKLAND` | 済み（本Issue #47 先行バッチ）                                      |
| 【ポンコツいいんちょ】大賀真桜                       | `UNIT_MAO_COMMITTEE`       | `CHAR_MAO_OGA`           | 済み（本Issue #58 Batch E） ※不完全変換あり→下表参照                |
| 【世界への反逆者】コトハ                             | `UNIT_KOTOHA_REBEL`        | `CHAR_KOTOHA`            | 済み（#41/#46, 代表10ユニット） ※不完全変換あり→下表参照            |
| 【享楽のワイルドカード】ユリア・バーンズ             | `UNIT_YURIA_WILDCARD`      | `CHAR_YURIA_BURNES`      | 済み（本Issue #47 先行バッチ）                                      |
| 【人見知りの聖騎士】ケイト・フルニエ                 | `UNIT_KATE_PALADIN`        | `CHAR_KATE_FOURNIER`     | 済み（#41/#46, 代表10ユニット）                                     |
| 【体育祭のサポート役】樋向心香                       | `UNIT_KOKORO_SPORTSDAY`    | `CHAR_KOKORO_HIMUKAI`    | 済み（本Issue #58 Batch E）                                         |
| 【体育祭の暴れん坊】ノエル・アルエ                   | `UNIT_NOEL_RUMBLE`         | `CHAR_NOEL_ARUE`         | 済み（本Issue #56 Batch D） ※不完全変換あり→下表参照                |
| 【優雅なる規律の花】生駒葵                           | `UNIT_AOI_ELEGANT`         | `CHAR_AOI_IKOMA`         | 済み（本Issue #59 Batch B） ※不完全変換あり→下表参照                |
| 【元気印の大魔導士】ミリアム・ヘイワード             | `UNIT_MIRIAM_MAGE`         | `CHAR_MIRIAM_HEYWARD`    | 済み（本Issue #58 Batch E） ※不完全変換あり→下表参照                |
| 【博識なハングリーガール】ルナ・メロウ               | `UNIT_LUNA_HUNGRY`         | `CHAR_LUNA_MELLOW`       | 済み（本Issue #58 Batch E） ※不完全変換あり→下表参照                |
| 【博識なメイズの探求者】月ヶ瀬ちゆる                 | `UNIT_CHIYURU_MAZE`        | `CHAR_CHIYURU_TSUKIGASE` | 済み（本Issue #47 先行バッチ） ※不完全変換あり→下表参照             |
| 【厳格な規律の守護者】生駒葵                         | `UNIT_AOI_GUARDIAN`        | `CHAR_AOI_IKOMA`         | 済み（本Issue #59 Batch B） ※不完全変換あり→下表参照                |
| 【唯一無二の幼なじみ】鑑純夏                         | `UNIT_JUNKA_CHILDHOOD`     | `CHAR_JUNKA_KAGAMI`      | 済み（本Issue #47 先行バッチ） ※不完全変換あり→下表参照             |
| 【大吉ハッピーニューイヤー】朽葉ラミ                 | `UNIT_RAMI_NEWYEAR`        | `CHAR_RAMI_KUZUHA`       | 済み（本Issue #57 Batch C） ※不完全変換あり→下表参照                |
| 【天命を受けし剣術乙女】御剣冥夜                     | `UNIT_MEIYA_FATED`         | `CHAR_MEIYA_MITSURUGI`   | 済み（本Issue #56 Batch D） ※不完全変換あり→下表参照                |
| 【天真爛漫トラブルメーカー】タリサ・マナンダル       | `UNIT_TARISA_TROUBLEMAKER` | `CHAR_TARISA_MANANDAL`   | 済み（本Issue #56 Batch D） ※不完全変換あり→下表参照                |
| 【心色見つめるムードメーカー】エレーナ・パステルコワ | `UNIT_ELENA_MOODMAKER`     | `CHAR_ELENA_PASTELKOVA`  | 済み（本Issue #60 Batch F） ※不完全変換あり→下表参照                |
| 【忠義の狂犬メイド】リュシー・ムーアクロフト         | `UNIT_LUCIE_MAID`          | `CHAR_LUCIE_MOORCROFT`   | 済み（本Issue #57 Batch C） ※不完全変換あり→下表参照                |
| 【恥じらうカジノラビット】劉翠蘭                     | `UNIT_SUIRAN_CASINO`       | `CHAR_SUIRAN_LIU`        | 済み（本Issue #55 Batch A） ※不完全変換あり→下表参照                |
| 【想い響かせるヒーローシンガー】リリー・ラヴォア     | `UNIT_LILY_SINGER`         | `CHAR_LILY_LAVOIE`       | 済み（本Issue #59 Batch B） ※不完全変換あり→下表参照                |
| 【愛を求めるトラブルメーカー】アニス・ベネット       | `UNIT_ANIS_TROUBLEMAKER`   | `CHAR_ANIS_BENNETT`      | 済み（本Issue #47 先行バッチ）                                      |
| 【憎まれ口の大賢者】ハリエット・ミルズ               | `UNIT_HARRIET_SAGE`        | `CHAR_HARRIET_MILLS`     | 済み（#41/#46, 代表10ユニット）                                     |
| 【戦うアントレプレナー】レイラ・ジェンキンス         | `UNIT_LAYLA_ENTREPRENEUR`  | `CHAR_LAYLA_JENKINS`     | 済み（本Issue #47 先行バッチ） ※不完全変換あり→下表参照             |
| 【新たなる時代の導き手】ドロテア・カークランド       | `UNIT_DOROTHEA_PIONEER`    | `CHAR_DOROTHEA_KIRKLAND` | 済み（本Issue #55 Batch A） ※不完全変換あり→下表参照                |
| 【新春のメイズ研究者】月ヶ瀬ちゆる                   | `UNIT_CHIYURU_NEWYEAR`     | `CHAR_CHIYURU_TSUKIGASE` | 済み（本Issue #55 Batch A） ※不完全変換あり→下表参照                |
| 【旋律を紡ぐ静謐のディーヴァ】シエナ・クラーク       | `UNIT_SIENA_DIVA`          | `CHAR_SIENA_CLARK`       | 済み（本Issue #57 Batch C） ※不完全変換あり→下表参照                |
| 【期待応える輝きの穹】一条白奈                       | `UNIT_SHIRANA_SORA`        | `CHAR_SHIRANA_ICHIJO`    | 済み（本Issue #59 Batch B） ※不完全変換あり→下表参照                |
| 【正々堂々なミス・ツンデレ】綺羅クララ               | `UNIT_CLARA_TSUNDERE`      | `CHAR_CLARA_KIRA`        | 済み（本Issue #59 Batch B） ※不完全変換あり→下表参照                |
| 【正義のヒーロー】リリー・ラヴォア                   | `UNIT_LILY_HERO`           | `CHAR_LILY_LAVOIE`       | 済み（本Issue #59 Batch B） ※不完全変換あり→下表参照                |
| 【歴戦の鉄母】オルガ・ヴォルコワ                     | `UNIT_OLGA_VETERAN`        | `CHAR_OLGA_VOLKOVA`      | 済み（本Issue #56 Batch D） ※不完全変換あり→下表参照                |
| 【気高きランウェイモデル】レイヴェル・ブライトリーフ | `UNIT_RAVEL_MODEL`         | `CHAR_RAVEL_BRIGHTLEAF`  | 済み（本Issue #60 Batch F） ※不完全変換あり→下表参照                |
| 【混沌の立役者】劉翠蘭                               | `UNIT_SUIRAN_CHAOS`        | `CHAR_SUIRAN_LIU`        | 済み（本Issue #47 先行バッチ） ※不完全変換あり→下表参照             |
| 【渇望秘めし淑女】紫雲沙耶                           | `UNIT_SAYA_LONGING`        | `CHAR_SAYA_SHIUN`        | 済み（本Issue #47 先行バッチ）                                      |
| 【温厚篤実な面倒くさがり】夕凪舞亜                   | `UNIT_MAIA_LAZY`           | `CHAR_MAIA_YUNAGI`       | 済み（本Issue #55 Batch A） ※不完全変換あり→下表参照                |
| 【無邪気なジャックナイフ】彩峰慧                     | `UNIT_KEI_JACKKNIFE`       | `CHAR_KEI_AYAMINE`       | 済み（本Issue #47 先行バッチ） ※不完全変換あり→下表参照             |
| 【理解深き老成の智者】タチアナ・ドロズドヴァ         | `UNIT_TATIANA_SAGE`        | `CHAR_TATIANA_DROZDOVA`  | 済み（本Issue #60 Batch F） ※不完全変換あり→下表参照                |
| 【発情バニー】紫雲沙耶                               | `UNIT_SAYA_BUNNY`          | `CHAR_SAYA_SHIUN`        | 済み（本Issue #47 先行バッチ） ※不完全変換あり→下表参照             |
| 【省エネ主義の天才ハッカー】エヴィ・レーナルト       | `UNIT_EVIE_ECO`            | `CHAR_EVIE_RENALT`       | 済み（#41/#46, 代表10ユニット）                                     |
| 【眠れる社交界の淑女】夕凪舞亜                       | `UNIT_MAIA_SALON`          | `CHAR_MAIA_YUNAGI`       | 済み（本Issue #55 Batch A） ※不完全変換あり→下表参照                |
| 【稀代の狙撃手】珠瀬壬姫                             | `UNIT_MIHIME_SNIPER`       | `CHAR_MIHIME_TAMASE`     | 済み（本Issue #56 Batch D） ※不完全変換あり→下表参照                |
| 【空っぽのアクター】フィー・ドレーゼ                 | `UNIT_FEE_ACTOR`           | `CHAR_FEE_DREZE`         | 済み（本Issue #47 先行バッチ）                                      |
| 【空想造形アーティスト】ロージー・ヒューズ           | `UNIT_ROSIE_ARTIST`        | `CHAR_ROSIE_HUGHES`      | 済み（本Issue #60 Batch F） ※不完全変換あり→下表参照                |
| 【純白のラッキーガール】一条白奈                     | `UNIT_SHIRANA_LUCKY`       | `CHAR_SHIRANA_ICHIJO`    | 済み（本Issue #59 Batch B） ※不完全変換あり→下表参照                |
| 【純真無垢なるジーニアス】リディア・エルドリッジ     | `UNIT_LYDIA_GENIUS`        | `CHAR_LYDIA_ELDRIDGE`    | 済み（#41/#46, 代表10ユニット） ※不完全変換あり→下表参照            |
| 【緋色の一匹狼】榊野ヒイロ                           | `UNIT_HIIRO_LONEWOLF`      | `CHAR_HIIRO_SAKAKINO`    | 済み（本Issue #56 Batch D） ※不完全変換あり→下表参照                |
| 【聖夜のサンタシンガー】綺羅クララ                   | `UNIT_CLARA_SANTA`         | `CHAR_CLARA_KIRA`        | 済み（本Issue #59 Batch B） ※不完全変換あり→下表参照                |
| 【臆病な褒められたがり少女】波瀬うるう               | `UNIT_URUU_TIMID`          | `CHAR_URUU_HASE`         | 済み（本Issue #60 Batch F） ※不完全変換あり→下表参照                |
| 【自己に揺れる白湯気】フィー・ドレーゼ               | `UNIT_FEE_BATH`            | `CHAR_FEE_DREZE`         | 済み（本Issue #55 Batch A） ※不完全変換あり→下表参照                |
| 【自称腹黒の深謀策士】姫川泉花                       | `UNIT_SENKA_SCHEMER`       | `CHAR_SENKA_HIMEKAWA`    | 済み（本Issue #58 Batch E）                                         |
| 【舞台を降りた元歌姫】シエナ・クラーク               | `UNIT_SIENA_OFFSTAGE`      | `CHAR_SIENA_CLARK`       | 済み（本Issue #57 Batch C）                                         |
| 【蒼き穹舞うフラットスピン】桃園める                 | `UNIT_MERU_FLATSPIN`       | `CHAR_MERU_MOMOZONO`     | 済み（本Issue #57 Batch C。唯一の不完全変換はIssue #129で解消済み） |
| 【譜代武家・篁家次期当主】篁唯依                     | `UNIT_YUI_HEIR`            | `CHAR_YUI_TAKAMURA`      | 済み（本Issue #56 Batch D） ※不完全変換あり→下表参照                |
| 【負けず嫌いな不屈少女】朽葉ラミ                     | `UNIT_RAMI_UNYIELDING`     | `CHAR_RAMI_KUZUHA`       | 済み（本Issue #57 Batch C） ※不完全変換あり→下表参照                |
| 【輝ける次代の娘】ナージャ・ヴォルコワ               | `UNIT_NADYA_SUCCESSOR`     | `CHAR_NADYA_VOLKOVA`     | 済み（本Issue #60 Batch F） ※不完全変換あり→下表参照                |
| 【連れ添い歩む傍らの友】リュシー・ムーアクロフト     | `UNIT_LUCIE_COMPANION`     | `CHAR_LUCIE_MOORCROFT`   | 済み（本Issue #57 Batch C） ※不完全変換あり→下表参照                |
| 【雪山もこもこ少女】ジュリー・ステイシー             | `UNIT_JULIE_SNOW`          | `CHAR_JULIE_STACEY`      | 済み（本Issue #60 Batch F） ※不完全変換あり→下表参照                |
| 【風紀委員会の策謀家】姜小花                         | `UNIT_SHOUKA_SCHEMER`      | `CHAR_SHOUKA_KYOU`       | 済み（本Issue #56 Batch D） ※不完全変換あり→下表参照                |
| 【＃激カワ吸血鬼配信者♪】フルート・メルヴィル        | `UNIT_FLUTE_VAMPIRE`       | `CHAR_FLUTE_MELVILLE`    | 済み（#41/#46, 代表10ユニット）                                     |

### 不完全変換の詳細（※印のユニット）

上表で「※不完全変換あり」と記載した50ユニット（#41/#46代表10ユニット3 + Issue #47先行バッチ6 + Issue #55 Batch A 7 + Issue #59 Batch B 8 + Issue #57 Batch C 6 + Issue #56 Batch D 8 + Issue #58 Batch E 5 + Issue #60 Batch F 7）について、raw記載の効果のうちCatalog v2の現行スキーマでは表現しきれず、近似または省略した箇所を一覧化する。いずれもM2の基礎Catalog項目（Unit ID・属性・タイプ・ロール・配置適性・基礎ステータス・AP/PP/EX関連値）には影響しない。スキル効果の完全表現は本Issueのスコープ外（Issue本文の「Out of scope」参照）であり、対応する`EffectActionDefinition`/`ConditionDefinition`/`TriggerDefinition`拡張が実装されるまで、後続Issueで解消する。

各行の `対応予定`（解消可能になる最初のマイルストーン）・`対応テーマ`（横断修正用の分類名）の定義とルールは [`16_不完全変換対応予定方針.md`](./16_不完全変換対応予定方針.md) を参照。

2026-07-20時点の全96行（M7相当62行、M8相当34行）と実装Taskの機械可読な対応は [`17_残作業対応表.json`](./17_残作業対応表.json) を正とする。台帳の行を追加・除去・再分類するPRは同JSONも更新し、`remaining-work.test.ts`で件数とテーマの一致を検証する。

#41/#46代表10ユニットのうち3件（カリナ・ジェンティーレ、コトハ、リディア・エルドリッジ）は、Issue #54の最終レビューで新たに不完全変換が確認されたため下表に追加した。

Issue #57 Batch Cでは、raw文の「対象が状態異常だった場合」という汎用条件を、`TARGET_STATE.field: HAS_STATUS`で判定可能な負の状態異常3種（`STUN`/`FREEZE`/`BLIND`）の`OR`条件として変換した（`UNIT_MERU_FLATSPIN`のAS2/AS3）。これは近似ではなくCatalog v2で表現可能な負の状態異常を網羅した解釈であり、下表には含めない。

Issue #129では、`COOLDOWN_MANIPULATION`（他スキルのクールタイムをリセット・短縮する`EffectAction`）を追加し、`UNIT_SAYA_BUNNY`（`SKL_SAYA_BUNNY_EX`）、`UNIT_CHIYURU_NEWYEAR`（`SKL_CHIYURU_NEWYEAR_EX`/`SKL_CHIYURU_NEWYEAR_PS1`）、`UNIT_AOI_ELEGANT`（`SKL_AOI_ELEGANT_EX`）、`UNIT_MERU_FLATSPIN`（`SKL_MERU_FLATSPIN_AS1`）の対応テーマ`COOLDOWN_MANIPULATION`の4行を解消した。解消済みの行は下表から除去する。

Issue #146（本Issue）でM6/M7の責務境界を確定した対応テーマは、次の実装Issueが解消する。実装完了・production Catalog統合テスト成功までは行を先に削除せず、`COOLDOWN_MANIPULATION`（Issue #129）と同じ運用で、該当Issueの受け入れ条件達成後に下表から除去する。テストケースID・トレーサビリティの定義は [`12_テスト戦略.md`](./12_テスト戦略.md)「不完全変換解消の統合テスト」を参照。

RES-004（Issue #171後半）では、`CAP_EFFECT_STEP_CONDITION`（ACTION stepのconditionが自身のtargetを参照するTARGET_STATE/TARGET_HAS_MARKERを対象ごとに個別評価する）を追加し、`UNIT_AOI_ELEGANT`（`SKL_AOI_ELEGANT_EX`）、`UNIT_LUCIE_MAID`（`SKL_LUCIE_MAID_AS1`/`SKL_LUCIE_MAID_PS2`）、`UNIT_ROSIE_ARTIST`（`SKL_ROSIE_ARTIST_PS2`）の対応テーマ`AOE_PER_TARGET_CONDITION`の4行を解消した。解消済みの行は下表から除去する。

Issue #225では、`muvluv-girls-garden.wikiru.jp`のタチアナ・ドロズドヴァのページを`muvluvgg-unit-md-from-wiki`スキルで再取得し（2026-07-24、`raw/units/【理解深き老成の智者】タチアナ・ドロズドヴァ.md`へ保存）、スキル1「静かな時を」の原文を確定した。`raw/`は`.gitignore`対象でCI環境に存在しないため、監査可能性のため原文を本書へ逐語引用する:

> 効果： 敵全体に威力169.6でEN攻撃する。攻撃時に対象が「凶兆」を2つ以上所持していた場合、さらに対象の次の攻撃での与ダメージを100％減少させるデバフを付与する。対象が「凶兆」を2つ所持していなかった場合、「凶兆」を1つ付与する

この原文に基づき、`UNIT_TATIANA_SAGE`（`SKL_TATIANA_SAGE_EX`）の対象別分岐自体（「凶兆」2つ以上でデバフ、未満でマーカー付与、`TARGET_HAS_MARKER`/`NOT(...)`の対象別ACTION step条件）は`AOE_PER_TARGET_CONDITION`と同じ理由で解消し、下表から除去した。当時はデバフ本体（`ACT_TATIANA_SAGE_EX_DEBUFF`）が参照する`EffectActionDefinition.kind: "APPLY_DAMAGE_MOD"`が`effect-action-group-resolver.ts`に未実装だったため、この残存部分を`DAMAGE_MOD_KIND_UNIMPLEMENTED`として下表の別行に分離・再分類していた（`DMG-002`／Issue #192が実装して解消済み。下記参照）。

`DMG-002`のmerge後、Issue #225は残る実ライフサイクル検証を`tatiana-sage-ex-omen-branch-production-catalog.test.ts`（`IT-CAP-TATIANA-OMEN-PROD-001`〜`005`）で完了した。実`catalog/`の`SKL_TATIANA_SAGE_EX`を無改変のまま実`resolveSkillUse`へ通し、「凶兆」0件・1件（しきい値直前）・2件（一致）・3件（超過）が同一AOEに混在する解決で、ダメージが全対象へ、デバフが2件以上の対象だけへ、Marker追加が2件未満の対象だけへ振り分けられること、Domain Event（`EffectApplied`／`MarkerApplied`／`MarkerUpdated`／`DamageApplied`）・StateDelta・`stateVersionBefore`/`After`の単調性・単一rootの因果木・初期スナップショットからの独立Reducer復元までを固定した。

1点だけ、Issue #225本文の受け入れ条件「デバフ対象の次の攻撃が0ダメージになり」は実装と一致しない。`ACT_TATIANA_SAGE_EX_DEBUFF`（`direction: OUTGOING`／`CONSTANT -1.0`）は与ダメージ倍率を確かに0まで落とす（`DamageCalculated.outgoingDamageMultiplier: 0`、`preTruncationDamage: 0`）が、最終ダメージは`R-DMG-02`（最終化）#3「計算結果が1未満の場合も1とする」で1へ引き上げられる。[`07_戦闘ルール詳細.md`](./07_戦闘ルール詳細.md)冒頭が「最低1ダメージ……の全体不変条件は、明示的に上書きを許可した仕様がない限り変更しない」と定めており、`APPLY_DAMAGE_MOD`はその上書きを宣言していないため、より詳細な`R-DMG-02`が優先する。近似ではなく規則どおりの結果であり、Catalog定義は変更していない。

`UNIT_LYDIA_GENIUS`（`SKL_LYDIA_GENIUS_AS1`、列などの絞り込み後の対象数を発動条件にする「集合条件」は本Capabilityでは未対応）の1行は引き続き残す。`UNIT_CHIYURU_MAZE`（`SKL_CHIYURU_MAZE_EX`）は`RES-004-STATUS-CONDITION`／Issue #224 が解消した（下記）。

M7-001E（Issue #248）では、`ConditionDefinition.kind: TARGET_HAS_EFFECT`（`CAP_TARGET_EFFECT_QUERY`）と`TARGET_STATE`の`HAS_STATUS`/`UNIT_TYPE`/`ROLE`評価（`CAP_TARGET_STATE_EXTENDED_FIELD`）を追加し、対応テーマ`TARGET_STATE_QUERY_BUFF_DEBUFF`の5行（`UNIT_CHIYURU_MAZE`／`UNIT_FLUTE_INFLUENCER`／`UNIT_MAIA_LAZY`／`UNIT_NOEL_RUMBLE`／`UNIT_SHOUKA_SCHEMER`）を解消した。分類軸は`REMOVE_EFFECTS`/`EFFECT_IMMUNITY`と同じ`effect-category-classifier.ts`を正本とし、毒・炎上は`continuousDamageKinds`、「対象の**攻撃力**がデバフ」は`statKinds`で絞り込むため、いずれの行も「何らかのデバフ」への近似を残さない。`UNIT_MAIA_LAZY`の「自身を除く味方全体」は既存の`EXCLUDE_RESOLVED_UNIT`フィルタ（TGT-002）で表し、`SKL_ELENA_MOODMAKER_AS1`の`TGT_OTHER_ALLIES`と同じ形になる（R-TGT-01 #4により、他に味方が居ない編成ではこのスキル自体が選択されない点も同じ）。解消済みの行は下表から除去する。

RES-004-STATUS-CONDITION（Issue #224）では、`AOE_PER_TARGET_CONDITION`の残り1行（`UNIT_CHIYURU_MAZE`／`SKL_CHIYURU_MAZE_EX`）を解消した。ブロッカーは対象別評価そのものではなく（`CAP_EFFECT_STEP_CONDITION`が既に実装済み）、「対象が状態異常にある場合」を表す分類が無いことだった。M7-001E時点の`STATUS`カテゴリは`APPLY_STATUS`の気絶・凍結・暗闇だけを含み、同じく状態異常である炎上・毒（`APPLY_CONTINUOUS_DAMAGE`、Markerではなく`AppliedEffect`として保持されるため`TARGET_HAS_MARKER`では代替できない）が漏れていた。[`01_ユビキタス言語.md`](./01_ユビキタス言語.md)「状態異常」が定義する5種を`effect-category-classifier.ts`（唯一の分類元）が漏れなく`STATUS`へ分類するようにし、Catalog側は`TARGET_HAS_EFFECT.categories: ["STATUS"]`だけを書く（種別を列挙しない）形に統一した。`SKL_CHIYURU_MAZE_EX`はこれをAOE（敵全体）の`targetCondition`として使い、条件成立対象にだけ1行動の気絶（`ACT_CHIYURU_MAZE_EX_STUN`）と一度だけの被ダメージ+100%デバフ（`ACT_CHIYURU_MAZE_EX_DAMAGE_TAKEN_UP`、`NEXT_INCOMING_ATTACK`で消費・重複可）を適用する。あわせて、M7-001Eが同じ総称を`HAS_STATUS`の3項ORで近似していた4定義（`SKL_MERU_FLATSPIN_AS1`〜`AS3`・`SKL_NANAE_COMMANDER_PS1`）も同じ形へ寄せ、分類元を1つに保った。解消済みの行は下表から除去する。

RES-004後続（Issue #227）では、`ConditionDefinition.kind: TARGET_SET_COUNT`と`CAP_EFFECT_STEP_SET_CONDITION`を追加し、Area/TargetFilterで絞り込んだ後の対象集合の存在・件数をしきい値判定できるようになった。ただし`UNIT_LYDIA_GENIUS`（`SKL_LYDIA_GENIUS_AS1`）と`UNIT_ELENA_MOODMAKER`（`SKL_ELENA_MOODMAKER_AS1`）の不発条件はいずれもEffectStepの条件ではなくAS自身の`activationCondition`（行動選択時に評価、`CAP_ACTION_ACTIVATION_CONDITION`）であり、本Capabilityの完了境界（`EffectStep`の`ACTION`/`BRANCH`条件）には含まれない。この2行は`CAP_ACTION_ACTIVATION_CONDITION`（#180、M7-003）がactivationConditionへの`TARGET_SET_COUNT`利用を実装し、production Catalog統合テストに成功するまで下表に残す（`IT-CAP-ACTION-ACTIVATION-CONDITION-001`〜`005`）。

Issue #180（M7-003）では、`action-selection-policy.ts`のisUsable/isExUsableが`evaluateEffectStepCondition`（TargetBinding解決後の最新対象集合に対しSELF/BINDINGのTargetReferenceを解決）を再利用してAS/EXの`activationCondition`を評価するようになった。`UNIT_LYDIA_GENIUS`（`SKL_LYDIA_GENIUS_AS1`、既存の`TGT_COLUMNS`bindingへ`TARGET_SET_COUNT`のactivationConditionを追加）はこれで不発条件が完全に解消したため下表から除去する。`UNIT_ELENA_MOODMAKER`（`SKL_ELENA_MOODMAKER_AS1`）は不発条件自体（`TGT_LOW_HP_ALLIES`/`TGT_OTHER_ALLIES`の2つの新規bindingへの`TARGET_SET_COUNT`、既存のSELF HP_RATIO`TARGET_STATE`とのAND）は解消したが、コスト（自身HPの10%消費）と回復リンク（回復効果100%自身へ転送）はいずれも別スコープ（それぞれ#185/M7-002、#229/M7-005）のため、下表の行は`HP_DIRECT_COST`テーマへ再分類して残す。

M7-001C（Issue #244）では、`REMOVE_BUFF_CATEGORY`の残り4行（`UNIT_NOEL_RUMBLE`のPS2、`UNIT_SHOUKA_SCHEMER`のEX・AS3、`UNIT_SENKA_CHRISTMAS`のPS2）を`REMOVE_EFFECTS`（`categories: ["BUFF"]`、EX/AS3は`maxRemovals`併用）へ変換し解消した。Senka PS2は識別用Marker付与（`ACT_SENKA_CHRISTMAS_PS2_MARK`）による近似を実際のバフ解除へ置き換えたため、当該Marker定義自体を削除した。解消済みの行は下表から除去する。`UNIT_TARISA_TROUBLEMAKER`（`SKL_TARISA_TROUBLEMAKER_PS1`）の残り1行（「与えたダメージが10以下だった場合、『負けん気』を3つ解除する」）は、当初の理由（REMOVE_MARKERがcount未対応）はM7-001（Issue #181）で既に解消済みだったが、真のブロッカーは別にあると判明した：この解除は「トリガーとなった`DamageApplied`イベントのpayload（`calculatedDamage`）」で条件付ける必要があるが、`EVENT_PAYLOAD`条件は`triggers[].condition`（`evaluateTriggerCondition`）でしか評価できず、`resolution.steps`側の条件評価器（`evaluateEffectStepCondition`、`LAST_RESULT`/`TARGET_STATE`等のみ対応）はトリガーイベントのpayloadを一切受け取らない。対応テーマを`REMOVE_EFFECTS_COUNT_LIMIT`から新設の`TRIGGER_PAYLOAD_IN_RESOLUTION`へ再分類し、`M7-010`（Issue #177、監査）は「新機能実装は含めない」旨を明記しているため、専用task`M7-001D`（Issue #247）へ引き継いだ。

M7-001D（Issue #247）では、`resolution.steps`（`ACTION.stepCondition`/`ACTION.targetCondition`/`BRANCH.condition`、Issueが明示する3スコープ全て）から`EVENT_PAYLOAD`を評価できる仕組み（CAP_TRIGGER_PAYLOAD_IN_RESOLUTION）を実装し、`UNIT_TARISA_TROUBLEMAKER`の残り1行を`REMOVE_MARKER`（`markerId: MARKER_TARISA_TROUBLEMAKER_FIGHTING_SPIRIT`, `count: 3`）を`stepCondition: {kind: EVENT_PAYLOAD, field: calculatedDamage, op: LTE, value: 10}`で条件付ける形へ変換し解消した。`IT-CAP-TRIGGER-PAYLOAD-RES-PROD-001`〜`003`が近似なしの実行結果（両分岐・Domain Event・StateDelta・独立Reducer復元）を確認した。解消済みの行は下表から除去する。

- `TRIGGER_EXCLUSION_TIMING`（3行）・`TRIGGER_PAYLOAD_FILTER`（2行）：Condition APIはIssue #144で実装済み。`RES-005`（Issue #172）が`EffectApplied`/`UnitBeingAttacked`/`HitPointReduced`の実行時発行と`TRIGGER_TARGET`/`TRIGGER_SOURCE`解決を実装したが、対象行の真のブロッカーはこれらとは別の未実装ギャップ（`EffectApplied`のpayloadに`category`/`effectKind`分類フィールドが存在しない）と判明したため、担当は`M7-010`（Issue #177、監査）へ引き継ぐ
- `DAMAGE_MOD_KIND_UNIMPLEMENTED`（2行）：`DMG-002`（Issue #192）

M7-010（Issue #177、M7完了監査）は、引き継いだこの5行が「`EffectApplied`のpayloadに`category`/`effectKind`分類フィールドが存在しない」という**単一の原因**へ完全に収束していることを確認した。除外タイミング側（「戦闘開始時・ターン開始時・ターン終了時には発動しない」）は`RESOLUTION_PHASE`で表現済み・実行時評価済みで、`SKL_SIENA_DIVA_PS1`は実際に`negate: true`の`RESOLUTION_PHASE`条件を3つ宣言している。テーマ名`TRIGGER_EXCLUSION_TIMING`／`TRIGGER_PAYLOAD_FILTER`はもはや実際のブロッカーを指していないため、「同じ原因は同じ対応テーマに寄せる」という[`16_不完全変換対応予定方針.md`](./16_不完全変換対応予定方針.md)の運用ルールに従い、5行を新テーマ`EFFECT_APPLIED_CLASSIFICATION_PAYLOAD`（`M7`）へ統合し、専用Task`M7-011`（Issue #265）へ引き継いだ。監査Issueは「新機能実装は含めない」ため、payload拡張自体は本監査では実装しない。

M7-011（Issue #265）は、その単一原因である分類payloadを`EffectApplied`へ追加し、`EFFECT_APPLIED_CLASSIFICATION_PAYLOAD`の5行すべてを近似なしへ解消して下表から除去した。`effect-grant-service.ts`は付与時に`EffectActionDefinition`そのものを受け取り、効果の種類（`effectKind`）と、解除・免疫判定の正本である`effect-category-classifier.ts`が導く分類集合（`categories`、R-STS-01により状態異常は`STATUS`と`DEBUFF`の両方を持つ配列）をpayloadへ載せる。trigger側は`categories`を`op: CONTAINS`で判定する。変換は`SKL_KEI_JACKKNIFE_PS2`・`SKL_LILY_SINGER_PS1`（`categories CONTAINS DEBUFF`）、`SKL_SIENA_DIVA_PS1`・`SKL_NADYA_SUCCESSOR_PS1`（`categories CONTAINS STATUS`。「状態異常」はSTEALTH等の有利な`APPLY_STATUS`を含まないため`effectKind: APPLY_STATUS`では広すぎる）、`SKL_URUU_TIMID_PS3`（`categories CONTAINS DEBUFF`）、`SKL_NADYA_SUCCESSOR_PS2`（`statusKind EQ STUN`）で、`SKL_NADYA_SUCCESSOR_PS1`/`PS2`にはraw原文が併記する除外タイミング（`RESOLUTION_PHASE`の`negate: true`3件）も併せて宣言した。`SKL_NADYA_SUCCESSOR_PS2`の`sourceSelector`はraw原文が付与者を限定していない（「敵に気絶が付与された際に発動」）ため`SELF`→`ANY`へ訂正した — 同じ形の`SKL_KEI_JACKKNIFE_PS2`/`SKL_SIENA_DIVA_PS1`/`SKL_KATE_PALADIN_PS1`はいずれも`ANY`である。実ライフサイクル（実カタログ→実付与サービスが発行した`EffectApplied`→候補検出→PS発動）での確認は`IT-CAP-TRIGGER-PAYLOAD-PROD-001`〜`007`、Catalog定義の固定は`IT-CAT-PROD-013`。`CAP_DAMAGE_MOD`（M8/`DMG-002`）・`CAP_SUBUNIT`（M8/`DMG-005`）へ依存する`SKL_LILY_SINGER_PS1`/`SKL_URUU_TIMID_PS3`/`SKL_NADYA_SUCCESSOR_PS1`/`PS2`は、EffectSequenceの完全解決だけが別テーマ・別行として残る（本テーマのtrigger側ギャップは解消済み）。

M7-014（Issue #268）は`DYNAMIC_DURATION_ON_REAPPLY`の1行（`UNIT_SIENA_DIVA`／`SKL_SIENA_DIVA_PS1`）を近似なしへ解消して下表から除去し、その表現契約を`R-EFF-12`「再付与時の動的期間」として`07_戦闘ルール詳細.md`へ新設した（Rule総数112→113）。`DurationDefinition.reapply`（`existingRemaining`の数値比較と差し替え後の`count`）を追加し、`ACT_SIENA_DIVA_PS1_STUN`を`timeLimit: {ACTION, 1}` ＋ `reapply: {existingRemaining: {op: EQ, value: 1}, count: 2}`へ変換して、raw原文「1行動の気絶を付与する。対象に1行動の気絶が付与されていた場合は、2行動の気絶に上書きする」を原文どおりにした。M7-011（Issue #265）のtrigger側payload分類と併せ、当該スキルは両方のギャップが解消した。

期間単位とownerは`timeLimit`から引き継ぎ、差し替えるのは回数だけとする — 別単位を宣言できてしまうと`existingRemaining`の比較対象が曖昧になるため。一致判定はその効果自身の再付与規則の同一性に合わせ、再付与が状態種別単位で1インスタンスへ集約される状態異常（`STUN`＝R-STS-02、`FREEZE`＝R-STS-03）だけを状態種別（raw原文も付与元スキルを限定していないため`ACT_SIENA_DIVA_EX_STUN`由来の気絶も契機になる）、それ以外はすべて`EffectActionDefinitionId`で行う。PR #277レビュー[P2]により、集約を持たない`APPLY_STATUS`（`BLIND`はR-STS-04で複数インスタンスを独立処理、`STEALTH`/`EVASION`/`DAMAGE_IMMUNITY`等も個別保持）まで状態種別で同一視していた実装を、集約を持つ2種別の列挙（`STATUS_KINDS_AGGREGATED_ON_REAPPLY`）へ絞り込んだ — 解除・免疫の分類（R-STS-01の`STATUS`カテゴリ）とは別の区別である。差し替え後の回数はR-STS-02「残り回数が長い方を一つだけ残す」の比較にも使うため、既存が残り1なら`StunDurationChanged`（`remainingBefore: 1`／`remainingAfter: 2`／`REGRANT_EXTENDED`）が発行され、既存が残り2なら`EQ 1`不成立で基本の1行動へ戻ってno-opになる（上書きが累積して延び続けない）。専用のイベント種別は追加していない — `EffectApplied`の初期残り回数と`StunDurationChanged`の差し替え後残り回数が、そのまま差し替え結果を表す。

再付与時に新しい付与内容を一切反映しない凍結（R-STS-03、`grantFreezeStatus`が既存インスタンスをそのまま返す）と、`stack.policy`で再付与を解決するMarker（R-EFF-10、`marker-apply-service.ts`）は`resolveDurationOnReapply`を通らないため、`reapply`の宣言自体を`catalog-integrity.ts`が`UNSUPPORTED_DYNAMIC_DURATION_REAPPLY`としてCatalogロード時点で拒否する（`UNSUPPORTED_MARKER_DURATION`と同じ「付与自体は成功するのに期間だけ差し替わらない」silent partial implementation対策）。実ライフサイクル（実カタログ→`resolveSkillUse`→APPLY_STATUS resolver→`grantStunStatus`）・Domain Event・StateDelta・独立Reducer復元の確認は`dynamic-duration-on-reapply-production-catalog.test.ts`（`IT-CAP-DYNAMIC-DURATION-PROD-001`〜`004`）。

M7-012（Issue #266）は`STACK_LIMIT_ON_STAT_MOD`の1行（`UNIT_TARISA_TROUBLEMAKER`／`SKL_TARISA_TROUBLEMAKER_PS1`）を近似なしへ解消して下表から除去し、併せて所有者不在だった`R-EFF-05`を完了させた。`APPLY_STAT_MOD.stacking`へ重複上限`max`（`APPLY_MARKER.stack.max`相当）と重複なし`mode: NON_STACKABLE`を追加し、`ACT_TARISA_TROUBLEMAKER_PS1_ATK_UP`を`stacking: {mode: STACKABLE, max: 14}`へ変換して、raw原文「攻撃力を2.5%上昇させる（重複可）……「負けん気」は最大14個まで所持できる」の上限をMarker側（`stack.max: 14`）と1対1で揃えた。

`AppliedEffect`は重複数を単一インスタンスの可変スタック数ではなくインスタンス数そのものとして保持する（R-EFF-05第1〜2項）ため、Markerの`ADD`のようにclampする先を持たない。よって上限到達時は付与自体を行わず、`EffectApplied`もCombatStat再計算も発行せずに`EffectActionCompleted.resultKind: SKIPPED`だけを記録する（`marker-apply-service.ts`の`KEEP_EXISTING`と同じ「変化が無ければイベントを発行しない」規約）。この判定は効果免疫（R-EFF-03）より前に行う — `rejectEffectApplication`は`EFFECT_IMMUNITY.maxBlocks`の残り回数を1消費するため、そもそも1件も追加できない付与でその有限な回数を使わせてはならない。

`mode: NON_STACKABLE`は`APPLY_STAT_MOD`だけが宣言できる。`APPLY_DAMAGE_MOD`／`APPLY_HEALING_MOD`／`APPLY_RESOURCE_GAIN_MOD`の合成経路（`composeHealingRate`等）は保持している全インスタンスを合算するだけで最強選択を持たないため、受理しても何も変わらないsilent partial implementationになる。これによりresolverが`stacking.mode`から`duplicate`を導けるようになり、R-EFF-05の重複なし経路・最強選択・次点繰上げ・`EffectiveEffectChanged`が実ライフサイクルから到達可能になった（それまでresolverは`duplicate: true`固定で付与しており、選択規則は単体テストからしか到達できなかった）。`NON_STACKABLE`を必要とするproduction定義は現時点で存在しない（raw原文が明記するのは「重複可」だけであり、重複なしへの再分類はどの台帳行も要求していない）ため、重複なし経路はR-EFF-11の`EFFECT_SEQUENCE`スコープと同じく明示的な実ライフサイクルテストで検証する（`UT-R-EFF-05-017`／`018`／`021`）。重複上限の実ライフサイクル・Domain Event・StateDelta・独立Reducer復元の確認は`stat-mod-stack-limit-production-catalog.test.ts`（`IT-CAP-STAT-MOD-STACK-LIMIT-PROD-001`〜`003`）。

なお`SKL_TARISA_TROUBLEMAKER_PS1`には「攻撃力バフは……「負けん気」が解除されると同時に解除される」というcross-typeのlinkedEffectGroupカスケード（`R-EFF-09`第1項）が別行として残っていたが（`LINKED_EFFECT_GROUP_CROSS_TYPE`）、`M7-013`（Issue #267）が解消して下表から除去した。

M7-013（Issue #267）は`LINKED_EFFECT_GROUP_CROSS_TYPE`の2行を対象に、`AppliedEffect`と`MarkerState`をまたぐ`linkedEffectGroup`カスケード（`R-EFF-09`第1項「同じ`linkedEffectGroupId`を持つ`AppliedEffect`**と**`MarkerState`は親子連動グループとして扱う」）を実装し、所有者不在のうえ規定の一部が未実装のまま完了計上されていた`R-EFF-09`を完了させた。種別ごとに分かれていた収集関数（`applied-effect-linked-group.ts`／`marker-linked-group.ts`）を`model/linked-effect-group.ts`の`collectLinkedGroupCascade`へ統合し、両種別を同じBFSへ載せて`linkedEffectGroupRole`の`CHILD`例外（「子効果だけが消費条件で失効した場合、親効果は維持する」）も種別を問わず適用する。カスケード分の除去とイベント発行（`AppliedEffect`は`EffectExpired`／`EffectRemoved`、`MarkerState`は`MarkerRemoved`、いずれも`reason: LINKED_GROUP_CASCADE`／`cascaded: true`）は`effects/linked-group-cascade.ts`へ共通化し、失効（`expireEffects`）・解除（`removeEffects`）・Marker除去（`removeMarkers`／`reduceMarkerStack`）・凍結解除（`removeFreezeEffectSteps`）の4経路すべてを同じ実装へ配線した。併せて`catalog-integrity.ts`の`validateMarkerLinkedGroupCascadeSupport`（`UNSUPPORTED_MARKER_LINKED_GROUP`）を撤去した。

Catalogは`ACT_TARISA_TROUBLEMAKER_PS1_MARKER`（「負けん気」）を`PARENT`・`ACT_TARISA_TROUBLEMAKER_PS1_ATK_UP`を`CHILD`とする`TARISA_TROUBLEMAKER_PS1_LINK`と、`ACT_AOI_ELEGANT_AS1_MARKER_KOUYOU`（「高揚」）を`PARENT`・`ACT_AOI_ELEGANT_AS1_KOUYOU_CRIT_DOWN`／`ACT_AOI_ELEGANT_AS1_KOUYOU_DOT`を`CHILD`とする`AOI_ELEGANT_AS1_KOUYOU_LINK`へ更新した。raw原文が併記する「解除不可」（`dispellable: false`）はそのまま維持する — `REMOVE_EFFECTS`では解除できないが親Markerの解除には連動する、という非対称はR-EFF-09が明示する規定であり近似ではない。実ライフサイクル（実カタログ→`resolveSkillUse`→`REMOVE_MARKER` resolver→カスケード）での確認は`IT-LINKED-GROUP-CROSS-TYPE-PROD-001`〜`004`（`004`は独立Reducer復元）。`UNIT_AOI_ELEGANT`の継続ダメージ本体は`CAP_CONTINUOUS_DAMAGE`に依存していたが、`DMG-008`（Issue #189）が`R-DOT-01`〜`R-DOT-04`を実ライフサイクルへ配線し`IMPLEMENTED`にしたため当該Unitは`selectable`になった（`ACT_AOI_ELEGANT_AS1_KOUYOU_DOT`は`continuousDamageKind: FIXED`）。

M7-015（Issue #269）は、同監査が所有者不在として再割当した`CAP_MARKER_STACK_FORMULA`（Marker所持数を参照するFormula `MARKER_COUNT_SCALE`、`R-NUM-04`）を完了させた。`EFF-004`（Issue #160）はMarker本体だけを実装してcloseし、このFormulaの実ライフサイクル検証を持たないまま`PLANNED`で残していた。production 2経路——`APPLY_STAT_MOD.payload.formula`（`target: SKILL_SOURCE`、`ACT_CHIYURU_NEWYEAR_AS1_ATK_UP`／`ACT_CHIYURU_NEWYEAR_AS1_DEF_UP`）と`DAMAGE.payload.damageModifiers`（`target: TARGET`、`R-DMG-01`のAction内追加ダメージ倍率、`ACT_KARINA_DOWNER_AS1_DAMAGE`／`ACT_FEE_BATH_AS2_DAMAGE`）——を実`catalog/`無改変のまま実`resolveSkillUse`へ通し、`IT-CAP-MARKER-STACK-PROD-001`〜`006`で所持数0（不成立）・`perStack`比例・Formulaの`max`での頭打ち・別`markerId`の非寄与・Domain Event・StateDelta・独立Reducer復元を固定した。

これらの行はCatalog変換の時点で既に近似なしであり（raw原文の「「お餅」1つにつき攻撃力3%・防御力6%上昇（最大6つまで）」「「警棒」1つにつき15%増加(最大3つまで)」「「ほてり」1つにつき20%増加（最大）5つまで」がそのまま`perStack`/`max`になっている）、下表に該当行も存在しないため、Catalog定義とこの表はいずれも変更していない。`UNIT_FEE_BATH`（`SKL_FEE_BATH_AS2`）に残る1行は「この攻撃で会心攻撃が発生した場合、『ほてり』を1つ追加で付与する」だけで、テーマは`POST_DAMAGE_CRITICAL_BRANCH`（`M8`／`DMG-003`）であり本Capabilityとは無関係である。あわせて`catalog-integrity.ts`へ、`MARKER_COUNT_SCALE`を含むFormulaを持つ`EffectActionDefinition`に`CAP_MARKER_STACK_FORMULA`宣言を必須とする検証（`CAP_SUM_DAMAGE_RESULT`と同じ「宣言漏れ自体を拒否する」パターン）を追加した。この完了により`UNIT_CHIYURU_NEWYEAR`・`UNIT_FEE_BATH`が`selectable`になった（`UNIT_KARINA_DOWNER`は`CAP_COVER_DAMAGE`・`CAP_TARGET_REDIRECT`で引き続き非selectable）。

M7-011（Issue #265）は台帳外の2スキルの同じ形の不整合も修正した。`SKL_KATE_PALADIN_PS1`（敵に凍結が付与された際）は`field: "status"`、`SKL_MEIYA_FATED_PS1`（自身にデバフが付与された際）は`field: "category"`という、いずれも`EffectApplied`のpayloadに存在しないフィールドを参照しており、変換は「済み」と記録されていたにもかかわらず一度も発動できない状態だった。それぞれ`statusKind`・`categories`へ訂正した。Catalogの`EVENT_PAYLOAD.field`がそのeventTypeのpayloadに実在するかはCatalogロード時点で検証していないため、同じ形の不整合は今後も持ち込めてしまう — 検証の追加は本Issueのスコープ外とし、`IT-CAT-PROD-013`で当該8スキルの条件を固定するに留めた。

同監査は`LINKED_EFFECT_GROUP`（3行、担当はclose済みのEFF-003／Issue #159）も再検証した。`AppliedEffect`同士のカスケードと`MarkerState`同士のカスケードはそれぞれEFF-003・EFF-004で実装済みである（`M7-013`が両者を`model/linked-effect-group.ts`へ統合した）。`UNIT_AOI_ELEGANT`（Marker「高揚」→`APPLY_STAT_MOD`／`APPLY_CONTINUOUS_DAMAGE`）と`UNIT_TARISA_TROUBLEMAKER`（Marker「負けん気」→`APPLY_STAT_MOD`）の2行が必要とするのは両者をまたぐcross-typeカスケード（R-EFF-09第1項）で、`catalog-integrity.ts`の`validateMarkerLinkedGroupCascadeSupport`がこれを`UNSUPPORTED_MARKER_LINKED_GROUP`としてCatalogロード時点で拒否している。この2行を`LINKED_EFFECT_GROUP_CROSS_TYPE`（`M7`）へ再分類し、R-EFF-09のCoverage計上を未完了へ戻したうえで`M7-013`（Issue #267）へ引き継いだ。`M7-013`はcross-typeカスケードを実装して両行の失効連動を近似なしへ解消し、`UNIT_TARISA_TROUBLEMAKER`の行を下表から除去した（`UNIT_AOI_ELEGANT`の行は「「高揚」は付与者が倒れると同時に解除される」という別機構の残余だけが残り、新テーマ`MARKER_REMOVAL_ON_SOURCE_DEATH`／`M7-020`（Issue #279）へ再分類した。その`M7-020`は`DurationDefinition.removeOnSourceDefeated`（`APPLY_MARKER`専用）を追加してこの残余も解消し、同行を下表から除去した）。残る1行（`UNIT_LILY_SINGER`の`SKL_LILY_SINGER_PS2`）は`ACT_LILY_SINGER_PS2_SHIELD`と`ACT_LILY_SINGER_PS2_ATK_UP`がどちらも`AppliedEffect`であり、カスケード機構自体は既に使える — 真のブロッカーは`APPLY_SHIELD`（`CAP_SHIELD`）本体の未実装であるため、「Catalog近似を解除できる最終側のマイルストーンを優先する」規則に従い`M8`の`LINKED_EFFECT_GROUP_SHIELD`（`DMG-004`／Issue #194）へ再分類した。

同監査のレビュー指摘[P1]により、`M9`の`UNREACHABLE_BRANCH_BY_RAW_DATA` 1行（`UNIT_SUIRAN_CASINO`／`SKL_SUIRAN_CASINO_AS1`）も`17_残作業対応表.json`の`conversionThemeAssignments`へ登録し、`REL-003`（Issue #200）へ割り当てた。`remaining-work.test.ts`のパーサーが`対応予定`列の`M7`/`M8`だけを読んでいたため、この行は件数からも割当検証からも黙って外れていた。パーサーと型を`M9`まで広げ、`current.unitCatalog.incompleteConversionRows`を下表の実行数55と一致させた。

そのほかM7-010が所有者不在を検出して再割当した残作業（`DYNAMIC_DURATION_ON_REAPPLY`／`STACK_LIMIT_ON_STAT_MOD`と、Capability 8件・Rule 3件）の全体像は[`17_残作業対応表.md`](./17_残作業対応表.md)「M7完了監査」を正本とする。

M7-005（Issue #184）は`HEAL`（R-HEAL-01）・`APPLY_HEALING_MOD`（R-HEAL-02）・`APPLY_CONTINUOUS_HEAL`（R-HEAL-03）の実効resolverを`effect-action-group-resolver.ts`／`heal-application-service.ts`／`continuous-heal-service.ts`へ実装し、対応テーマ2件を解消した。`HEAL_KIND_UNIMPLEMENTED`（1行、`UNIT_SUIRAN_CHAOS`の`SKL_SUIRAN_CHAOS_PS2`）は、`ACT_SUIRAN_CHAOS_PS2_HEAL`が実ライフサイクル（実カタログ→`PassiveActivationRuntime`→HEAL resolver）で最後まで解決し実際に対象を回復することを`IT-CAP-TRIGGER-CONTEXT-PROD-003`／`IT-CAP-TRIGGER-CONTEXT-PROD-004`で確認した。`HEAL_DISTRIBUTE`（1行、`UNIT_LUCIE_COMPANION`の`SKL_LUCIE_COMPANION_AS3`）は、`HEAL.payload`へ`distribution`（`NONE`/`EVEN`、`MODIFY_RESOURCE.operation: DISTRIBUTE`のHEAL版）を新設して「威力65分のHP回復量を均等に配分して回復する」を近似なしで表現し、`ACT_LUCIE_COMPANION_AS3_HEAL`を`distribution: "EVEN"`へ変換した（`IT-CAP-HEAL-PROD-001`）。なお`SKILL_POWER`の回復量は`14_Catalog定義スキーマ.md`の定義（「攻撃力を基礎にしたスキル威力倍率」）どおり回復者の攻撃力×威力として解決する（R-HEAL-01 #1、防御力は差し引かない）。継続回復は`timing: {eventType: "ActionStarted", targetSelector: "EFFECT_OWNER"}`（production定義13件がすべて使う唯一の組み合わせ）だけを実装し、それ以外の`timing`は`catalog-integrity.ts`が`UNSUPPORTED_CONTINUOUS_HEAL_TIMING`としてCatalogロード時点で拒否する。

M7-004（Issue #183）は`APPLY_STATUS`のstatus: "EVASION"（R-HIT-02）/"BLIND"（R-HIT-03/R-STS-04）/"FREEZE"（R-STS-03）/"DAMAGE_IMMUNITY"（R-DMG-02）の実効resolverを実装し、`SKL_SUIRAN_CHAOS_PS1`（`ACT_SUIRAN_CHAOS_PS1_EVASION`）が実ライフサイクル・Domain Event・StateDelta・独立Reducer復元まで完全に発動することを`IT-CAP-TRIGGER-CONTEXT-PROD-002`で確認した。`APPLY_STATUS_KIND_UNIMPLEMENTED`（1行）を解消したため下表から除去する。同Issueは新設の`APPLY_ATTACK_DAMAGE_BONUS`（ON_ATTACK_BONUS_DAMAGE_BUFF）と`TargetOrderKey: LOWEST_ATTACK`も実装し、`UNIT_ELENA_MOODMAKER`（`SKL_ELENA_MOODMAKER_EX`）の対象選定とボーナスダメージバフを近似なしへ変換した。ただし同じACTION stepが付与する「与ダメージ+10%」（`ACT_ELENA_MOODMAKER_EX_DMGUP_HIGH`/`_LOW`、`APPLY_DAMAGE_MOD`）は別スコープ（`DMG-002`、Issue #192）のため未実装のまま残り、`ON_ATTACK_BONUS_DAMAGE_BUFF`の行は`DAMAGE_MOD_KIND_UNIMPLEMENTED`テーマへ再分類する。

M7-018（Issue #272）は、`07_戦闘ルール詳細.md`にRule定義自体が存在しなかった命中判定系の`APPLY_STATUS`種別 — `HIT_EVASION`（Nヒット回避）と`GUARANTEED_HIT`（必中） — について、まず`R-HIT-04`「Nヒット回避」・`R-HIT-05`「必中付与」を新設し、そのうえで実装した。`UNIT_FLUTE_VAMPIRE`（`ACT_FLUTE_VAMPIRE_PS2_EVASION`）・`UNIT_LAYLA_ENTREPRENEUR`（`ACT_LAYLA_ENTREPRENEUR_PS1_GUARANTEED_HIT`）はどちらも下表の不完全変換行を持たない（Catalog表現自体は近似なしで、実行時のresolverだけが欠けていた）ため、下表の行数は変わらない。近似なしの実行結果は`hit-evasion-guaranteed-hit-production-catalog.test.ts`（`IT-CAP-HIT-EVASION-PROD-001`/`002`、`IT-CAP-GUARANTEED-HIT-PROD-001`/`002`）が、実カタログ定義→`resolveSkillUse`→`grantEffect`→命中判定の実ライフサイクル・Domain Event・StateDelta・独立Reducer復元まで確認した。

同Issueは、M7-010（Issue #177）の監査が「`CAP_HIT_COUNT_EVASION`を宣言する`EVASION`/`DAMAGE_IMMUNITY`の6件は既に解決可能」と記録したのが判定だけを見た過大評価であったことを検出した。`ACT_ANIS_TROUBLEMAKER_PS1_EVASION`等の`EVASION`行はraw原文が`HIT_EVASION`行とまったく同じ「1ヒットだけ攻撃を回避するバフ」であるにもかかわらず、回避で`consumption: INCOMING_HIT`が消費されず（R-EFF-07は命中確定でしか消費しない）、実際には1行動の間すべての被ヒットを回避していた。`R-HIT-04`はこの消費をルールとして規定し、`EVASION`/`HIT_EVASION`の両方へ適用する。`UNIT_JUNKA_CHILDHOOD`（`ACT_JUNKA_CHILDHOOD_EX_SHIELD`／`SKL_JUNKA_CHILDHOOD_EX`）はIssueの指示に従って宣言を見直し、`CAP_HIT_COUNT_EVASION`の宣言を除去した — Nヒットで消費するシールドは回避ではなく`APPLY_SHIELD`本体の期間機構であり、`CAP_SHIELD`（`DMG-004`／Issue #194）が担当する。同Unitは引き続き`CAP_DAMAGE_MOD`・`CAP_SHIELD`で非selectableのままである。

Issue #143では、`RUNTIME_COUNTER`へのmodulo追加と、counterの更新契機を宣言する`SkillDefinition.counterUpdates`（`INCREMENT`/`CUMULATIVE_DAMAGE_THRESHOLD`）を実装し、対応テーマ`RUNTIME_COUNTER_MODULO`の9行（`UNIT_LAYLA_ENTREPRENEUR`/`UNIT_JUNKA_CHILDHOOD`/`UNIT_SHIRANA_SORA`/`UNIT_CLARA_SANTA`/`UNIT_OLGA_VETERAN`/`UNIT_MAO_COMMITTEE`/`UNIT_MIRIAM_MAGE`/`UNIT_ELENA_MOODMAKER`/`UNIT_NADYA_SUCCESSOR`の各PS）と`CUMULATIVE_DAMAGE_THRESHOLD_TRIGGER`の3行（`UNIT_CHIYURU_NEWYEAR`/`UNIT_CHIZURU_DOMESTIC`/`UNIT_TATIANA_SAGE`の各PS）の計12行を解消した。解消済みの行は下表から除去する。

Issue #144では、PS所有者から見た対象のFormation位置関係を判定する`POSITION_RELATION`（`relation: IN_FRONT_OF`）と、現在の解決スコープのroot事象が属するBattle/Turn phaseを判定する`RESOLUTION_PHASE`（`negate: true`）を`ConditionDefinition`へ追加し、`TriggerDefinition`/`ConditionEvaluator`（`PassiveTriggerMatcher`）から評価できるようにした。Issue #144 follow-upでは、`BattleStarted`/`TurnCompleting`から`PassiveActivationRuntime`を呼び出し`resolutionPhase: "BATTLE_START"`/`"TURN_END"`を配線した（`TurnStarted`の`"TURN_START"`と同じ契約）ほか、`SkillUseStarting`のpayloadに`skillType`が欠落していたため`EVENT_PAYLOAD field: "skillType"`を条件に持つtrigger（`SKL_SUIRAN_CHAOS_PS3`等）が実際には一度も候補化されない不具合を修正した。これにより`SKL_SUIRAN_CHAOS_PS3`は実ライフサイクル（`advanceBattle`）経由で候補検出（trigger一致）することを統合テストで確認できた。同follow-upのレビュー[P1]では、`TurnStarted`/`TurnCompleting`が`sourceUnitId`/`targetUnitIds`のどちらも持たないグローバルイベントであるにもかかわらず`sourceSelector`/`targetSelector: SELF`（`08_ドメインイベント.md`「自身がASを使う前」と同じ著者慣習、`TurnStarted`27行・`TurnCompleting`12行）を評価器が常にfalseと判定していた不具合も発見・修正した（`trigger-selector-evaluator.ts`）。この39行はいずれも`15_Unit_Memory変換台帳.md`の不完全変換一覧に含まれない（表現自体は正確で、実行時の評価器バグだったため）が、本PRで初めて実行時に発動するようになった。

RES-005（Issue #172）は`TRIGGER_TARGET`/`TRIGGER_SOURCE`の効果適用先解決（`skill-resolution-service.ts`の`resolveReference`、`target-selection-policy.ts`の`resolveTargets`/`resolveBase`、いずれも`PassiveActivationRuntime`が発動契機イベントの発生源・対象を解決して渡す）と、`HitPointReduced`（新規、`UnitBeingAttacked`と`DamageApplied`の間で発行しStateDeltaを持つ）の基本pipeline発行を実装した。`UnitBeingAttacked`はPR #209（EFF-003/R-EFF-07）時点で既に実行時発行されており、この記載は誤りだったと判明した。`SKL_SUIRAN_CHAOS_PS3`はこれにより実ライフサイクル経由で候補検出・PS発動・EffectSequence解決（`DAMAGE`＋`APPLY_STAT_MOD`の適用が`TRIGGER_TARGET`/`TRIGGER_SOURCE`それぞれ正しい実ユニットへ届くこと）まで production統合テストで確認できたため、下表から除去する。

`SKL_SUIRAN_CHAOS_PS1`（`ACT_SUIRAN_CHAOS_PS1_EVASION`、kind `APPLY_STATUS`）・`SKL_SUIRAN_CHAOS_PS2`（`ACT_SUIRAN_CHAOS_PS2_HEAL`、kind `HEAL`）は、TRIGGER_TARGET解決とは独立した別のEffectActionDefinitionKind（`effect-action-group-resolver.ts`は`DAMAGE`/`COOLDOWN_MANIPULATION`/`APPLY_STAT_MOD`/`APPLY_MARKER`/`REMOVE_MARKER`のみ対応）が未実装のため、候補検出・trigger解決・PS発動（PP消費等）までは実ライフサイクルで確認できるものの、実効果解決の最後で例外になり引き続き完全な発動はできない（production統合テストで、TRIGGER_TARGET起因の例外ではなくこのEffectActionKind起因の例外であることを確認済み）。それぞれ`APPLY_STATUS_KIND_UNIMPLEMENTED`（PS1、担当`M7-004`/Issue #183）・`HEAL_KIND_UNIMPLEMENTED`（PS2、担当`M7-005`/Issue #184）として下表の別行に分離・再分類する。

また調査の過程で、`UNIT_KEI_JACKKNIFE`/`UNIT_LILY_SINGER`/`UNIT_SIENA_DIVA`の各PSが参照する`EffectApplied`は、実際にはEFF-001（Issue #164）時点で既に実行時発行されており、この記載も誤りだったと判明した。これら3スキルの真のブロッカーは、trigger conditionが参照する`EVENT_PAYLOAD field: "category"`（値`"DEBUFF"`）/`"effectKind"`（値`"APPLY_STATUS"`）に相当する分類情報が`EffectApplied`のpayload（`effect-grant-service.ts`）に存在しないこと（`kindKey`は`EffectActionDefinitionId`そのものであり、BUFF/DEBUFF等の分類ではない）で、`TRIGGER_PAYLOAD_FILTER`（`UNIT_URUU_TIMID`/`UNIT_NADYA_SUCCESSOR`）と同種の、RES-005のスコープ外のギャップである。該当3行の「原因」列をこの内容へ修正し、担当を`RES-005`から`M7-010`（Issue #177、監査）へ引き継ぐ。

M7-002（Issue #185）は`MODIFY_RESOURCE`（HP_DIRECT_COST、`resource: HP`の新設）と新規`APPLY_RESOURCE_GAIN_MOD`（RESOURCE_GAIN_MOD）を`effect-action-group-resolver.ts`へ実配線した。`HP_DIRECT_COST`4行（`UNIT_SUIRAN_CASINO`のAS2レイズ、`UNIT_LILY_HERO`のAS1アイム・ア・ヒーロー、`UNIT_OLGA_VETERAN`のPS2ヴォルコワの血脈A、`UNIT_MAO_COMMITTEE`のAS1風紀を守る一撃）と`RESOURCE_GAIN_MOD`2行（`UNIT_MAIA_SALON`のAS2優雅なるロンド、`UNIT_KARINA_DOWNER`のPS2包囲かんりょ～）を近似なしへ変換し、下表から除去した。`UNIT_ELENA_MOODMAKER`（`SKL_ELENA_MOODMAKER_AS1`）はコスト側（自身HPの10%消費）が解消されたため`HP_DIRECT_COST`テーマから除去したが、残る回復リンク（Issue #229）の分は新設の`HEALING_LINK`テーマへ再分類して残す。`MODIFY_RESOURCE_CAPACITY`（`CAP_RESOURCE_CAPACITY_MOD`）は本Issueでは実装を見送りM7-002A（Issue #255）へ切り出した。M7-002Aが実装した — 見送りの根拠だった「これを参照するCatalog変換行が現行に存在しない」という前提はM7-010（Issue #177）の監査で事実誤りと判明しており（`UNIT_FLUTE_VAMPIRE`の`ACT_FLUTE_VAMPIRE_PS1_MAX_AP_UP`が参照している）、当該定義は近似なしのまま実ライフサイクルで解決する。同Unitは`CAP_DEATH_SURVIVAL`（DMG-006／Issue #188）で引き続き非selectableであり、不完全変換表に変更はない。

M7-017（Issue #271）は、M7-002が見送っていた`MODIFY_RESOURCE`の`operation: DISTRIBUTE`（`CAP_RESOURCE_DISTRIBUTE`）を`resource-modification-service.ts`へ実装した。Formula評価結果を「対象間で分け合う総量」とみなし、同一EffectStep内でこのEffectActionが実際に適用される対象数（`effect-action-group-resolver.ts`の`resolveActionApplications`が算出し、`HEAL`の`distribution: "EVEN"`と共有する分母）で等分した取り分を、`ADD`と同じ規約（`bounds`∩`[0, currentMax]`、R-NUM-02の1回切り捨て）で現在値へ加算する。分母の定義だけがHEAL版と異なり、`MODIFY_RESOURCE`は`includeDefeated`が明示された戦闘不能の対象へも実際に適用されるためそれを分母に残す（`HEAL`はR-HEAL-01に蘇生規則が無いため常に除外する）。対象は`UNIT_SUIRAN_CHAOS`（`SKL_SUIRAN_CHAOS_EX`「敵単体に威力318でEN攻撃し、このスキルの発動のために自身が消費したEXゲージを、自身を除く味方全体に分配して加算する」）の1件で、Catalog表現（`ACT_SUIRAN_CHAOS_EX_EX_DISTRIBUTE`、`formula: CONSTANT 8`＝EXコストと同値）自体は近似なしのまま実行時のresolverだけが欠けていたため、下表の行数は変わらない。近似なしの実行結果は`suiran-chaos-ex-distribute-production-catalog.test.ts`（`IT-CAP-RESOURCE-DISTRIBUTE-PROD-001`／`002`）が、実カタログ定義→`resolveSkillUse`→`resource-modification-service.ts`の実ライフサイクル・Domain Event・StateDelta・独立Reducer復元まで確認した。同Unitは引き続き`CAP_DAMAGE_MOD`（`DMG-002`／Issue #192）で非selectableのままである。

`M7-005-HEAL-LINK`（Issue #229）は新規`EffectActionDefinition.kind: APPLY_HEALING_LINK`とR-HEAL-04（回復リンク）を実装し、`heal-application-service.ts`の`applyOneHeal`（即時回復`HEAL`と継続回復`APPLY_CONTINUOUS_HEAL`が共有するR-HEAL-01の手順そのもの）へ転送を実配線した。`UNIT_ELENA_MOODMAKER`（`SKL_ELENA_MOODMAKER_AS1`）へ`TGT_LOWEST_HP_RATIO_ENEMY` bindingと`ACT_ELENA_MOODMAKER_AS1_HEALING_LINK`（最もHP割合の低い敵単体と自身への100%転送リンク、`duration: {unit: ACTION, count: 1, owner: EFFECT_SOURCE}`＝「自身が1回行動を終えるまでの間」）を追加し、`HEALING_LINK`テーマ1行を近似なしへ変換して下表から除去した（`IT-CAP-HEALING-LINK-PROD-001`〜`003`が実ライフサイクル・Domain Event因果・StateDelta・独立Reducer復元まで検証する）。

ただし同じAS1の「自身の現在HPの10%を消費し、…消費分×235%分回復する」というコストと回復量の連動は、依然として`ACT_ELENA_MOODMAKER_AS1_HEAL`の`CURRENT_HP_RATIO`（`0.1 × 2.35 = 0.235`）へ畳み込んだ近似のままである。M7-002（Issue #185）のノートは`MODIFY_RESOURCE`（`resource: HP`）の追加によってコスト側が解消されたと記録したが、実際にはコストstepをCatalogへ追加していない。コストstepを先に置くと回復量Formulaが消費後のHPを参照してしまい（`消費分×235%`＝消費前HP×23.5%と一致しない）、回復stepを先に置くとElena自身が最低HP割合の味方だった場合に回復後のHPからコストを引いてしまうため、**実際に消費した量を参照する`FormulaDefinition`が存在しない限り厳密には表現できない**。本Issueのスコープ（回復リンク）ではないため、この事実をここに記録して残す。

DMG-002（Issue #192）は`R-DMG-03`（防御貫通）・`R-DMG-04`（DamageModifier）を実装し、3テーマ計14行を近似なしへ解消して下表から除去した。

- `DAMAGE_MOD_KIND_UNIMPLEMENTED`（2行、`UNIT_ELENA_MOODMAKER`／`UNIT_TATIANA_SAGE`）: `effect-action-group-resolver.ts`が`APPLY_DAMAGE_MOD`を`AppliedEffect`として付与するようになり（向き・対象ダメージタイプ・動的条件を`AppliedEffect.damageModifier`へ焼き込み、補正値は`APPLY_STAT_MOD`と同じ付与時snapshot規約で`magnitude`へ）、`combat/damage-modifier-policy.ts`がヒットごとに与/被ダメージ倍率へ集計する。Catalog定義自体は変更していない — 実行時解決の欠落だけがブロッカーだった。
- `HP_RATIO_SCALE_FORMULA`（7行）: `FormulaDefinition.kind: HP_RATIO_SCALE`の`direction`を`LOWER_HP_IS_MAX`／`HIGHER_HP_IS_MAX`の2値として[`14_Catalog定義スキーマ.md`](./14_Catalog定義スキーマ.md)へ定義し、Mapperと`FormulaEvaluator`へ実装した（同書「Issue #6実装で判明した制約」#2の解消）。`SKL_MERU_SIRIUS_AS2`／`SKL_RAMI_UNYIELDING_AS1`は`damageModifiers`へ、`SKL_RAMI_UNYIELDING_PS2`／`SKL_MEIYA_FATED_PS2`／`SKL_MAO_COMMITTEE_PS2`は各効果の`formula`へ逓減をそのまま表現する。`SKL_MEIYA_FATED_PS2`は「攻撃対象のHPが多いほど」を`TRIGGER_TARGET`参照（`SkillUseStarting.targetUnitIds`）で解決する。`SKL_YUI_HEIR_EX`は固定`piercing.defenseIgnoreRate: 0.5`という近似をやめ、`SKILL_USE`期間の防御力デバフ（`ACT_YUI_HEIR_EX_DEF_DOWN`）としてダメージより前のACTIONで適用する。`SKL_SENKA_CHRISTMAS_AS2`は「威力 × (1 + HP割合スケール)」を丸めなしの1 Formulaで表すため`FormulaDefinition.kind: PRODUCT`（`SUM`の乗算版）を新設した。
- `DYNAMIC_DAMAGE_MOD_CONDITION`（5行）: `APPLY_DAMAGE_MOD.payload.condition`を新設し、補正の保持者（`EFFECT_OWNER`）とそのヒットの相手（`OPPONENT`）だけを参照する条件（`UNIT_STATE`／`UNIT_HAS_MARKER`／`HP_RATIO_COMPARISON`とその論理合成）をヒットごとに評価する。`SKL_KEI_JACKKNIFE_PS1`（自身のHPが65%以上のときだけ）と`SKL_KOTOHA_REBEL_PS2`（対象のHP割合が自身より低い敵にだけ）は、これまで補正自体を省略していたため`ACT_KEI_JACKKNIFE_PS1_DMG_DOWN`／`ACT_KOTOHA_REBEL_PS2_DMG_UP`を新規に追加した。`SKL_AOI_ELEGANT_PS2`／`SKL_OLGA_VETERAN_AS2`はMarker所持による対象限定を、`SKL_JULIE_SNOW_PS1`はHP割合比較を既存定義へ追加した。

`CAP_DAMAGE_MOD`を`IMPLEMENTED`にした結果、実在Unitのselectableは6件から22件、Memoryは18件から31件へ増えた。この過程で、`CAP_DAMAGE_MOD`が偶然ブロックしていただけの宣言漏れが2つ見つかったため併せて修正した（golden battle層が実戦闘の失敗として検出した）: `APPLY_SUBUNIT`を持つ3ユニットの`CAP_SUBUNIT`宣言漏れと、条件evaluatorが解決できない`TARGET_STATE.field`（`UNIT_TYPE`のtrigger条件・`HAS_STATUS`）を表す新設`CAP_TARGET_STATE_EXTENDED_FIELD`（担当`M7-001E`／Issue #248）である。

M7-001A（Issue #242）では、`REMOVE_EFFECTS`の`SHIELD`／`SUBUNIT`カテゴリを実行時に有効化し、対応テーマ`REMOVE_EFFECTS_CATEGORY_GAP`の2行（`UNIT_YUI_HEIR`／`UNIT_OLGA_VETERAN`）を解消した。Catalogスキーマ側は M7-001（Issue #181）の時点で両カテゴリを受理していたが、シールド・サブユニットの実行時状態が無かったため`effect-action-group-resolver.ts`が明示的に拒否していた。DMG-004（`CAP_SHIELD`／Issue #194）とDMG-005（`CAP_SUBUNIT`／Issue #190）が両者を`AppliedEffect.shield`／`AppliedEffect.subUnit`として実行時状態にしたことで前提が揃ったため、拒否を外して他カテゴリと同じ`removeEffects`経路へ通した — シールドプール（`shield-policy.ts`）もサブユニット耐久力（`sub-unit-policy.ts`）もインスタンス集合からの導出値であり、インスタンス除去がそのまま解除になるため、解除側に固有の実装は不要だった。分類は`effect-category-classifier.ts`（`APPLY_SHIELD`→`SHIELD`、`APPLY_SUBUNIT`→`SUBUNIT`を定義kindから固定）ただ1つを正本のまま使う — どちらも`magnitude`（付与時の最大値・最大耐久力）が非負のため、符号から導く既定の分岐に落ちると`BUFF`扱いになり、「バフを解除する」`REMOVE_EFFECTS`がシールドまで巻き込んでしまう。`SKL_YUI_HEIR_EX`は「敵単体のシールドを全て解除し、さらに……防御力を最高50%低下させて、威力243.8で攻撃する」の順序どおり`ACT_YUI_HEIR_EX_REMOVE_SHIELD`をダメージより前のACTIONへ置き、`SKL_OLGA_VETERAN_PS1`は「自身に付与されているシールドとサブユニットをすべて解除し、……サブユニット『カムラッドⅡ』を3つ付与する」の順序どおり`ACT_OLGA_VETERAN_PS1_REMOVE_SHIELD_SUBUNIT`を同じSELF stepの先頭へ置く（順序が逆だと新しく付与した3体を自分で解除してしまうため、`IT-REMOVE-EFFECTS-PROD-008`／`009`が実resolver経路で順序ごと検証する）。解消済みの行は下表から除去する。

DMG-003（Issue #196）は`POST_DAMAGE_CRITICAL_BRANCH`（6行）・`POST_DAMAGE_SURVIVAL_BRANCH`（3行）・`TEMP_PIERCING_GRANT`（1行）の計10行を近似なしへ解消して下表から除去した。

- `POST_DAMAGE_CRITICAL_BRANCH`（6行）: `LAST_RESULT`の`field`へ`criticalHitCount`（直前ACTION step**全体**で実際に適用された会心ヒット数）を追加し、「この攻撃で会心攻撃が発生した場合」を`BRANCH`条件でそのまま表せるようにした。step全体スコープにしたのは、raw原文の同じ文言がAOE（`SKL_ROSIE_ARTIST_AS1`の敵横一列、`SKL_URUU_TIMID_EX`の敵全体）でも「その攻撃のどこかで会心が出たか」を意味しており、最後に処理した対象1体の会心では表せないためである。`SKL_FEE_BATH_AS2`（「ほてり」1つ追加付与）・`SKL_RAVEL_MODEL_AS2`（「ターゲット」1つ付与、既存の「3つ以上で気絶」分岐より前へ挿入）・`SKL_ROSIE_ARTIST_AS1`（敵横一列への威力39の追撃＋自身への与ダメージ+25%）・`SKL_URUU_TIMID_EX`（被ダメージ+100%＋威力28.44の追加1ヒット）・`SKL_URUU_TIMID_AS1`（戦闘終了までの被ダメージ+5%と会心率-2%）がそれぞれの原文どおりの分岐を持つ。
- `SKL_YUI_HEIR_AS2`だけは構造ごと組み直した。「剣気」の付与手段が生まれたことで、これまで到達不能として実装対象外にしていた「攻撃時に『剣気』を所持している場合、この攻撃の威力は249.6になり、攻撃後に所持している『剣気』を全て解除する」分岐も同時に実装した。`LAST_RESULT`が指すのは**直前のACTION step**だけであり、DAMAGE stepと会心分岐の間に「剣気」解除stepを挟むと会心が観測できなくなるため、解除は会心分岐の then／else 両枝の先頭へ複製して置いている（raw原文の「解除 → 会心時の再付与」という順序はどちらの経路でも保たれる）。
- `POST_DAMAGE_SURVIVAL_BRANCH`（3行）: `TARGET_SET_COUNT`へ`countOf`（`ALIVE`／`DEFEATED`、省略時`ALIVE`）を追加した。対象集合の大きさは実行時にしか分からないため、「この攻撃で敵を倒した場合」は`ALIVE`側のしきい値（`生存数 < 集合の大きさ`）では表せない。判定対象はスキル自身の対象binding（`SKL_RAMI_UNYIELDING_EX`は`TGT_ROW`、`SKL_HIIRO_LONEWOLF_AS2`は`TGT_COLUMN`、`SKL_JULIE_SNOW_EX`は`TGT_3ENEMIES`）とする — bindingはR-SKL-01により再評価されず同じunit集合を指し続け、状態だけが最新化されるため、DAMAGE後に撃破された構成員も数え漏らさない。これで基準対象(TGT_BASE)1体の生存確認だけという近似が外れた。
- `TEMP_PIERCING_GRANT`（1行）: `EffectActionDefinition.kind`へ`APPLY_PIERCING_MOD`を追加し、`SKL_RAMI_NEWYEAR_PS1`（おみくじ運試し）の大吉・中吉枝（`defenseIgnoreRate` 0.5／0.25）を実装した。消費契機は同PSが既に持つ`ACT_RAMI_NEWYEAR_PS1_DMG_UP`（どの結果でも付く与ダメージ+20%）と同じ`NEXT_OUTGOING_ATTACK`1回で、raw原文の「このスキルに続く自身の攻撃で」に対応する。合成は「無視されずに残る割合の積」（`1 - Π(1 - 各無視率)`）で、`combat/piercing-policy.ts`がヒットごとに静的率と保持中の一時付与をまとめる。

この過程で`ACT_FLUTE_VAMPIRE_AS1_HP_COST`（3率とも1の自傷コスト）の`CAP_PARTIAL_PIERCING`宣言漏れが見つかったため併せて埋めた — 貫通を宣言する定義に本Capabilityの自己申告を必須化した（`catalog-integrity.ts`）ことで表面化した、`CAP_DAMAGE_MOD`新設時（Issue #192）と同じ形の隠れた漏れである。会心保証・会心不可（`CAP_CRITICAL_CONTROL`、`APPLY_STATUS`の`CRITICAL_GUARANTEE`／`CRITICAL_PREVENTION`）は本Issueの3テーマとは独立した機能のため`DMG-003A`（Issue #295）へ分離した。

| unitDefinitionId        | 該当スキル                                       | 変換できなかった内容                                                                                                                                        | 原因                                                                                                                                                                                  | 対応予定 | 対応テーマ                       |
| ----------------------- | ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------- |
| `UNIT_SUIRAN_CASINO`    | `SKL_SUIRAN_CASINO_AS1`（ショーダウン）          | 「自身が『ワンペア』所持時は敵2体」への対象拡張を省略した                                                                                                   | 支給された5スキルの中に「ワンペア」を付与する手段が存在せず、到達不能な分岐のため実装対象外とした                                                                                     | `M9`     | `UNREACHABLE_BRANCH_BY_RAW_DATA` |
| `UNIT_SUIRAN_CASINO`    | `SKL_SUIRAN_CASINO_AS1`（ショーダウン）          | 2枚目のシールド発生時に付与する、自身と味方全体間のダメージリンク（被ダメージ50%を自身へ転送）を省略した                                                    | `EffectActionDefinition.kind`に`APPLY_DAMAGE_LINK`が存在しない（Cover/Reflect/DamageLinkの割り込み順が未確定のため未実装）                                                            | `M8`     | `DAMAGE_LINK`                    |
| `UNIT_DOROTHEA_PIONEER` | `SKL_DOROTHEA_PIONEER_PS1`（融和の象徴）         | 「最も近い敵と最も遠い敵にリンクを付与し、被ダメージの35%を共有しあう」を、両者へ被ダメージ+35%を個別付与する近似に置き換えた                               | `EffectActionDefinition.kind`に`APPLY_DAMAGE_LINK`が存在しない                                                                                                                        | `M8`     | `DAMAGE_LINK`                    |
| `UNIT_DOROTHEA_PIONEER` | `SKL_DOROTHEA_PIONEER_PS2`（新時代の先駆け）     | 発動条件「自身が付与したダメージリンクの対象が倒された際」を「敵が倒された際」全般に近似した                                                                | ダメージリンクを実装していないため、「どのリンクを自身が付与したか」を追跡する手段がない                                                                                              | `M8`     | `DAMAGE_LINK`                    |
| `UNIT_OLGA_VETERAN`     | `SKL_OLGA_VETERAN_EX（鉄の女）`                  | 「混乱」（本来の対象と逆陣営への攻撃振り替え、対象時ダメージ30%減少、被害者攻撃力が防御力以下の場合の特殊ダメージ計算式）を、識別用Marker付与のみに近似した | 「混乱」に相当する状態異常、攻撃対象を陣営ごと入れ替える効果、通常と異なるダメージ計算式を適用する手段のいずれもCatalog v2に存在しない                                                | `M8`     | `CONFUSION_OR_DAMAGE_TO_HEAL`    |
| `UNIT_CHIZURU_DOMESTIC` | `SKL_CHIZURU_DOMESTIC_PS1`（チェーンマニューバ） | 「対象と自身の間にリンクを付与し、自身が受けたダメージの35%を対象に送り込む」ダメージリンクを省略した                                                       | EffectActionDefinition.kindにAPPLY_DAMAGE_LINKが存在しない（UNIT_DOROTHEA_PIONEERと同一理由）                                                                                         | `M8`     | `DAMAGE_LINK`                    |
| `UNIT_LUNA_HUNGRY`      | `SKL_LUNA_HUNGRY_PS1`（ネガティブヘイト）        | 「受けたダメージの75%を反射する」効果を省略した                                                                                                             | `APPLY_REFLECT`の必須Capability `CAP_REFLECT_DAMAGE` が`catalog-src/capabilities.json`に未登録で、Mapperの検証（UNKNOWN_CAPABILITY）を通過できない（本Issueで新たに確認したギャップ） | `M8`     | `REFLECT_DAMAGE`                 |
| `UNIT_TATIANA_SAGE`     | `SKL_TATIANA_SAGE_AS1`（遅効の毒針）             | 「幻惑」（ダメージを回復効果へ変換し、回復値を本来のダメージの70%とする状態異常）を省略した                                                                 | `APPLY_STATUS`の`status`列挙にダメージ変換を表す値が存在しない                                                                                                                        | `M8`     | `CONFUSION_OR_DAMAGE_TO_HEAL`    |

## Memory 変換台帳

| raw/memories/ ファイル            | memoryDefinitionId               | ステータス / M2向け分類                                                                                                                                              |
| --------------------------------- | -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Chaos Maiden                      | `MEM_CHAOS_MAIDEN`               | 済み（本Issue #176 M7-008 所属・動的効果バッチ） 所属条件あり（`AFF_CHAOS_MAIDEN`、`18_Affiliation台帳.md`参照）                                                     |
| Colorful Bouquet                  | `MEM_COLORFUL_BOUQUET`           | 済み（本Issue #176 M7-008 所属・動的効果バッチ） 所属条件あり（`AFF_COLORFUL_BOUQUET`、`18_Affiliation台帳.md`参照）                                                 |
| Pyxis Ma Soeur                    | `MEM_PYXIS_MA_SOEUR`             | 済み（本Issue #176 M7-008 所属・動的効果バッチ） 所属条件あり（`AFF_PYXIS_MA_SOEUR`、`18_Affiliation台帳.md`参照）                                                   |
| STRANGERS                         | `MEM_STRANGERS`                  | 済み（本Issue #47 先行バッチ）                                                                                                                                       |
| Sirius Sugar                      | `MEM_SIRIUS_SUGAR`               | 済み（本Issue #176 M7-008 所属・動的効果バッチ） 所属条件あり（`AFF_SIRIUS_SUGAR`、`18_Affiliation台帳.md`参照）                                                     |
| Treble Quintet                    | `MEM_TREBLE_QUINTET`             | 済み（本Issue #176 M7-008 所属・動的効果バッチ） 所属条件あり（`AFF_TREBLE_QUINTET`、`18_Affiliation台帳.md`参照）                                                   |
| Trinity Jewel                     | `MEM_TRINITY_JEWEL`              | 済み（本Issue #176 M7-008 所属・動的効果バッチ） 所属条件あり（`AFF_TRINITY_JEWEL`、`18_Affiliation台帳.md`参照）                                                    |
| おパンツ咥えたドラネコ            | `MEM_PANTS_STRAY_CAT`            | 済み（本Issue #178 M7-007 静的補正バッチ）                                                                                                                           |
| お傍にいるのはいつでもピコですよ♪ | `MEM_ALWAYS_PICO_BESIDE_YOU`     | 済み（本Issue #176 M7-008 所属・動的効果バッチ）                                                                                                                     |
| お嬢様直伝マナー講座              | `MEM_OJOSAMA_MANNERS`            | 済み（本Issue #47 先行バッチ）                                                                                                                                       |
| お忍びシスターの冒険              | `MEM_INCOGNITO_SISTER_ADVENTURE` | 済み（本Issue #176 M7-008 所属・動的効果バッチ） 所属条件あり（`AFF_PYXIS_MA_SOEUR`、`18_Affiliation台帳.md`参照）                                                   |
| これからも共に歩むあなたへ        | `MEM_WALK_TOGETHER`              | 済み（本Issue #47 先行バッチ）                                                                                                                                       |
| エッ◯な罰ゲームやってみた         | `MEM_NAUGHTY_PENALTY_GAME`       | 済み（本Issue #176 M7-008 所属・動的効果バッチ） ※与ダメージ補正の実行時解決は`DMG-002`待ち→下記参照                                                                 |
| カオスメイデンツインテ祭り♡       | `MEM_CHAOS_MAIDEN_TWINTAIL_FEST` | 済み（本Issue #178 M7-007 静的補正バッチ）                                                                                                                           |
| ハードな準備運動                  | `MEM_HARD_WARMUP`                | 済み（本Issue #47 先行バッチ）                                                                                                                                       |
| メイド３人のおもてなし？          | `MEM_THREE_MAIDS_HOSPITALITY`    | 済み（本Issue #178 M7-007 静的補正バッチ） ※与ダメージ補正の実行時解決は`DMG-002`待ち→下記参照                                                                       |
| 不満と不安                        | `MEM_DISCONTENT_AND_ANXIETY`     | 済み（本Issue #176 M7-008 所属・動的効果バッチ）                                                                                                                     |
| 安心する香り                      | `MEM_SOOTHING_SCENT`             | 済み（本Issue #176 M7-008 所属・動的効果バッチ） ※与ダメージ補正の実行時解決は`DMG-002`待ち→下記参照                                                                 |
| 家族のかたちを象りながら          | `MEM_SHAPING_FAMILY`             | 済み（本Issue #176 M7-008 所属・動的効果バッチ） ※与ダメージ補正の実行時解決は`DMG-002`待ち→下記参照 所属条件あり（`AFF_KURASUNA`、`18_Affiliation台帳.md`参照）     |
| 密着！？テントの中の珍騒動        | `MEM_TENT_COMMOTION`             | 済み（本Issue #176 M7-008 所属・動的効果バッチ） ※与ダメージ補正の実行時解決は`DMG-002`待ち→下記参照 所属条件あり（`AFF_PRE_KURASU_A`、`18_Affiliation台帳.md`参照） |
| 少女たちとの邂逅                  | `MEM_ENCOUNTER_WITH_GIRLS`       | 済み（本Issue #176 M7-008 所属・動的効果バッチ） ※与ダメージ補正の実行時解決は`DMG-002`待ち→下記参照                                                                 |
| 心の色                            | `MEM_HEART_COLOR`                | 済み（本Issue #47 先行バッチ）                                                                                                                                       |
| 忙しい時のまどろみ                | `MEM_BUSY_DAY_SLUMBER`           | 済み（本Issue #176 M7-008 所属・動的効果バッチ） ※与ダメージ補正の実行時解決は`DMG-002`待ち→下記参照                                                                 |
| 新年のご挨拶                      | `MEM_NEW_YEAR_GREETING`          | 済み（本Issue #176 M7-008 所属・動的効果バッチ） ※与ダメージ補正の実行時解決は`DMG-002`待ち→下記参照                                                                 |
| 桃園家のお正月                    | `MEM_MOMOZONO_NEW_YEAR`          | 済み（本Issue #178 M7-007 静的補正バッチ）                                                                                                                           |
| 気になる装備                      | `MEM_CURIOUS_EQUIPMENT`          | 済み（本Issue #176 M7-008 所属・動的効果バッチ）                                                                                                                     |
| 絶対命令行使権！                  | `MEM_ABSOLUTE_ORDER`             | 済み（本Issue #178 M7-007 静的補正バッチ） ※与ダメージ補正の実行時解決は`DMG-002`待ち→下記参照                                                                       |
| 腐れ縁で犬猿の仲？                | `MEM_CATS_AND_DOGS_BOND`         | 済み（本Issue #176 M7-008 所属・動的効果バッチ） ※与ダメージ補正の実行時解決は`DMG-002`待ち→下記参照                                                                 |
| 臆病トナカイの聖夜                | `MEM_TIMID_REINDEER_EVE`         | 済み（本Issue #47 先行バッチ）                                                                                                                                       |
| 風紀委員会                        | `MEM_FUUKI_IINKAI`               | 済み（本Issue #176 M7-008 所属・動的効果バッチ） 所属条件あり（`AFF_FUUKI_IINKAI`、`18_Affiliation台帳.md`参照）                                                     |
| 駆け落ちフルスロットル！          | `MEM_ELOPEMENT_FULL_THROTTLE`    | 済み（本Issue #176 M7-008 所属・動的効果バッチ） ※与ダメージ補正の実行時解決は`DMG-002`待ち→下記参照 所属条件あり（`AFF_CHAOS_MAIDEN`、`18_Affiliation台帳.md`参照） |
| １日ユーロ・タワー体験            | `MEM_EURO_TOWER_DAY`             | 済み（本Issue #178 M7-007 静的補正バッチ）                                                                                                                           |

### 未変換 Memory の分類基準

Issue #176（M7-008）で `raw/memories/` 32件すべてが変換済みになったため、**該当件数は全行0件**である。バッチ分割の根拠として、どの観点でスコープを切ったかの記録を残す。単純なstat補正も `APPLY_STAT_MOD` を持つ `triggeredEffects` として扱い、M2開始状態生成ではなくM7のMemory発動エンジンで解決する。下表の「該当件数」はIssue #178完了時点（未変換20件）の値である。

| 分類                                                               | 該当件数 | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 所属条件あり（affiliationId確定済み。`18_Affiliation台帳.md`参照） | 11       | `所属フィルタ`（例:「カオスメイデンに所属するキャラクター」）を持つ。`affiliationId`（`AFF_*`）の採番方針・一覧・所属キャラクター40件はIssue #161で確定した（[`18_Affiliation台帳.md`](./18_Affiliation台帳.md)）。対応するUnit58件の `metadata.affiliations` もIssue #161で反映済み（[`14_Catalog定義スキーマ.md`](./14_Catalog定義スキーマ.md) の方針どおり、出典を伴う根拠が確認できたUnitのみ）。Memory自体の変換（`triggeredEffects`実装）はIssue #176（M7-008）が完了した |
| 与ダメージ・被ダメージ補正あり                                     | 5        | stat補正ではなく `APPLY_DAMAGE_MOD` が必要（対象がstatではなくdamage方向の補正のため）                                                                                                                                                                                                                                                                                                                                                                                          |
| TurnStarted発動を含む                                              | 2        | `BattleStarted` 以外の発動タイミング（ターン開始時）を持つ                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 動的効果（Marker付与等）あり                                       | 1        | Marker付与を伴い、`EffectAction` 変換が必要                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 敵側(ENEMY)対象の効果を含む                                        | 1        | 上記分類と重複するものを除き、Memoryが味方だけでなく敵側を対象とする効果を持つ                                                                                                                                                                                                                                                                                                                                                                                                  |

合計は重複分類を含むため20件と一致しないことがある（例: 所属条件 + 敵側対象の両方に該当するMemoryが1件ある）。実際に`APPLY_DAMAGE_MOD`を要したのは、所属条件・TurnStartedと重複する分を含めて9件だった（下記「Issue #176（M7-008）」参照）。

Issue #47先行バッチで変換した6 Memory（`MEM_HARD_WARMUP`、`MEM_STRANGERS`、`MEM_TIMID_REINDEER_EVE`、`MEM_WALK_TOGETHER`、`MEM_HEART_COLOR`、`MEM_OJOSAMA_MANNERS`）はいずれも raw 記載の効果を近似・省略なくフル変換できている。

### Issue #178（M7-007）: 静的補正中心の6 Memory

「静的補正のみ・変換容易」に分類していた6件を、`M7-006`（Issue #179）が実装したMemory発動エンジン上の`triggeredEffects`として近似・省略なく変換した。分類表の同行は0件になったため除去した。

| raw/memories/ ファイル      | memoryDefinitionId               | 変換した装備条件・効果                                                                                                         |
| --------------------------- | -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| おパンツ咥えたドラネコ      | `MEM_PANTS_STRAY_CAT`            | `UNIT_TYPE: ENERGY`へ攻撃力+1250（FIXED）／`ROLE: EN_ATTACKER`へ防御力+1000（FIXED）                                           |
| カオスメイデンツインテ祭り♡ | `MEM_CHAOS_MAIDEN_TWINTAIL_FEST` | `UNIT_TYPE: ENERGY`へ攻撃力+750（FIXED）／味方全体へ最大HP+300（FIXED）                                                        |
| メイド３人のおもてなし？    | `MEM_THREE_MAIDS_HOSPITALITY`    | `ATTRIBUTE: CUTE`へ与ダメージ+2.5%（`APPLY_DAMAGE_MOD`）／`ATTRIBUTE: SMART`へ攻撃力+1250（FIXED）                             |
| 桃園家のお正月              | `MEM_MOMOZONO_NEW_YEAR`          | `UNIT_TYPE: PHYSICAL`へ攻撃力+1250（FIXED）／`UNIT_TYPE: AGILE`へ攻撃力+1250（FIXED）                                          |
| 絶対命令行使権！            | `MEM_ABSOLUTE_ORDER`             | `ROLE: PHYSICAL_ATTACKER`へ物理与ダメージ+2.5%（`APPLY_DAMAGE_MOD`、`damageType: PHYSICAL`）／`UNIT_TYPE: PHYSICAL`へ会心率+5% |
| １日ユーロ・タワー体験      | `MEM_EURO_TOWER_DAY`             | `ROLE: SUPPORT`へ攻撃力+4%（RATIO）／`POSITION_ROW: BACK` かつ `POSITION_COLUMN: RIGHT/CENTER`へ攻撃力+1250（FIXED）           |

`APPLY_DAMAGE_MOD`を含まない4件（`MEM_PANTS_STRAY_CAT`／`MEM_CHAOS_MAIDEN_TWINTAIL_FEST`／`MEM_MOMOZONO_NEW_YEAR`／`MEM_EURO_TOWER_DAY`）は、実`startBattle`（`BattleStarted`）→`MemoryTriggered`／`MemoryResolved`→`AppliedEffect`付与→StateDelta→独立Reducer復元まで実データで検証した（`IT-CAP-MEMORY-STATIC-PROD-001`〜`005`）。対象・非対象の両方（`UNIT_TYPE`／`ROLE`／位置スロットのフィルタが外れるユニット、敵陣営）も同じテストで確認している。

`MEM_THREE_MAIDS_HOSPITALITY`・`MEM_ABSOLUTE_ORDER`のCatalog定義自体も近似なしであり、与ダメージ補正が参照する`EffectActionDefinition.kind: "APPLY_DAMAGE_MOD"`も`DMG-002`（Issue #192）が実装したため、この2件はCapability preflightを通過するようになった（`IT-CAP-MEMORY-STATIC-PROD-006`がこの境界を固定する）。既存の`MEM_WALK_TOGETHER`・`MEM_OJOSAMA_MANNERS`（Issue #47先行バッチ）と同じ扱いであり、Unit側の`DAMAGE_MOD_KIND_UNIMPLEMENTED`テーマと同じく`DMG-002`が解消した。

なお分類表が「静的補正のみ・変換容易」に数えていた6件のうち、この2件は実際には`APPLY_DAMAGE_MOD`を要する（raw原文の「与えるダメージを2.5%上昇」はstat補正ではない）。分類時点の誤りであり、本Issueで実データにあたって確定した。

### Issue #176（M7-008）: 所属・動的効果の20 Memory

未変換だった残り20件を `triggeredEffects` として近似・省略なく変換し、`raw/memories/` 32件の変換を完了した。装備条件は `AFFILIATION`（`18_Affiliation台帳.md` の `AFF_*`）・`ROLE`・`POSITION_ROW`(+`POSITION_COLUMN`)の既存TargetFilterで、発動タイミングは `BattleStarted` / `TurnStarted` で表現できた。

| raw/memories/ ファイル            | memoryDefinitionId               | 変換した装備条件・効果                                                                                                                                                            |
| --------------------------------- | -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Chaos Maiden                      | `MEM_CHAOS_MAIDEN`               | `AFF_CHAOS_MAIDEN`へ攻撃力+250（FIXED）／味方全体へ防御力+200（FIXED）                                                                                                            |
| Colorful Bouquet                  | `MEM_COLORFUL_BOUQUET`           | `AFF_COLORFUL_BOUQUET`へ攻撃力+250（FIXED）／味方全体へ攻撃力+250（FIXED）                                                                                                        |
| Pyxis Ma Soeur                    | `MEM_PYXIS_MA_SOEUR`             | `AFF_PYXIS_MA_SOEUR`へ攻撃力+250（FIXED）／味方全体へ行動速度+12（FIXED）                                                                                                         |
| Sirius Sugar                      | `MEM_SIRIUS_SUGAR`               | `AFF_SIRIUS_SUGAR`へ攻撃力+250（FIXED）／味方全体へ最大HP+300（FIXED）                                                                                                            |
| Treble Quintet                    | `MEM_TREBLE_QUINTET`             | `AFF_TREBLE_QUINTET`へ攻撃力+250（FIXED）／味方全体へ会心率+1%（RATIO）                                                                                                           |
| Trinity Jewel                     | `MEM_TRINITY_JEWEL`              | `AFF_TRINITY_JEWEL`へ攻撃力+250（FIXED）／味方全体へ防御力+200（FIXED）                                                                                                           |
| 風紀委員会                        | `MEM_FUUKI_IINKAI`               | `AFF_FUUKI_IINKAI`へ攻撃力+250（FIXED）／味方全体へ行動速度+12（FIXED）                                                                                                           |
| お忍びシスターの冒険              | `MEM_INCOGNITO_SISTER_ADVENTURE` | `AFF_PYXIS_MA_SOEUR`へ防御力+800（FIXED）／`ROLE: PHYSICAL_ATTACKER`へ攻撃力+2.5%（RATIO）                                                                                        |
| 家族のかたちを象りながら          | `MEM_SHAPING_FAMILY`             | `AFF_KURASUNA`へ与ダメージ+2.5%（`APPLY_DAMAGE_MOD`、種別なし）／`ROLE: CONTROL`へ攻撃力+1250（FIXED）                                                                            |
| 密着！？テントの中の珍騒動        | `MEM_TENT_COMMOTION`             | `AFF_PRE_KURASU_A`へ与ダメージ+2.5%（`APPLY_DAMAGE_MOD`）／`POSITION_ROW: FRONT`へ防御力+2.5%（RATIO）                                                                            |
| 駆け落ちフルスロットル！          | `MEM_ELOPEMENT_FULL_THROTTLE`    | `AFF_CHAOS_MAIDEN`へ与ダメージ+2.5%（`APPLY_DAMAGE_MOD`）／`side: ENEMY` かつ `POSITION_ROW: BACK`へ行動速度-70（FIXED）                                                          |
| エッ◯な罰ゲームやってみた         | `MEM_NAUGHTY_PENALTY_GAME`       | `ROLE: CONTROL`へ物理与ダメージ+3.5%（`APPLY_DAMAGE_MOD`、`damageType: PHYSICAL`）／`ROLE: TANK`へ防御力+1000（FIXED）                                                            |
| 安心する香り                      | `MEM_SOOTHING_SCENT`             | `ROLE: EN_ATTACKER`へEN与ダメージ+3.5%（`APPLY_DAMAGE_MOD`、`damageType: EN`）／`POSITION_ROW: BACK` かつ `POSITION_COLUMN: LEFT`へ攻撃力+2500（FIXED、1行動）                    |
| 少女たちとの邂逅                  | `MEM_ENCOUNTER_WITH_GIRLS`       | 味方全体へ与ダメージ+2%（`APPLY_DAMAGE_MOD`、種別なし）／味方全体へ会心率+1%（RATIO）                                                                                             |
| 新年のご挨拶                      | `MEM_NEW_YEAR_GREETING`          | 味方全体へEN与ダメージ+1.75%（`APPLY_DAMAGE_MOD`、`damageType: EN`）／味方全体へ攻撃力+250（FIXED）                                                                               |
| 腐れ縁で犬猿の仲？                | `MEM_CATS_AND_DOGS_BOND`         | `POSITION_ROW: FRONT`へ物理与ダメージ+3%（`APPLY_DAMAGE_MOD`、`damageType: PHYSICAL`）／`POSITION_ROW: FRONT`へ最大HP+1500（FIXED）                                               |
| 不満と不安                        | `MEM_DISCONTENT_AND_ANXIETY`     | **`TurnStarted`**で`POSITION_ROW: FRONT`へ攻撃力+1%（RATIO）／`BattleStarted`で`POSITION_ROW: BACK`へ最大HP+1500（FIXED）                                                         |
| 忙しい時のまどろみ                | `MEM_BUSY_DAY_SLUMBER`           | **`TurnStarted`**で`POSITION_ROW: BACK`へ被ダメージ-5%（`APPLY_DAMAGE_MOD`、`consumption: NEXT_INCOMING_ATTACK`）／`BattleStarted`で同じ後衛へ防御力+1000                         |
| お傍にいるのはいつでもピコですよ♪ | `MEM_ALWAYS_PICO_BESIDE_YOU`     | `POSITION_ROW: BACK` かつ `POSITION_COLUMN: CENTER`へ攻撃力+3000（FIXED、1行動）と「三ツ星」（`MARKER_MEM_ALWAYS_PICO_BESIDE_YOU_THREE_STARS`）／味方全体へ最大HP+300・防御力+300 |
| 気になる装備                      | `MEM_CURIOUS_EQUIPMENT`          | 味方全体へ最大HP+2500（FIXED）／`side: ENEMY` かつ `POSITION_ROW: FRONT`へ防御力-1%（RATIO）                                                                                      |

`MEM_ALWAYS_PICO_BESIDE_YOU`（Marker付与）は、Domain・StateDelta・独立Reducer復元までは実データで完走できるが（`IT-CAP-MEMORY-DYNAMIC-PROD-007`）、v1のAPI契約が `MarkerStateResponse.sourceUnitId` を必須にしているためHTTPレスポンスとしては表現できない。必須プロパティの削除は `10_API設計.md`「バージョニング」の破壊的変更に当たるためv1契約は据え置き、新規Capability `CAP_MEMORY_GRANTED_MARKER`（`runtimeStatus: PLANNED`、`REL-008`／Issue #263）でCapability preflightが編成不可として弾く（PR #262レビュー[P1]）。

`APPLY_DAMAGE_MOD`を含まない11件（Capability preflightを通る10件と、上記のとおりAPI公開だけを保留した`MEM_ALWAYS_PICO_BESIDE_YOU`）は、実`startBattle`（`BattleStarted`）／実`advanceBattle`（`TurnStarted`）→`MemoryTriggered`／`MemoryResolved`→`AppliedEffect`・`MarkerState`付与→StateDelta→独立Reducer復元まで、未改変のproduction Catalogで検証した（`IT-CAP-MEMORY-DYNAMIC-PROD-001`〜`007`）。対象・非対象の両方（所属外・ロール外・列外のユニット、敵陣営、所属メンバーが0件で`MemoryTriggered`自体が発行されない境界）も同じテストで確認している。

`APPLY_DAMAGE_MOD`を含む9件（`MEM_SHAPING_FAMILY`／`MEM_TENT_COMMOTION`／`MEM_ELOPEMENT_FULL_THROTTLE`／`MEM_NAUGHTY_PENALTY_GAME`／`MEM_SOOTHING_SCENT`／`MEM_ENCOUNTER_WITH_GIRLS`／`MEM_NEW_YEAR_GREETING`／`MEM_CATS_AND_DOGS_BOND`／`MEM_BUSY_DAY_SLUMBER`）のCatalog定義自体も近似なしだが、`CAP_DAMAGE_MOD`が`runtimeStatus: PLANNED`（`DMG-002`／Issue #192）である間はCapability preflightが編成不可として弾く。M7-007の2件と同じ扱いで、`IT-CAP-MEMORY-DYNAMIC-PROD-008`がこの境界と全20件の変換内容（filter・発動タイミング・補正値・期間・消費条件）を1行ずつ固定する。

本Issueで必要になった唯一のDomain拡張は、`MarkerState`の付与元である。`APPLY_MARKER`は`MarkerState.sourceId`を要求するため、`catalog-integrity.ts`がMemory由来の`APPLY_MARKER`を`MEMORY_REQUIRES_SOURCE_UNIT`として拒否していた。R-MEM-04（Memoryは使用者BattleUnitを持たず陣営を source side とする）に合わせ、`MarkerState`・`MarkerSnapshot`・`MarkerApplied`／`MarkerUpdated` payloadを`sourceId`／`sourceSide`のexactly-one unionへ揃えた（`UT-R-EFF-10-019`／`UT-R-EFF-10-020`／`IT-CAP-MEMORY-DYNAMIC-PROD-007`）。API公開（`MarkerStateResponse`）はv1契約を据え置いたため`REL-008`（Issue #263）が担当する。

変換上の判断:

- 「会心率を1%上昇」（Treble Quintet・少女たちとの邂逅）は、同じraw表現を持つ`MEM_WALK_TOGETHER`（Issue #47）・`MEM_ABSOLUTE_ORDER`（Issue #178）と同じく`valueType: RATIO`（基礎会心率に対する割合）で変換した。raw原文だけからは「1パーセントポイント加算」とも読めるが、既存2件と異なる解釈を同一バッチで持ち込む方が害が大きいため、既存慣習に揃えたうえでここに論点として記録する。`CRITICAL_RATE`のRATIO/FIXED解釈を確定するなら、既存3 Memoryをまとめて再変換する別Issueで扱う。
- 「1行動の間」（安心する香り・お傍にいるのはいつでもピコですよ♪）は`timeLimit: { unit: ACTION, count: 1, owner: EFFECT_TARGET }`とした。`owner: EFFECT_SOURCE`は付与者ユニットの行動を減算契機にする宣言で、Memoryでは`catalog-integrity.ts`が拒否する（PR #260再レビュー[P2]）。
- 「三ツ星」には期間の記載がないため、Markerは`timeLimit: { unit: BATTLE, count: 1 }`・`stack: { policy: ADD, max: null }`とした（「1行動の間」は攻撃力バフだけに掛かる修飾）。
- 「ターン開始時に発動」の2件は、raw原文に期間・重複の指定がないため毎ターン重ねて付与される（`IT-CAP-MEMORY-DYNAMIC-PROD-005`が2ターン分の重ね掛けを固定する）。

## 後続バッチへの申し送り

- Memory側の残作業は無い（Issue #176／M7-008 で `raw/memories/` 32件すべてが変換済み）。所属条件を持つ11件が参照する affiliationId の採番方針と、対応する40キャラクター・58 UnitへのaffiliationId付与はIssue #161で完了している（[`18_Affiliation台帳.md`](./18_Affiliation台帳.md)）。
- Issue #178（M7-007）で新たに確認したこと: Memory由来の`triggeredEffects`でも`UNIT_TYPE`／`ROLE`／`ATTRIBUTE`／`POSITION_ROW`+`POSITION_COLUMN`のTargetFilterはUnitのPSと同じ`CAP_TARGET_FILTER_ORDER`でそのまま表現でき、静的補正Memoryの変換に新しいスキーマ拡張は不要だった。
- Issue #176（M7-008）で新たに確認したこと: `AFFILIATION`フィルタと`side: ENEMY`（Memoryを指定した陣営から見た相対陣営）はMemory由来のEffectSequenceでもそのまま解決でき、`TurnStarted`発動も`M7-006`の配線で追加実装なしに動く。唯一のギャップは`MarkerState`が付与元ユニットを必須にしていたことで、本Issueで`AppliedEffect`と同じ`sourceId?`/`sourceSide?`へ揃えて解消した。残る効果側のブロッカーは`APPLY_DAMAGE_MOD`（9 Memory、`DMG-002`／Issue #192）だけである。
- 残り23ユニットの変換は、代表10ユニット昇格時と同水準の精度（Issue #41/#44/#45/#46 相当）を要するため、5〜15ユニット程度のバッチに分割した後続Issueで進める。
- 「不完全変換の詳細」に挙げた表現ギャップ（自身/対象除外フィルタ、AOE内の対象別条件分岐、周期発動、クールタイムリセット、動的な被ダメージ軽減条件、位置関係trigger selector、同時発生イベント除外）は、Issue #44 のG-01〜G-10と同様に、対応するスキーマ拡張を検討する後続Issueの候補とする。
- Issue #55 Batch A で新たに確認したギャップ: Marker所持数の昇順/降順を指定する`TargetOrderKey`（最大HP/Marker数が最も高い・少ない敵の選定）、HPを直接減算する`EffectAction`（コストとしての自傷）、将来のリソース獲得量を割合で減少させる`EffectAction`、「対象が何らかのバフ/デバフを持つか」を汎用判定する`ConditionDefinition`、累計ダメージが閾値を超えるたびに周期発火する`ConditionDefinition`、同一EffectSequence内で直前の`DAMAGE`が会心だったかを判定する`ConditionDefinition`。いずれも`APPLY_DAMAGE_LINK`同様、対応するスキーマ拡張を検討する後続Issueの候補とする。
- Issue #59 Batch B で新たに確認したギャップ: 特定Marker/効果の解除・失効に連動して他の効果を解除する`linkedEffectGroupId`相当の仕組み、状態異常の種類単位（気絶のみ等）を指定する`EFFECT_IMMUNITY`カテゴリ、`REMOVE_EFFECTS`の解除件数上限、`APPLY_DAMAGE_MOD`が攻撃者側のMarker所持を条件にする仕組み、`TargetFilterDefinition`のMarker所持数（閾値）判定、`APPLY_SUBUNIT`の`duration`フィールド（存続期間の明示指定）、unitType優先・自身除外優先を表す`TargetOrderKey`、`APPLY_SUBUNIT.additionalDamage`に付随デバフを持たせる仕組み、列単位でMarkerの所在を判定する`TargetFilter`/`AreaDefinition`。いずれも対応するスキーマ拡張を検討する後続Issueの候補とする。
- Issue #57 Batch C で新たに確認したギャップ: `HP_RATIO_SCALE`の`direction`フィールド値候補未定義によるMapper未サポート（HPが少ないほど効果が高まる系のダメージ・バフ）、他スキルのクールタイムを操作する`EffectAction`、既存効果の残存有無に応じてdurationを動的に変える仕組み、`HEAL`に対象数で総量を分配するDISTRIBUTE相当の表現、一時的に防御力無視（piercing）を後続の自身の攻撃へ付与する`EffectAction`、複数対象のうちいずれかが撃破されたかを判定する`ConditionDefinition`。いずれも対応するスキーマ拡張を検討する後続Issueの候補とする。なお「対象が状態異常だった場合」という汎用条件自体は、`TARGET_STATE.field: HAS_STATUS`の`STUN`/`FREEZE`/`BLIND`を`OR`で束ねることで近似なしに表現できることを確認した（`UNIT_MERU_FLATSPIN`で採用）。
- Issue #56 Batch D で新たに確認したギャップ: 「混乱」（対象を陣営ごと入れ替える攻撃振り替え・特殊ダメージ計算式）に相当する状態異常・効果表現の全面的な欠如（`UNIT_OLGA_VETERAN`のEX。識別用Marker付与のみに近似）、`REMOVE_EFFECTS`/`EFFECT_IMMUNITY`の`categories`に`SHIELD`・`SUBUNIT`・`BUFF`（`DEBUFF`の対になる正の効果全体）に相当するカテゴリが存在しないこと（現行は`DEBUFF`/`STATUS`/`MARKER`/`DAMAGE_MOD`/`SPECIFIC_EFFECT`のみ）、`APPLY_STAT_MOD.stacking`に`Marker`の`stack.max`相当の重複上限フィールドが存在しないこと、`REMOVE_MARKER`がMarkerの解除数（部分解除）を指定できないこと、解決済み束縛から「もう1体」のような非特定の対象を除外する`TargetFilter`が存在しないこと（自身除外と同根の制約だが除外対象が任意の場合）。いずれも対応するスキーマ拡張を検討する後続Issueの候補とする。
- Issue #58 Batch E で新たに確認したギャップ: `EffectActionDefinition.kind: APPLY_REFLECT`は`14_Catalog定義スキーマ.md`にpayload例が定義されているにもかかわらず、対応する`CAP_REFLECT_DAMAGE`が`catalog-src/capabilities.json`のCapabilityレジストリに未登録であり、登録なしに使用すると`validate-catalog`が`UNKNOWN_CAPABILITY`で拒否すること（`UNIT_LUNA_HUNGRY`の「ネガティブヘイト」で反射効果を省略）。これは新規のスキーマ/Mapper拡張ではなくレジストリ登録漏れであるため、対応するCapability追加は本バッチのOut of scope（Catalog schema / Mapper拡張）の外側で、レジストリ更新のみの小さな後続Issueとして起票することを推奨する。既存ギャップ（自身/対象除外フィルタ、`RUNTIME_COUNTER`のmodulo未対応、`HP_RATIO_SCALE`のMapper未サポート、`REMOVE_EFFECTS`のBUFFカテゴリ不在、`APPLY_DAMAGE_LINK`未実装、Marker所持数順の`TargetOrderKey`不在、HPを直接減算する`EffectAction`不在）は本バッチでも複数ユニットで再確認された。
- Issue #60 Batch F（本Issue、Issue #54 の最終バッチ）で新たに確認したギャップ: 攻撃力・行動速度が最も低い/速い対象を選ぶ`TargetOrderKey`（現行は`HIGHEST_ATTACK`のみで`LOWEST_ATTACK`相当や速度系の値が存在しない）、ダメージを回復効果へ変換する状態異常（`APPLY_STATUS.status`列挙に「幻惑」に相当する値がない）、`APPLY_DAMAGE_MOD`が攻撃者側の相対HP割合を条件にする仕組み、「HPが70%未満の対象がいない」のような集合の一部がしきい値を満たすかを判定する`ConditionDefinition`、回復効果を対象へ転送するリンク相当の`EffectAction`。既存ギャップ（同一EffectSequence内の直前`DAMAGE`会心判定の欠如、AOE対象ごとの条件分岐、`APPLY_SUBUNIT.duration`の欠如、累計ダメージ周期発火、複数対象のいずれかが生存/撃破されたかの判定、状態異常種別限定のtriggerフィルタ、同時発生イベント除外）も本バッチで複数ユニットにわたり再確認された。
- `raw/units/` の全69ユニットの変換が本Issueで完了した（未変換0件）。なお【理解深き老成の智者】タチアナ・ドロズドヴァは新規実装キャラクターで、スクレイピング時点の`raw/units/`ファイルはステータス・スキル名・威力のみを収録し、スキル効果本文が空欄だった。効果文は`raw/`と数値が完全一致する外部Wiki（kamigame.jp、Lv.15/MAX表記）を参照して補完し、変換した。他ユニットのように`raw/`単体では変換できないため、将来`raw/units/`のタチアナ・ファイルを更新する場合は本台帳とcatalog-srcとの整合を再確認すること。
