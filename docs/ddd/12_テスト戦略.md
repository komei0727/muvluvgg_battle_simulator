# テスト戦略

## 目的

本書は、Battle Simulation Contextの仕様を継続的に検証するため、次を定義する。

- テストレベルと各レベルの責務
- ルールID、仕様確認ID、テストケースのトレーサビリティ
- 乱数、時刻、IDを制御した決定的なテスト方法
- ドメインルール、状態遷移、イベント、状態差分の検証方法
- API、Catalog、Worker Pool、負荷試験の範囲
- CIでの実行区分と品質ゲート

本書は [07\_戦闘ルール詳細.md](./07_戦闘ルール詳細.md) の110ルール、[08\_ドメインイベント.md](./08_ドメインイベント.md) 以降の設計、および [02\_仕様確認事項.md](./02_仕様確認事項.md) の決定・保留状況をテストへ対応づける。

## 品質目標

- 決定済みのドメインルールを変更時に即座に検知できる。
- 同じテスト入力と固定RandomSourceから同じ結果を得られる。
- 単独ルールの誤りと、複数ルールの接続順序の誤りを区別して診断できる。
- イベントログと状態差分から戦闘経過を一意に追跡できる。
- API、Worker、Catalogなど技術境界がドメインの意味を変えない。
- 性能劣化、無限連鎖、メモリー増大からサービスを保護できる。

## 基本方針

- 仕様の正本は設計書とルールIDであり、現在の実装結果をそのまま期待値にしない。
- ドメインルールは最も小さい責務のテストで詳細に検証する。
- Battle全体のテストでは、ルール間の順序、状態遷移、イベントを検証する。
- 外部ポートだけをテストダブルへ差し替え、Domain Entity同士を過剰にmockしない。
- 正常例だけでなく、境界値、不成立条件、順序逆転、再入防止を検証する。
- 大きなレスポンス全体のsnapshotだけに依存せず、意味のあるフィールドと不変条件をassertする。
- 発見した不具合には、修正前に最小の再現テストを追加する。
- 実行時間の長い負荷試験と、PRごとの高速な回帰テストを分離する。

## テストレベル

| レベル             | 主対象                                 | 外部依存                         | 主な目的                     | 実行頻度          |
| ------------------ | -------------------------------------- | -------------------------------- | ---------------------------- | ----------------- |
| Domain Unit        | 値オブジェクト、ポリシー、計算サービス | なし                             | 110ルールの個別検証          | 全PR              |
| Domain Scenario    | Battle集約、状態遷移、イベント連鎖     | 固定RandomSource                 | 複数ルールの接続順序         | 全PR              |
| Application Unit   | UseCase、事前検証、結果組み立て        | Portのfake／stub                 | ユースケース調整とエラー     | 全PR              |
| Catalog Contract   | JSON Schema、参照整合性                | テストCatalog／実Catalog         | 定義データの構造・意味整合性 | 全PR              |
| API Contract       | DTO、JSON Schema、HTTP変換             | Fastify inject、fake Worker Pool | 外部契約の互換性             | 全PR              |
| Worker Integration | ESM Worker、Piscina、キャンセル        | 実Worker Thread                  | スレッド境界と障害処理       | main／PR選択      |
| End-to-End         | HTTPからBattle完了まで                 | 実Catalog、実Worker              | 全経路の成立確認             | main／リリース    |
| Property／Model    | 比較器、差分、リソースなど             | 生成データ                       | 広い入力空間の不変条件       | 全PR／nightly     |
| Load／Soak         | HTTP、Pool、Battle、メモリー           | 配備相当環境                     | 容量・性能・耐久性           | nightly／リリース |

Domain UnitとDomain Scenarioをテストの中心とする。End-to-Endだけでルールの網羅を目指さない。

## テスト配置

```text
tests/
  unit/
    domain/
    application/
    infrastructure/
  scenario/
    battle/
  property/
  contract/
    api/
    catalog/
    openapi/
  integration/
    worker/
    http-worker/
  e2e/
  load/
  fixtures/
    catalog/
      minimal/
      scenarios/
      invalid/
  support/
    builders/
    random/
    clock/
    assertions/
    traceability/
```

テストを対象コードと同じ場所へ置く方式を採用する場合も、上記のレベルとfixtureの責務は維持する。

### 実際の配置（co-location + 横断テストのディレクトリ分類）

本 project は上記ツリーではなく **co-location 方式**を採用する。単一モジュールに属するテストは対象コード直下へ `<module>.test.ts` で置き（Unit／Scenario／Contract／Property を含む）、test 支援コードは `src/testing/`（`clock`／`id`／`random`／`traceability`／`fixtures`／`scenario`＝Builder・harness・assertion／`property`＝arbitrary。上記 `support/`＋`fixtures/` 相当）に集約する。

単一モジュールに属さない**横断テスト**は `src/__tests__/` 配下を**カテゴリ別サブディレクトリ**へ分類する。

```text
src/__tests__/
  architecture/        # レイヤー/モジュール境界の禁止import検査（layer-boundary・module-boundary）
  deploy/              # Cloud Run / Knative マニフェスト契約
  e2e/                 # HTTP→Worker→Battle の実経路（*.e2e.test.ts）
  load/                # 負荷・耐久 baseline（*.load.test.ts）
  scenario/            # SCN-BTL 全戦闘シナリオ（runScenario harness）
  golden/              # ユニット軸: production ユニットごとの golden battle（+ __snapshots__/）
  production-catalog/  # 機能軸: 収束先ゼロの移送待ち層（IT-CAP-*。Issue #379）
    units/             # ユニット効果軸: <UNIT_ID>.test.ts（IT-UNIT-*）
    memories/          # ユニット効果軸: <MEM_ID>.test.ts（IT-MEM-*）
```

vitest の各 config は `src/**/…` の再帰globのためサブディレクトリで動作する。`production-catalog/units`・`production-catalog/memories`（ユニット効果軸）、`golden`（完走軸）、`production-catalog/` 直下（移送待ちの機能軸）の関係は「production Catalog 結合テストの軸モデル」節を参照。

#### ファイル名から対象実装が辿れること（REF-013／Issue #316）

co-location の目的は「実装を開いたら隣にテストがある」探索性なので、**テストファイル名は必ず対象実装の
モジュール名で始める**。1モジュールのテストを観点で分割する場合は `<module>.<観点>.test.ts` と
**ドット区切り**にし、`<観点>-service.test.ts` のように**実在しないモジュール名を名乗らない**
（`poison-grant-service.ts` は存在せず実装は `continuous-damage-service.ts` にある、という状態を作らない）。
`.integration`／`.e2e`／`.load` は観点ではなく実行段の指定であり、`<module>.<観点>.integration.test.ts`
のように観点の後ろに置く。

観点で分割している現在のスイートは次の20件（`REF-015`時点の全件）。

| 対象モジュール                                            | 分割スイート                                                                                                                                                                                                                                                                                          |
| --------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `domain/battle/lifecycle/battle.ts`                       | `battle.memory-activation.test.ts`                                                                                                                                                                                                                                                                    |
| `domain/battle/lifecycle/continuous-damage-service.ts`    | `continuous-damage-service.poison.test.ts`                                                                                                                                                                                                                                                            |
| `domain/battle/lifecycle/effect-action-group-resolver.ts` | `effect-action-group-resolver.status.test.ts`／`.stat-mod.test.ts`／`.damage.test.ts`／`.heal.test.ts`／`.resource.test.ts`／`.modifier.test.ts`／`.absorber.test.ts`／`.continuous-damage.test.ts`（`effect-action-group-resolver.test.ts`はstep・EffectSequence走査とディスパッチャ共通契約を担当） |
| `infrastructure/worker/simulation-worker-pool.ts`         | `simulation-worker-pool.cancellation.integration.test.ts`／`.capacity.integration.test.ts`／`.crash-circuit.integration.test.ts`／`.poisoning.integration.test.ts`／`.shutdown.integration.test.ts`                                                                                                   |
| `presentation/http/build-server.ts`                       | `build-server.cors.test.ts`／`build-server.logging.test.ts`／`build-server.disconnect.integration.test.ts`                                                                                                                                                                                            |
| `apps/ui` `features/simulation/api-client.ts`             | `api-client.simulate.test.ts`（`api-client.test.ts`は`getCatalog`を担当）                                                                                                                                                                                                                             |
| `apps/ui` `features/simulation/response-validator.ts`     | `response-validator.simulation.test.ts`（`response-validator.test.ts`は`validateCatalogResponse`を担当）                                                                                                                                                                                              |

`bootstrap/index.integration.test.ts`と`infrastructure/worker/simulation-worker-pool.integration.test.ts`は
観点分割ではなく実行段だけを分けた `<module>.integration.test.ts` であり、上表には含めない。

`effect-action-group-resolver.*.test.ts`の観点はREF-015で切り出した`effect-action/`のkind別
ハンドラ群に対応する（`status`→`status-effect-action.ts`、`absorber`→`absorber-effect-action.ts`など）。
ハンドラは`applyEffectActionGroups`経由でしか駆動できないため、テストの対象実装はresolverのままであり、
ファイル名も`effect-action-group-resolver.<観点>.test.ts`に揃える。

単一モジュールへ紐づかない横断テストは原則 `src/__tests__/` のカテゴリ別サブディレクトリへ置く。
そこへ移すと検証できなくなるもの（対象ディレクトリ内の実装群を総なめする不変条件、`docs/` の台帳自体を
対象とする照合など）だけを co-location 側へ残し、**その場合はファイル冒頭のコメントで、対象モジュール名を
名乗らない理由を明示する**。現在の該当ファイルは `immutability.test.ts`／`fixtures.test.ts`／
`affiliation-registry.test.ts`／`remaining-work.test.ts`／`m7-completion-audit.test.ts`／
`m8-completion-audit.test.ts`／`catalog-production-units.test.ts`／`catalog-src-inventory.test.ts`／
`catalog-src-production.test.ts`／`openapi.test.ts`／`state-restoration.test.ts`、および対象が
`routes/catalog-route.ts` でありながら実サーバー経由でしか観測できないため `http/` 直下に置く
`battle-simulation-catalog-route.test.ts`。`REF-024`（Issue #353）で `production-catalog/` から移した
`simulation-route.log-level-projection.test.ts`／`simulation-route.memory-granted-marker.test.ts`
（実サーバーを建てるため `http/` 直下）と、`schemas/simulation/simulation-schema.doc-schema.test.ts`
（対象モジュールと同居）も同じ規約に従う。

### 共通テストビルダー（`src/testing/fixtures/`）

`production-catalog` テストが個別に持っていた同名ローカルヘルパ（`member`／`testUnitDefinition`／
`actorFor`／`definitionsWith`／`initialSnapshotFor`／`seedRecorder`／`contextFor` ほか）は
`src/testing/fixtures/` へ集約する。`scenario/`（合成Catalogで戦闘を組む Builder・harness）とは
役割が異なり、こちらは**実 `catalog/` から読んだ定義を素材に、戦闘途中の状態を直接組み立てる**
ためのものである。

| モジュール                 | 提供するもの                                                                        |
| -------------------------- | ----------------------------------------------------------------------------------- |
| `production-catalog.ts`    | `loadProductionSnapshot`／`unitFrom`・`skillFrom`・`effectActionFrom`・`memoryFrom` |
| `battle-actors.ts`         | `testPartyMember`／`testBattleUnit`                                                 |
| `unit-definitions.ts`      | `testUnitDefinition`                                                                |
| `battle-definitions.ts`    | `definitionsWith`／`definitionsForSkill`                                            |
| `battle-state.ts`          | `initialSnapshotFor`／`reconstruct`                                                 |
| `event-seed.ts`            | `seedRecorder`                                                                      |
| `effect-action-context.ts` | `effectActionGroupContext`／`completedTargetIdsOf`                                  |
| `markers.ts`／`random.ts`  | `testMarker`／`noMissNoCrit`                                                        |

ユニット効果軸のテストが共有するハーネスは `src/testing/production-unit/` に置く。

| モジュール                | 提供するもの                                                                                                             |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `skill-behaviour.ts`      | `observeSkillUse`／`productionBoard`／`SkillBehaviourCase`（`-001` の振る舞い表の駆動）                                  |
| `trigger-events.ts`       | PS契機イベントの型付きfactory（`unitBeingAttacked`・`turnStarted` ほか8種別）                                            |
| `memory-manifestation.ts` | `observeMemory`／`observeMemoryTurnStarts`／`mirroredForEnemyDeclaration`／`MEMORY_SLOTS`（Memory側の6スロット固定盤面） |
| `passive-activation.ts`   | `openPassiveChain`／`activatedPassiveSkillIds`（PS発動連鎖）                                                             |
| `definition-closure.ts`   | `unitEffectActionClosure`／`memoryEffectActionClosure`（`-003` 実行ベース網羅監査）                                      |

ID は必ずこれらの型付きビルダー経由で組み、テストから branded type への `as never` キャストを
持ち込まない。定義の取得は `!` ではなく throw する getter（`skillFrom` 等）を使い、
Catalog 側の欠落をテスト失敗ではなく明示的なエラーとして表面化させる。

既定値は「会心なし・属性相性なし」の決定的ステータスとし、乱数消費と補正を検証対象の
検証対象の機能だけへ閉じる。テスト意図に関わる値だけを overrides で明示する。

`runProductionUnitBattle`（`testing/scenario/run-production-battle.ts`）の適用基準は
`src/testing/fixtures/index.ts` の冒頭コメントを正本とする。要約すると、実Catalogユニットの
固定1対1戦闘を実経路で完走させ最終結果だけを見るテストには適用し、戦闘途中状態の作り込み・
抽選単位の乱数制御・resolver 内部の中間産物の検証・固定1対1で表現できない編成を必要とする
テストには適用しない。

## テストケース識別子

テストケースへ次の形式の安定したIDを付ける。

```text
<LEVEL>-<SUBJECT>-<NUMBER>
```

例：

- `UT-R-NUM-01-001`：ルール単体テスト
- `SCN-BTL-006`：Battle複合シナリオ
- `APP-SIM-004`：Applicationテスト
- `API-SIM-012`：API契約テスト
- `INT-WRK-003`：Worker結合テスト
- `PROP-ORD-002`：行動順のプロパティテスト

テスト名にはケースIDと期待する振る舞いを含める。

```text
UT-R-ACT-03-004: AP0・EX満タン・行動不能ならEXを全量消費して待機する
```

ケースIDはファイル名に必須とせず、テスト定義またはメタデータから検索可能にする。

`<NUMBER>` は数字だけにする。`API-CONTRACT-015b` のような英小文字サフィックスや `API-ERR-PATH-escaping` のような語尾は、収集器（`test-case-definitions.ts` の `TEST_CASE_ID_PATTERN`）から見て別IDにも同一IDにもなり得ず、一意性検査をすり抜ける。同じ理由で、あるテストのタイトルに別テストのIDを書かない（参照は「上記の互換性検査」のように文章で示す）。

一意性は `UT-TRACEABILITY-005` が `UT`／`IT`／`SCN`／`E2E`／`PROP`／`API` の全レベルへ機械適用する。`it.each` のように1つの論理ケースを複数行で回すものだけ `INTENTIONALLY_SHARED_TEST_CASE_IDS` へ登録する。

## トレーサビリティ

### 追跡単位

次を相互に追跡する。

```text
仕様確認ID（Q-*）
  ↓
ルールID（R-*）
  ↓
テストケースID
  ↓
実装モジュール
```

通常のテストは `R-*` を直接参照する。複数ルールを確認するシナリオは、関連するすべてのルールIDをメタデータへ持つ。仕様決定の経緯確認が必要な場合だけ `Q-*` も関連づける。

### ルールカバレッジ台帳

機械判読可能な台帳をテストコード内に持つ。

```text
RuleTestCoverage {
  ruleId
  testCaseIds[]
  kinds: POSITIVE | NEGATIVE | BOUNDARY | PROPERTY | SCENARIO
}
```

CIは次を検証する。

- [07\_戦闘ルール詳細.md](./07_戦闘ルール詳細.md) から抽出した全ルールIDが台帳に存在する。件数の正本は[`17_残作業対応表.json`](./17_残作業対応表.json)の`current.rules.total`であり、本書と[13\_実装計画.md](./13_実装計画.md)が計画時点の値として書く「110ルール」ではない（M7・M8の実装中に8件を新設し、`REF-022`／Issue #351のEffect kind棚卸しで`R-DMG-06`を後追い新設して現在118件）。
- 台帳が未知または重複したルールIDを参照していない。
- テストケース未登録のルールは、[`17_残作業対応表.json`](./17_残作業対応表.json)で完了責任を持つTaskへ一意に割り当てられている。
- ルールの追加・削除時に台帳更新がない場合は失敗する。
- 残作業（Rule・不完全変換テーマ・未変換Memory）の割当先Taskが、`17_残作業対応表.json`の`tasks[].status`上で`OPEN`である。
- `13_実装計画.md`「M8：高度ダメージ 21件」の21ルールと、M8の実装中に新設した4ルールが、いずれも空でない`testCaseIds`を持つ。
- M8のTaskが未完了Rule・不完全変換テーマを1件も保持していない。

Feature Completeの最終ゲートでは、各ルールに少なくとも1件の実行対象テストがあり、残作業対応表の`ruleAssignments`が空であることを要求する。開発途中は未完了ルールを空の`testCaseIds`として明示し、`remaining-work.test.ts`が全件のTask割当を検証する。Markdownのチェックリストだけで管理せず、テスト実行と台帳をCIで照合する。

「所有者不在」（完了責任を持つIssueがcloseしても残作業だけが台帳へ残る状態）の検出は`M7-010`（Issue #177、M7完了監査）が`m7-completion-audit.test.ts`として追加したが、検証対象がCapability台帳だったため`REF-023`（Issue #352）のCapability廃止で同テストごと退役した。`remaining-work.test.ts`は引き続き件数と割当の一致を見る。

最後の項目は`DMG-011`（Issue #186、M8完了監査）が`m8-completion-audit.test.ts`（`UT-AUDIT-M8-001`〜`002`）として追加した。所有者不在とは別の壊れ方 — 件数は整合しているのに、そのマイルストーンが引き受けたRule・変換行が黙って次のマイルストーンへ滑り込む — を、マイルストーン単位の完了境界として固定する。`13_実装計画.md`の21ルール列挙をテスト側へ写して照合するため、計画側の列挙を書き換えただけでは通らない。

### Effect kind カバレッジ台帳

すべてのEffect kind（`effect-action-definition.ts`の`EFFECT_ACTION_KINDS`、現在26種）は、いずれかの戦闘ルール（`R-*`）に紐づき、単体テストでその効果を保証する。`src/testing/traceability/effect-kind-coverage.ts`の`EFFECT_KIND_COVERAGE`が正本で、`Record<EffectActionKind, { ruleIds, testCaseIds }>`型により kind の追加漏れ・削除余剰をコンパイルエラーとして検出する。CIは次を検証する（`UT-TRACEABILITY-006`〜`009`）。

- 台帳のキー集合が`EFFECT_ACTION_KINDS`と完全一致する（型強制のすり抜けに対する実行時の二重防御）。
- 各kindの`ruleIds`が非空で、全IDがルールカバレッジ台帳に実在する（`UT-TRACEABILITY-002`経由で`07_戦闘ルール詳細.md`の実在ルールへ連鎖する）。
- 各kindの`testCaseIds`が非空で、それぞれが実行対象テストちょうど1件へ解決される。
- 各kindの`testCaseIds`が、そのkindの`ruleIds`のいずれかの`RULE_COVERAGE.testCaseIds`に登録されている（kind→ルール→テストの三者チェーンが閉じており、証跡テストがどのルールの振る舞いを保証するか常に追跡できる）。

新しいEffect kindを追加するときは、規定するルール（無ければ`07_戦闘ルール詳細.md`へ新設）と、その効果を保証する単体テストを同一PRで台帳へ登録する。

### production 全ID網羅監査（ユニット効果軸の受け皿）

全Unit・Memoryの全Skill・全EffectActionは、ユニット単位の結合テスト（`src/__tests__/production-catalog/units/<UNIT_ID>.test.ts`／`memories/<MEM_ID>.test.ts`）が効果発現を検証する。**Unit 69件・Memory 32件すべてが載り、`src/testing/traceability/production-id-coverage.ts`のallowlist（`UNCOVERED_UNIT_IDS`／`UNCOVERED_MEMORY_IDS`）は空である**（`REF-031`／Issue #361 でUnit、`REF-033`／Issue #363 でMemoryが空になり、`REF-025`／Issue #354 が確認した）。allowlistの機構自体は将来のUnit・Memory追加に備えて存置する。CIは次を検証する（`UT-AUDIT-UNITCOV-001`〜`005`）。

- allowlist外の全定義に対応テストファイルが存在し、当該定義の全Skill IDと、Skillから到達可能な全EffectAction ID（payload経由のEffectAction間参照の閉包を含む）がファイル内で参照されている。
- allowlist掲載の定義には対応テストファイルが存在しない（カバー済み定義のallowlist残留を禁止し、台帳が縮小のみ許されることを機械的に強制する）。
- allowlistとカバー済み集合の合併がcatalogの全定義と過不足なく一致する（重複・catalog未知IDも拒否）。
- **両allowlistが空である**（`UT-AUDIT-UNITCOV-005`）。上の3つは「allowlistに載っている定義」を検査対象から落とすため、**新規**定義をCatalogとallowlistの両方へ足してテストファイルを作らない変更を3つとも通してしまう（1つ目は対象外、2つ目はファイルが無いので成功、3つ目は分割が成立する）。空であること自体を固定してこの抜け道を塞ぐ。

allowlistは**縮む方向にしか編集してはならない**。空になった現在、これは「新規Unit・Memoryをallowlistへ載せて先送りすることはできない」ことを意味する — 追加時は同一PRでユニット単位結合テストを書く。この契約は上記`UT-AUDIT-UNITCOV-005`が機械的に強制する。

### M8 ダメージpipelineの通し監査

`R-DMG-05`（ダメージイベント順）と`08_ドメインイベント.md`不変条件#6（ダメージ保存則）は、9手順・5項がそれぞれ4つの実装Task（`DMG-004`〜`DMG-007`）へまたがるため、各Taskは自分が追加したイベント・項の前後関係だけをunit levelで検証していた。`DMG-011`が`__tests__/audit/m8-damage-pipeline-audit.test.ts`（`IT-AUDIT-M8-001`〜`003`）として、実 `catalog/` の全production Unitの実戦闘に対する通し検証を追加した。

- `IT-AUDIT-M8-001`: 全ヒットが`R-DMG-05`の9段階を単調非減少の順で発行し、**かつヒット単位で終端に応じた必須部分列を持つ**。ヒットの窓は`skillUseId`で分けてから`UnitBeingAttacked`で開く（1ヒットの途中で発火したPS連鎖が自分のヒットを内側で完走させるため、発行順だけで切ると親ヒットの残り手順が子ヒットの後ろへ回り込む）。
  - 順序だけを見ると「そのヒットで`DamageCalculated`や`DamageApplied`が欠落しても、別のヒットが同じ段階を出していれば成功する」ため、終端を`MISSED`（`R-HIT-01`、#2で終わる）・`CONVERTED_TO_HEAL`（`R-DTH-01`が#7の適用段階だけを差し替える）・`APPLIED`の3つに分類し、それぞれの必須手順をヒットごとに要求する。
  - 必須部分列は「足りない」ことしか見ないため、終端の意味に反する余分なイベント（回避したヒットのシールド誤消費、回復変換前のサブユニット誤消費など）は**許可側の集合**で弾く。「現れてはならないイベント」の列挙では必ず取りこぼす — 実際に`MISSED`が`ShieldConsumed`・`DamageRedirected`・`UnitDefeated`を、`CONVERTED_TO_HEAL`が`ShieldConsumed`・`LethalDamageSurvived`を禁止できていなかった。許可側で書くことで、pipelineイベントを新設したときの既定が「途中終了する2終端では不許可」になる。
  - 9段階すべてと3終端すべてが実データで観測されることも要求し、空振りを防ぐ。`R-DMG-05`が持つもう一組の正当な途中終了 — PS連鎖によるヒットの取り消し（`UT-R-DMG-05-004`／`008`／`009`）と吸収中の中断（`UT-R-SUB-02-016`）— は現行のproduction定義では発生しないため、意図的に終端へ含めていない。発生した時点でこの検査が失敗し、「production Catalogにその経路が現れた」ことを監査結果として扱える。
- `IT-AUDIT-M8-002`: `DAMAGE_APPLIED`／`CONTINUOUS_DAMAGE_APPLIED`の全件で5項の保存則が成立する。5項それぞれが0でない実データを持つことも要求する。
- `IT-AUDIT-M8-003`: `isLinkedDamage`／`isReflectedDamage`が立った`DamageApplied`から、さらにリンク・反射が発生しない（`R-LNK-03`／`R-INT-03`第2項）。

### production Catalog 結合テストの軸モデル

実データに対する結合テストは、**ユニット効果軸 × golden（完走）軸**の2軸で構成する。機能軸（`IT-CAP-*`）は**収束先ゼロの過渡層**であり、恒久的な層ではない。

| 層                                                                                                                       | 保証する                                                                                                                    | 保証しない                                           |
| ------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| **ユニット効果軸**（`production-catalog/units/<UNIT_ID>.test.ts`／`memories/<MEM_ID>.test.ts`、`IT-UNIT-*`／`IT-MEM-*`） | 実 `catalog/` 定義を**スキル使用1回**の単位で使ったときの、発動 / 対象 / 分岐選択 / 結果 / 付与効果が宣言する期間・消費条件 | 原文との一致（機械検証不能）、エンジン横断の不変条件 |
| **golden（完走）軸**（`golden/`）                                                                                        | 1対1ミラー戦と**混成編成戦**の完走・不変条件・イベント snapshot                                                             | 効果量・効果の正しさ                                 |
| 機能軸（`production-catalog/` 直下、`IT-CAP-*`／`IT-CAT-PROD-*`）                                                        | 移送待ちの機構検証のみ。ユニット効果軸へ移し終えた時点で廃止する（Issue #379）                                              | —                                                    |

#### 機能軸は恒久層ではない（収束先ゼロ）

**機能軸を廃止し、`production-catalog/` 直下を空にする。** 「複数ユニットが登場する」ことは残置の理由にならない。全 Unit・全 Memory がユニット効果軸に載った現在、残る機能軸ファイルはすべて**移送待ち**であって恒久層ではない（進行はIssue #379）。

- 例えば `specific-immunity`（Aoi の STUN 限定 immunity が Lily の STUN を弾く）の被験体は **Aoi の `EFFECT_IMMUNITY` 宣言**で、Lily は実在の STUN を供給しているだけ → Aoi のファイルへ移せる。
- 再付与でしか差が出ない機構（`marker-stack-formula` の stack policy 等）も、「同じスキルを2回使う」はスキル使用単位の範疇なので表の行として書ける。
- どの単一定義にも帰属しない機構（`charge-restriction` の「チャージ中は回避しない」— 抑止する側と抑止される側が別ユニット）は、**関係する全ユニットのファイルへ複製して移送する**。重複は受け入れる。
- ルール自体は `UT-R-*` が合成定義で押さえており（117ルール全てに `testCaseIds`）、機能軸の付加価値は「実データでも成立する」の一点。ユニット効果軸が全実定義を実行すればその付加価値も吸収される。

**効果軸ではないものは移動する**（廃止ではない）。HTTP DTO とログレベル投影を検証していた `log-level-projection-api` / `memory-granted-marker-api` / `response-doc-schema` は `production-catalog/` に置かれていること自体が分類の誤りで、`presentation/http/` 配下へ移した。

残る機能軸ファイルと移送先ユニットの対応表はIssue #379が持ち、12バッチ（`REF-034`〜`REF-045`）へ分割してある。`REF-025`（Issue #354）時点の残数は33ファイル・143 `it()`、`REF-034`（Issue #381、Memory 系3ファイルを retire）の後は30ファイル・136 `it()`、`REF-035`（Issue #382、`turn-boundary-self-selector` を retire）の後は29ファイル・135 `it()`、`REF-036`（Issue #383、発動条件・step条件の3ファイルを retire）の後は26ファイル・118 `it()`、`REF-037`（Issue #384、対象解決の2ファイルを retire）の後は24ファイル・103 `it()` で、いずれも合成定義（`UNIT_TEST_*`／`SKL_TEST_*`）や後続行動を使った機構検証のため、移送には**ユニット効果軸へ `-004`以降を新規に書く**作業を伴う。合成定義であること自体は障壁ではない — `IT-UNIT-MAO-COMMITTEE-004` は合成の敵攻撃スキルをユニット軸ファイルの中で組み立てて実「ステルス」の対象逸らしと消費を固定しており、これが標準形である。

##### ハーネスの契機イベントは発行地点と同じ形にする（`REF-035`）

`turn-boundary-self-selector` が守っていたのは「`battle.ts` が実際に発行する `TurnStarted`/`TurnCompleting` は `sourceUnitId`/`targetUnitIds` を持たない」という**発行地点のイベント形**で、ユニット効果軸のハーネス（`trigger-events.ts` の `turnStarted`/`turnCompleting`）が行動envelopeの中で保持者を帰属させた形を組み立てていたため、retire基準1（同等以上のアサーション）を満たしていなかった。`REF-035` でハーネスを発行地点と同じ形（両欄とも持たない）へ寄せ、`IT-UNIT-KARINA-DOWNER-004` へ移送して retire した。

**ハーネスが実イベントより強い形を合成すると、実戦闘では成立しない契機表現を見逃す。** ターン境界は特定のBattleUnitに帰属しないグローバルイベントなので、`sourceSelector`/`targetSelector` のうち成立し得るのは `SELF`（帰属先が存在しないため所有者自身の視点で成立する）と `ANY` だけで、`ALLY`/`ENEMY`/`OTHER_ALLY` は常に不成立になる。production Catalog の `TurnStarted` 27件・`TurnCompleting` 12件はすべて `SELF`/`SELF` のため定義側の是正は**0件**だった。

一方、ハーネスの合成に依拠していた契機記述が3行落ちた。いずれも「(不成立): 契機は自身のターン開始であり、敵のターン開始では発動しない」という同一の主張で、`SKL_SHIRANA_LUCKY_PS2`／`SKL_SIENA_DIVA_PS2`／`SKL_SIENA_OFFSTAGE_PS1` の3行。**3行とも原文には無い主張**（原文はいずれも「ターン開始時に発動」で、ターンは陣営ごとに分かれない）で、ハーネスが保持者を帰属させていたためだけに成立していた。実装にも定義にも対応物が無いため、是正ではなく行そのものを削除した（定義側の食い違い0件・テスト側の誤った転記3行）。

#### ユニット効果軸の標準形

**単位は EffectAction ではなくスキル使用1回**。EffectAction を1件ずつ単体実行する形では、分岐がどの腕を選ぶか・発動条件が成立するか・production の対象選択が誰に当たるかを一切見られない（`REF-024` 初版がこの形で、第1バッチ40スキル中 `BRANCH` を持つ11スキルの分岐選択を0件しか検証していなかった）。

- 配置は1定義1ファイル（最終的にUnit 69件・Memory 32件）。テストIDは `IT-UNIT-<ID断片>-NNN`／`IT-MEM-<ID断片>-NNN`（定義IDから `UNIT_`／`MEM_` 接頭辞を落とし、アンダースコアをハイフンへ。`NNN`は数字のみ）。一意性は `UT-TRACEABILITY-005` に自動的に乗る。
- **`-001` は振る舞い表**。`(SKL_ID, intent, 前提盤面, 期待する振る舞い)` を `it.each` で駆動する（ハーネスは `src/testing/production-unit/skill-behaviour.ts` の `observeSkillUse`）。AS/EXは `selectAsCandidate`／`isExUsable`→`resolveSkillUse`、PSは `PassiveActivationRuntime` を実際に通す。**`activationCondition` は行動選択層が評価する**ため、`resolveSkillUse` を直接呼ぶだけでは発動可否を見られない。
- 表が持つべき必須観点（「必要なテスト種別」の発動条件・連鎖・リソース・期間に対応）:
  1. **発動** — 成立と不成立の両方を行として持つ。
  2. **対象** — production の `targetBindings`/`filters`/`order`/`area` を実際に通し、当たる相手と当たらない相手を固定する。
  3. **分岐選択** — `BRANCH`/`RANDOM_BRANCH` の**全腕**について、その腕が選ばれる前提を盤面で作る。
  4. **結果と宣言** — 実行済みEffectActionの列と対象（`actions`）・HP変動・効果付与/解除・マーカー・リソース収支・クールタイム。付与効果の期間・消費条件は `AppliedEffect.duration.definition` から観測へ載るため、定義を別途 `toMatchObject` する必要はない。1回の使用では出ない payload 宣言（`stacking.max` 等）だけを行の任意欄で補う。
- 変化しなかった観測項目はキーごと落ちるため、`toEqual` の完全一致が「宣言した振る舞いが起きること」と「余計なことを起こさないこと」を同時に固定する。
- **`intent` 欄は原文の該当句を書く**。`raw/` は `.gitignore` 対象で CI に存在せず、原文と定義を突き合わせるテストはリポジトリに1件も無い。転記が正しいかをレビューできる接点はこの欄だけである（設計書が要求する authoring Markdown 層は未実装）。
- **`-002`** は表のスキル列が `UnitDefinition` の宣言と完全一致すること。**`-003` は実行ベース網羅監査** — `UT-AUDIT-UNITCOV-001` は「IDが文字列として書かれているか」しか見ないため、表に載っているだけで一度も実行されない定義を見逃す。定義から到達可能なEffectActionの閉包と、表を回して実際に実行された集合を突き合わせる。**集合へ入れるのは `EffectActionCompleted` の `resultKind` が `APPLIED` のものだけ**にする（`SKIPPED`/`MISSED`/`REJECTED`/`INTERRUPTED` は効果が一度も発現していないため、数えるとその定義やresolverが壊れていても監査を通してしまう）。前提状態を要する定義（`REMOVE_EFFECTS` 等）は、行の `precedingActions` で**実 production 定義を使って**前提を作る。ただし前提アクションはスキルの対象選択・分岐・発動条件を通さずEffectAction 1件を直接撃つものなので、**その実行を網羅の実績には数えない**（数えると「スキル使用単位で保証する」方針そのものを迂回できる）。到達不能な定義は理由付きの許可リストで除外する。
- **`-004` 以降**は1回のスキル使用で表現できない機構（PS連鎖・EX分配の等分と端数・回復リンクの転送・StateDelta の独立Reducer復元など）。
- 相手役は実Catalogのユニットではなく最小定義（`testUnitDefinition`）を使い、相手側のPS・特性が観測へ混ざらないようにする。

##### Memory 側の標準形

Memory はスキル使用を持たず、`triggeredEffects` が契機イベントから対象群へ EffectAction を配るだけなので、単位は「スキル使用1回」ではなく**契機1回**になる。`-001` は `MEMORY_SLOTS`（行・列・ロール・ユニット種別が互いに相関しない6スロット×両陣営の固定盤面）へ実 `startBattle` を通し、`(EffectAction ID, 当たったスロット, 効果量, sourceSide)` の表と `MemoryTriggered` の発火順を `toEqual` で固定する。`-002` は同じ Memory を相手陣営が宣言したときの鏡像（R-MEM-04 の source side と `side: "ENEMY"` の相対解決）、`-003` は実行ベース網羅監査、`-004` 以降が機構別。

Memory 固有の前提は2つある。

- **所属条件**（`AFFILIATION` TargetFilter）は静的 Catalog の `UnitDefinition.metadata.affiliations` を引くため、既定の「所属なし」6スロットのままでは対象0件になり効果が一度も発現しない。`AFF_*` は Memory ごとに異なり全所属を常設できないので、`MemoryBoardOverrides.affiliationsBySlot` で**そのMemoryのメンバーを1スロットだけ**にし、残る5スロットをそのまま非対象の検証に使う。`-004` は所属メンバーが盤面に1体もいない編成を作り、対象0件の `triggeredEffect` が `MemoryTriggered` 自体を発行しないこと（`08_ドメインイベント.md`「発動直前の再確認」）を境界として持つ。**属性条件**（`ATTRIBUTE` TargetFilter）も同じ扱いで、こちらは `UnitDefinition` ではなく編成時に決まる `BattleUnit.attribute` を読むため `MemoryBoardOverrides.attributesBySlot` で宣言する（盤面全体を実属性へ寄せると属性相性倍率が全ダメージ観測へ混ざるため opt-in）。
- **`TurnStarted` 発動**は `observeMemoryTurnStarts` が実 `advanceBattle` を回して観測する。ただし観測は**ターン開始の解決スコープへ閉じる** — `advanceBattle` は同じ呼び出しの中で行動フェーズまで進めるため、最終的なユニット状態では被弾で消費された効果（`consumption: NEXT_INCOMING_ATTACK`）が既に消えており「ターン開始時に何が配られたか」を表せない。`TurnStarted` イベント自身の `resolutionScopeId` に属する `EffectApplied` だけを拾って、配られた事実を行動フェーズの結果から切り離す。積み上がりのように最終状態で見るべき性質は、同じ観測が返す `battle` を使う。

この標準形は導入初日に production 定義の原文との不一致を3件検出した（`SKL_KOTOHA_REBEL_AS2` の憤怒0個が誤った腕へ落ちる／`SKL_SAYA_BUNNY_PS1` が会心しなかった攻撃でも発動する／`SKL_ELENA_MOODMAKER_EX` が高攻撃側へ追加ダメージバフを付けない）。いずれも `2026-08-08.1` で修正済み。

第2バッチ（`REF-026`／Issue #356）はさらに9件を検出し `2026-08-08.2` で修正した。内訳は (1) trigger の `targetSelector`/`sourceSelector` が実イベントの帰属と噛み合わず一度も候補化されない5件、(2) 「HPが多いほど高い効果」を絶対値Formula（`CURRENT_HP_RATIO`）で表していた3件、(3) 「与えたダメージの◯%」を最終ヒット分だけ読んでいた1件である。同時に、`DamageApplied` payload に `skillType` が無く「自身がアクティブスキルで攻撃された後」を表す trigger（`SKL_AOI_GUARDIAN_PS2` ほか計7スキル）が実戦闘で一度も発動しなかったDomain側の欠落も修正した（`SkillUseStarting` の同じ欠落と同じ対処）。

第3バッチ（`REF-027`／Issue #357）は8件を検出し `2026-08-08.3` で修正した。内訳は (1) 「自身がアクティブスキルで攻撃した後／する前に発動」を `targetSelector: SELF` で表していた6件（`SKL_EVIE_KYONSHI_PS2`・`SKL_YURIA_YUKATA_PS1`・`SKL_LYDIA_GENIUS_PS1`・`SKL_MIKOTO_SURVIVOR_PS2`・`SKL_MEIYA_FATED_PS2`・`SKL_MERU_FLATSPIN_PS1`）— `SkillUseStarting`/`SkillUseCompleted` は攻撃対象を `targetUnitIds` に持つため、第2バッチの帰属不一致と同じ理由で一度も候補化されない、(2) 「会心攻撃になるたびに」（`SKL_EVIE_KYONSHI_PS1`）に `result` 条件が無く非会心の判定でも毎回発動していた1件、(3) 「他の味方の」（`SKL_MIKOTO_SURVIVOR_PS1` の `TGT_OTHER_ALLIES`）が自身を除外していなかった1件である。

**(1) の6件は陣営（`targetSelector: ENEMY`）でも表せない。** 混乱（`R-CFS-01`）はASの`DAMAGE` stepが指すTargetBindingの`side`を反転させ、`SkillUseStarting`/`SkillUseCompleted.targetUnitIds` にも反転後の味方が入るため、陣営で絞ると混乱中のAS攻撃を取りこぼす。原文はいずれも「自身がアクティブスキルで攻撃する前／後」であって攻撃先の陣営を限定していないので、`targetSelector: ANY` と `EVENT_PAYLOAD.skillDefinitionId` の**攻撃を含むASのIDリスト**で判定する。`skillType: AS` だけでは `SKL_YURIA_YUKATA_AS2`（味方バフのみ）のような非攻撃ASまで拾ってしまうため、種別ではなくID列挙にする。各ユニットのテストは通常の攻撃と混乱中の攻撃の両方を成立側に、非攻撃AS／EXを不成立側に持つ。

同バッチはDomain側の欠落も2件修正した。`APPLY_DAMAGE_MOD` のバフ／デバフ分類が `magnitude` の符号だけで決まり、被ダメージ補正（`direction: INCOMING`、production 43件）で意味が反転していた — 「デバフをすべて解除」（`ACT_MEIYA_FATED_PS1_REMOVE_DEBUFF`）が自分の被ダメージ減少バフを剥がし、「自身にデバフが付与された際に発動」するPSが自分への防御バフで発動していた（`UT-R-EFF-02-029`/`030`）。`ALIVE_UNIT_COUNT_SCALE`・`MARKER_COUNT_SCALE` の頭打ちも `Math.min` 固定で、減少側の定義（`ACT_LYDIA_GENIUS_PS1_SHIELD_BUFF`）では常に上限が選ばれ「数が多いほど高い効果」というスケールが効いていなかった（`UT-R-NUM-04-033`/`034`）。

第4バッチ（`REF-028`／Issue #358）は8件を検出し `2026-08-08.4` で修正した。内訳は (1) 「**他の**味方が〜した際に発動」を `sourceSelector: ALLY` で表していた3件（`SKL_CHIYURU_MAZE_PS1`／`PS2`・`SKL_HARRIET_SAGE_PS2`）、(2) 発生源・対象の帰属が実イベントと噛み合わず一度も候補化されない3件（`SKL_DOROTHEA_PIONEER_PS2` の `sourceSelector: ENEMY`＝倒した側を敵陣営に限定、`SKL_FLUTE_VAMPIRE_PS1` の `HitPointReduced`＋`sourceSelector: SELF`＝`sourceUnitId`は攻撃側なので自傷でしか成立しない、`SKL_JULIE_SNOW_PS2` の `SkillUseCompleted`＋`targetSelector: SELF`＝第3バッチ(1)と同型）、(3) `SKL_AOI_ELEGANT_AS2` の追加ダメージ分岐が恒真だった1件（自分が付けた「浮足」で条件が必ず成立する位置にBRANCHが置かれていた。付与をBRANCHの後ろへ回した）、(4) `SKL_FEE_BATH_AS1`「「ほてり」を2つ付与する」が1つしか付いていなかった1件（`APPLY_MARKER` の `stack.policy: ADD` は1回の適用で1しか増やさないため、stepへ同じEffectActionを2件並べた）である。

(1) のために `TriggerDefinition` の selector 語彙へ `OTHER_ALLY` を新設した（`14_Catalog定義スキーマ.md`・`R-PS-01`）。`ALLY` は所有者自身を含むため、「他の敏捷タイプの味方がASで攻撃する前」（`SKL_CHIYURU_MAZE_PS2`）は自分自身が敏捷タイプであるだけで**自分のAS使用のたびに**追加ダメージと毒を上乗せしていた。allowlistに残るユニットにも同じ原文が11件あり、各バッチで差し替える。

同バッチはDomain側の欠落も1件修正した。`UnitBeingAttacked` payload に `skillType` が無く、「自身がアクティブスキルで攻撃される直前に発動」を表す trigger（`SKL_CHIZURU_DOMESTIC_PS2`・`SKL_RAMI_UNYIELDING_PS2`）が実戦闘で一度も候補化されなかった（第2バッチが `DamageApplied`／`SkillUseStarting` で直したのと同型）。公開ログの JSON schema へも任意プロパティとして追加した。

第5バッチ（`REF-029`／Issue #359）は11件を検出し `2026-08-08.5` で修正した。内訳は (1) 「**他の**味方が〜した際に発動」を `ALLY` selector で表していた4件（`SKL_KARINA_DOWNER_PS1`・`SKL_LAURA_MOUNTAIN_PS3`・`SKL_LILY_SINGER_PS1`・`SKL_MERU_SIRIUS_PS1`。第4バッチが挙げた残り11件のうちの4件）、(2) 「自身がアクティブスキルを使用した後／するたびに」を `targetSelector: SELF` で表し一度も候補化されない2件（`SKL_LUCIE_COMPANION_PS1`・`SKL_MAIA_SALON_PS1`。第3バッチ(1)と同型で、`SkillUseStarting`/`SkillUseCompleted` の `targetUnitIds` は攻撃対象であって自身ではない）、(3) 「自身がアクティブスキルで攻撃する前」を `targetSelector: ENEMY` で表し、混乱でASの対象が味方へ反転した攻撃を取りこぼす1件（`SKL_LAURA_MOUNTAIN_PS1`。(2) と違い通常の攻撃では候補化されるため、陣営で絞っていること自体が取りこぼしの原因）、(4) 発生源の帰属が実イベントと噛み合わない1件（`SKL_MAIA_SALON_PS2` の `HitPointReduced`＋`sourceSelector: ENEMY`＝HPを削るのは通常こちら側なので味方の攻撃では成立しない。原文「「乱調」を所持している敵のHPが60%以下になった際」は削った側を限定していない）、(5) 「アクティブスキルで攻撃される」の `skillType` 条件が無く EX の攻撃でも発動していた1件（`SKL_KARINA_DOWNER_PS1`）、(6) 「最高30%低下させる…対象のHPが多いほど高い効果」を絶対値Formula（`CURRENT_HP_RATIO`）で表していた1件（`ACT_KARINA_DOWNER_PS2_DEBUFF`。第2バッチ(2)と同型で、実測値は割合ではなく -3000 だった）、(7) 4ヒット攻撃の「与えたダメージの100%分」を最終ヒット分（`LAST_DAMAGE_DEALT`）だけ読んでいた1件（`ACT_KATE_PALADIN_AS1_SELF_HEAL_100`。第2バッチ(3)と同型）である。

(3) の `SKL_LAURA_MOUNTAIN_PS1` は「自身がアクティブスキルで**攻撃する**前」なので、第3バッチと同じく `ENEMY` → `targetSelector: ANY` ＋ `EVENT_PAYLOAD.skillDefinitionId` の攻撃ASのIDリストへ差し替えた。(2) の `SKL_LUCIE_COMPANION_PS1`・`SKL_MAIA_SALON_PS1` は原文が「アクティブスキルを**使用**した後／するたびに」で攻撃を含意しないため、`SELF` → `ANY` としたうえで `skillType: AS` はそのまま残す（回復ASも契機に含める）。`ANY` は `SELF`・`ENEMY` のどちらへ戻しても落ちる形で固定する。

- `SKL_LAURA_MOUNTAIN_PS1`・`SKL_MAIA_SALON_PS1`: 通常の敵対象ASの行が `SELF` への回帰を、**実混乱経路**を通した行が `ENEMY` への回帰を捕まえる。後者は混乱（`R-CFS-01`）を保持した状態で実ASを使用させるもので、反転先を合成せず `resolveSkillOrder` に決めさせる — 反転後の候補には使用者自身が距離0で含まれ `order: DEFAULT` が距離順に並べるため、実際に発行される `SkillUseStarting.targetUnitIds` は**味方前列ではなく使用者自身**になる。混乱を付与するproduction定義は `ACT_OLGA_VETERAN_EX_CONFUSION` の1件だけで検証対象のスナップショットに載らないため、前提の `AppliedEffect` だけを同じ `statusDetails` の値で組む（`skill-behaviour.ts` の `confusionStatus`）。
- `SKL_LUCIE_COMPANION_PS1`: 成立行の契機は混乱ではなく、**通常の味方対象回復AS**（`SKL_LUCIE_COMPANION_AS2`、対象は最もHP割合の低い味方）である。`targetUnitIds` が自分自身でも敵でもない味方になるため、この1行だけで `SELF`・`ENEMY` どちらへの回帰も落ちる。

`SKL_LAURA_MOUNTAIN_PS1` の「このスキルに続く自身の攻撃での攻撃力を最高7%上昇させる」は、同じPSが確率で撃つ隣接攻撃（`ACT_LAURA_MOUNTAIN_PS1_ADJACENT_DAMAGE`）に `NEXT_OUTGOING_ATTACK` の消費点を取られる近似が残る。原文では隣接攻撃は「続く自身の攻撃」に付く追加攻撃だが、定義はPS側の攻撃として表しているため、ASへ届く前にバフが消費される。消費点をASへ寄せるには隣接攻撃をAS側の追加stepとして表す構造変更が要るため本バッチでは触れていない（`IT-UNIT-LAURA-MOUNTAIN-001` が現状の振る舞いを固定する）。

`SKL_JULIE_SNOW_PS2` の対象は「攻撃した敵単体」だが、`TRIGGER_TARGET` selector は `count` を取れないため（`SELECT` 専用）、**攻撃した敵全員**へ解決する近似が残る。単体へ絞る表現は selector 語彙の拡張を要するため本バッチでは触れていない。

#### `IT-CAP-*` の retire 基準

1. retire対象が検証するproduction定義すべてが、ユニット効果軸のテストで**同等以上のアサーション**を持つこと。
2. `rule-coverage.ts` に残る参照は代替ID（`IT-UNIT-*` 等）へ差し替えてから削除する（漏れは `UT-TRACEABILITY-005` が検出する）。
3. どの単一定義にも帰属しない機構は、**関係する全ユニットのファイルへ複製して移送する**（重複を受け入れる）。恒久残置は認めない。
4. retire は対応するバッチと**同一PR**で実施する。

`REF-024`（Issue #353）の第1バッチでは、単一ユニットへ閉じた3ファイル（`suiran-chaos-ps3-position-relation`・`suiran-chaos-ex-distribute`・`elena-moodmaker-healing-link`）を `IT-UNIT-SUIRAN-CHAOS-004`〜`009`／`IT-UNIT-ELENA-MOODMAKER-004`〜`006` へ移して retire した。

`REF-026`（Issue #356）の第2バッチでは `attack-damage-bonus`（`IT-CAP-ATTACK-DAMAGE-BONUS-PROD-001`〜`003`）を retire した。参照する production 定義は `UNIT_ELENA_MOODMAKER`（付与側）と `UNIT_AOI_GUARDIAN`（保持側）の2件だけで、本バッチで後者もユニット効果軸に載ったため基準1を満たす。付与そのものと `magnitude` は `IT-UNIT-ELENA-MOODMAKER-001` のEX行が持ち、**別のスキル使用で起きる加算**（切り捨て前ダメージへちょうど `magnitude` 分／付与時snapshotであること）は `IT-UNIT-ELENA-MOODMAKER-007`〜`008` へ移した。本バッチの他の機能軸ファイルは、いずれも allowlist に残るユニットの定義を併せて検証しているため基準1を満たさず残置する。

`REF-027`（Issue #357）の第3バッチでは `target-derived-area`（`IT-CAP-TARGET-DERIVED-AREA-PROD-001`）と `target-state-extended-field`（`IT-CAP-TARGET-STATE-FIELD-PROD-002`）を retire した。どちらも参照する production 定義は `UNIT_LUCIE_MAID` の1件だけで、本バッチでユニット効果軸に載ったため基準1を満たす。前者の `BINDING_DERIVED`+`ADJACENT_ORTHOGONAL`（binding解決だけを見ていた）は `IT-UNIT-LUCIE-MAID-004` が、後者の trigger scope `UNIT_TYPE`（候補検出だけを見ていた）は `IT-UNIT-LUCIE-MAID-005` が、どちらも `SKL_LUCIE_MAID_AS2`／`SKL_LUCIE_MAID_PS1` をスキル使用・実PS経路として通した上で引き継ぐ（列も行も違う敵には追加攻撃が届かないこと、物理型・敏捷型で発動しEN型で発動しないこと）。`rule-coverage.ts` の `R-TGT-09`・`R-PS-01` の参照はこの2件へ差し替えた。台帳の照合器（`UT-TRACEABILITY-005`）は静的テーブルを持たない `it.each` を実行対象と見なさないため、`-001` の振る舞い表そのものは台帳の参照先にできない。

`REF-028`（Issue #358）の第4バッチでは `marker-source-defeat`（`IT-MARKER-SOURCE-DEFEAT-PROD-001`〜`003`）・`sum-damage-result`（`IT-CAP-SUM-DAMAGE-PROD-001`／`002`）・`resource-capacity-mod`（`IT-CAP-RESOURCE-CAPACITY-MOD-PROD-001`〜`003`）・`charge-started`（`IT-CAT-PROD-014`）の4ファイルを retire した。参照する production 定義はそれぞれ `UNIT_AOI_ELEGANT`・`UNIT_FLUTE_VAMPIRE`（2件）・`UNIT_HARRIET_SAGE` の1件だけで、本バッチでユニット効果軸に載ったため基準1を満たす。「高揚」の付与者戦闘不能による解除とR-EFF-09カスケードは `IT-UNIT-AOI-ELEGANT-004` が、`SUM_DAMAGE_DEALT` の「合計であって最終ヒットではない」は `IT-UNIT-FLUTE-VAMPIRE-004` が、`MODIFY_RESOURCE_CAPACITY` の上限上昇・現在値非追随・ターン回復への反映は `IT-UNIT-FLUTE-VAMPIRE-005` が引き継ぐ。`charge-started` の候補検出は `IT-UNIT-HARRIET-SAGE-001` の3行（他の味方／自身／敵のチャージ開始）が実PS経路で通した上で引き継ぐ。`rule-coverage.ts` の `R-ACTN-03`・`R-EFF-09`・`R-EFF-10` の参照はこれらのIDへ差し替えた。

`REF-029`（Issue #359）の第5バッチでは `lucie-companion-heal`（`IT-CAP-HEAL-PROD-001`／`IT-CAP-CONTINUOUS-HEAL-PROD-001`）・`mao-committee-ps2-stealth`（`IT-CAP-STEALTH-PROD-001`／`002`）・`random-branch`（`IT-CAP-RANDOM-BRANCH-PROD-001`／`002`）の3ファイルを retire した。参照する production 定義はそれぞれ `UNIT_LUCIE_COMPANION`・`UNIT_MAO_COMMITTEE`・`UNIT_KATE_PALADIN` の1件だけで、本バッチでユニット効果軸に載ったため基準1を満たす。`HEAL_DISTRIBUTE` の「総量1つを均等配分する（各自が満額を受け取るのではない）」は `IT-UNIT-LUCIE-COMPANION-004` が、継続回復の「付与時点では回復しない」とACTION(2)期間・独立Reducer復元は `IT-UNIT-LUCIE-COMPANION-005` が、Stealthの対象逸らしと消費（リンクグループの巻き添え失効を含む）は `IT-UNIT-MAO-COMMITTEE-004` が、その付与payload・`SKILL_USE(3)`・独立Reducer復元は `IT-UNIT-MAO-COMMITTEE-005` が引き継ぐ。`WEIGHTED_ONE` の腕選択は `IT-UNIT-KATE-PALADIN-001` の3行が**実経路で3腕すべてを実行**したうえで（retire 元は `selectWeightedBranch` を直接呼ぶ近似だった）、`RandomBranchSelected` の記録とStateDelta復元を `IT-UNIT-KATE-PALADIN-004` が引き継ぐ。`rule-coverage.ts` の `R-HEAL-01`・`R-HEAL-03`・`R-TGT-08`・`R-SKL-07` の参照はこれらのIDへ差し替えた。

`REF-030`（Issue #360）の第6バッチでは `resolution-branch-repeat`（`IT-CAP-BRANCH-REPEAT-PROD-001`〜`004`）を retire した。参照する production 定義は `UNIT_NADYA_SUCCESSOR` の1件だけで、本バッチでユニット効果軸に載ったため基準1を満たす。BRANCH の `EffectStepStarting` 発行と独立Reducer復元は `IT-UNIT-NADYA-SUCCESSOR-004` が引き継ぐ。同バッチは raw原文と実挙動の食い違い5件を `2026-08-09.1` で是正した（内訳は当該PRの記録を参照）。

`REF-031`（Issue #361）の第7バッチ（**ユニット最終**）では `dynamic-duration-on-reapply`（`IT-CAP-DYNAMIC-DURATION-PROD-001`〜`004`）・`tarisa-troublemaker-ps1`（`IT-CAP-TRIGGER-PAYLOAD-RES-PROD-001`〜`003`）・`stat-mod-stack-limit`（`IT-CAP-STAT-MOD-STACK-LIMIT-PROD-001`〜`003`）・`tatiana-sage-ex-omen-branch`（`IT-CAP-TATIANA-OMEN-PROD-001`〜`005`）の4ファイルを retire した。参照する production 定義はそれぞれ `UNIT_SIENA_DIVA`・`UNIT_TARISA_TROUBLEMAKER`（2件）・`UNIT_TATIANA_SAGE` の1件だけで、本バッチでユニット効果軸に載ったため基準1を満たす。`duration.reapply` の1行動→2行動上書きと `StunDurationChanged`・境界（残り2は上書きしない）は `IT-UNIT-SIENA-DIVA-004`／`005` が、`stacking.max: 14` の頭打ちと独立Reducer復元は `IT-UNIT-TARISA-TROUBLEMAKER-004`／`005` が、`stepCondition: EVENT_PAYLOAD calculatedDamage` の条件付きstepは `IT-UNIT-TARISA-TROUBLEMAKER-006` が、AOEの対象別振り分け・因果木・stateVersion・与ダメージ100%減少の消費とR-DMG-02の最低1ダメージ・StateDelta復元は `IT-UNIT-TATIANA-SAGE-004`〜`006` が引き継ぐ。`rule-coverage.ts` の `R-SKL-06`・`R-DMG-02`・`R-DMG-04`・`R-EFF-05`・`R-EFF-07`・`R-EFF-12` の参照はこれらのIDへ差し替えた。**本バッチで `UNCOVERED_UNIT_IDS` が空になり、Unit 69件すべてがユニット効果軸に載った**（Memory 32件は別バッチ）。

第7バッチはハーネス側の観測欠落も3件塞いだ。(1) サブユニット追加ダメージに付随するデバフ（R-SUB-02第3項）は `grantSubUnitAdditionalDamageDebuff` フックから直接付与され `EffectActionCompleted` を持たないため、実行ベース網羅（`-003`）が「実際に適用された」ことを数えられなかった → `EffectApplied` も実行の実績として数える。(2) `REMOVE_MARKER` が剥がしたMarkerは観測に一切現れなかった（`markers` は増加分だけの差分）→ 保持そのものが無くなったMarkerを `markersRemoved` として載せる。これで「解除する」を宣言する既存5ユニット・6行（`UNIT_AOI_ELEGANT` 2行、`UNIT_FEE_BATH`・`UNIT_HARRIET_SAGE`・`UNIT_MAIA_SALON`・`UNIT_NADYA_SUCCESSOR` 各1行）が初めて解除を固定する。段数が動いただけ（`UNIT_RAVEL_MODEL` の「ターゲット」3→4など）は保持そのものが残るため `markersRemoved` へは入れず、従来どおり `markers` が変化後の段数で表す。(3) 検証対象自身の属性が既定の `AGGRESSIVE` 固定で「自身を含む◯◯属性の味方に」を観測できなかった → `BoardOverrides.subject.attribute` で行ごとに実定義の属性を置けるようにした（盤面全体を実属性へ寄せると属性相性倍率が全ダメージ観測へ混ざるため、opt-in に留める）。

同バッチはDomain側の欠落も1件修正した。`resolution.kind: CHARGE` のスキルはチャージ経路が `SkillUseStarting`/`SkillUseCompleted` を一切発行しないため、「自身がアクティブスキルで攻撃した後に発動」を表すPS（`SKL_SIENA_OFFSTAGE_PS2`）がチャージ解放からは一度も成立しなかった。`ChargeReleaseCompleted`（`08_ドメインイベント.md`「チャージイベント」）を新設し、「チャージ効果発動」#4（チャージ状態の終了）の後に発行する — #1（`ChargeReleased`）ではPSの与ダメージバフが解放攻撃自身へ乗り、#2〜#3の間は「チャージ中は自身のパッシブスキルが使用できない」（R-SKL-05）でそもそも候補化されない。`IT-UNIT-SIENA-OFFSTAGE-004` が発行順（攻撃3件の確定 → 契機 → PSの付与）とチャージ状態終了後であることを固定する。

完了/中断の分岐とチャージ終了差分の所有者は `action-charge-resolver.test.ts` が押さえる。(a) チャージ終了の `StateDelta` は `ChargeReleaseCompleted`/`ChargeReleaseInterrupted` が所有し `ActionCompleting` へ持ち越さない（公開差分を契機イベントまで当て直した時点で既にチャージが終わっていること・二重適用しないことを固定）、(b) 使用者が自身の解放効果で戦闘不能になった解放は `ChargeReleaseInterrupted` になり、`ChargeReleaseCompleted` を契機に持つPSは候補化されない（AS/EX経路の `SkillUseCompleted`/`SkillUseInterrupted` と同じ Issue #217 設計方針B）、(c) 解放中にactorがSTUNを受けた場合は `ChargeCancelled` が既にcharge削除を所有しているため終了イベントは削除差分を持たず、全 StateDelta の独立Reducer replay が `before` 不一致で落ちない。

第7バッチが検出した raw原文と実挙動の食い違いは4件で、うち3件を `2026-08-09.3` で是正した。

- `SKL_SHIRANA_LUCKY_PS1`（原文「自身のHPが50%以下になった際」）の `HitPointReduced`＋`sourceSelector: SELF`。`REF-030` の `SKL_RAMI_UNYIELDING_PS1` と同型で、HPを削るのは通常敵であるため被弾では成立しなかった。`ANY` へ直した。
- `SKL_SIENA_OFFSTAGE_PS2`（原文「自身がアクティブスキルで攻撃した後に発動」）の `targetSelector: SELF`。第3バッチ(1)と同型で一度も候補化されない。陣営で絞ると混乱（R-CFS-01）で反転した攻撃を取りこぼすため、`ANY` ＋ `EVENT_PAYLOAD.skillDefinitionId` の攻撃ASのIDリストにした。
- `SKL_STELLA_STATUE_PS1`（原文「「惑光」を所持している敵のHPが50%以下になった際」）の `sourceSelector: ENEMY`。`REF-029` の `SKL_MAIA_SALON_PS2` と同型で、HPを削るのはこちら側なので味方の攻撃では成立しなかった。`ANY` へ直した。
- `SKL_SHIRANA_SORA_PS2`（原文「自身がENタイプの敵から攻撃される直前に発動」）の `EVENT_PAYLOAD field: "damageType"`。`UnitBeingAttacked` payload にその欄は無く、trigger は一度も成立しない。原文は**攻撃側のユニット種別**を指すので `TARGET_STATE target: TRIGGER_SOURCE / field: UNIT_TYPE`（`SKL_LUCIE_MAID_PS1` と同じ形）が正しいが、そう直すとEN同士のミラー戦でPS2が毎ヒット候補化し、1解決スコープの効果解決数が実行ガード（`maxEffectsPerScope: 50`、`passive-activation-service.ts`）を超えて**当のユニットが戦闘を完走できなくなる**（クールタイム1行動では抑えきれない）。ガードの上限はM9「実行保護の全上限を設定可能にする」まで固定値のため、**是正はその後段へ送り**、`IT-UNIT-SHIRANA-SORA-001` に実挙動（発動しない）の行として記録した。`ACT_SHIRANA_SORA_PS2_GUARD` は理由付きで実行ベース網羅から除外している。

`REF-032`（Issue #362）の第8バッチ（**Memory 第2バッチ**）では Memory 13件をユニット効果軸へ載せ、`memory-affiliation-dynamic-production-catalog.test.ts` のうち単一Memoryへ閉じていた5件（`IT-CAP-MEMORY-DYNAMIC-PROD-002`〜`005`／`-007`）を retire した。参照する production 定義はそれぞれ `MEM_FUUKI_IINKAI`・`MEM_INCOGNITO_SISTER_ADVENTURE`・`MEM_CURIOUS_EQUIPMENT`・`MEM_DISCONTENT_AND_ANXIETY`・`MEM_ALWAYS_PICO_BESIDE_YOU` の1件だけで、本バッチでユニット効果軸に載ったため基準1を満たす。所属メンバー限定と対象0件のskipは `IT-MEM-FUUKI-IINKAI-001`／`004` が、所属フィルタとROLEフィルタが互いに素な集合を選ぶことは `IT-MEM-INCOGNITO-SISTER-ADVENTURE-001` が、`side: "ENEMY"` の相対解決は `IT-MEM-CURIOUS-EQUIPMENT-001`／`002` が、`TurnStarted` の毎ターン再付与と積み上がりは `IT-MEM-DISCONTENT-AND-ANXIETY-004`／`005` が、Memory由来Markerの `sourceSide`・「1行動の間」の期間所有者・独立Reducer復元は `IT-MEM-ALWAYS-PICO-BESIDE-YOU-004`／`005` が引き継ぐ。`rule-coverage.ts` の `R-MEM-01`・`R-MEM-03`・`R-MEM-04`・`R-EFF-10` の参照はこれらのIDへ差し替えた。同ファイルに残る3件（`-001`・`-006`・`-008`）はいずれも複数Memoryを跨ぐ検証で、まだ allowlist に残る Memory の定義も併せて参照しているため基準1を満たさず残置する。本バッチ完了時点で Memory 32件中20件がユニット効果軸に載り、`UNCOVERED_MEMORY_IDS` は12件になった。

第8バッチはハーネス側も2点広げた。(1) `MemoryBoardOverrides.affiliationsBySlot` を新設し、所属条件Memoryが固定盤面上で実際に発現できるようにした（従来は所属なし6スロットのため対象0件だった）。(2) `observeMemoryTurnStarts` を新設し、`TurnStarted` 発動の `triggeredEffect` をターン開始の解決スコープへ閉じて観測できるようにした。併せて実行ベース網羅（`-003`）の集合を、ユニット効果軸と同じく `EffectActionCompleted.resultKind === "APPLIED"` だけに絞った（従来は結果種別を問わず数えていた）。

`REF-033`（Issue #363）の第9バッチ（**Memory 最終**）では残る Memory 12件をユニット効果軸へ載せ、**`UNCOVERED_MEMORY_IDS` が空になった**（Unit 69件・Memory 32件の全定義がユニット効果軸に載った）。同バッチは raw原文と実挙動の食い違いを1件も検出していない — 12件はいずれも `BattleStarted` 発動の静的補正2件だけで構成され、既存バッチが是正した trigger 帰属・Formula 表現・期間表現のいずれの類型にも当たらないためである。

第9バッチはハーネスへ `MemoryBoardOverrides.attributesBySlot` を追加した。`ATTRIBUTE` TargetFilter は `UnitDefinition` ではなく編成時に決まる `BattleUnit.attribute` を読むため、所属と同じく「そのMemoryの対象属性を名乗るスロット」を宣言できないと `MEM_THREE_MAIDS_HOSPITALITY`（`ATTRIBUTE` を使う唯一のMemory）が固定盤面で一度も発現しない。盤面全体を実属性へ寄せると属性相性倍率が観測へ混ざるため opt-in に留める。

同バッチは retire を2件行った。(1) `IT-CAP-MEMORY-STATIC-PROD-001` は `MEM_PANTS_STRAY_CAT` の1件だけを参照し、`UNIT_TYPE` と `ROLE` が入れ子になった集合の当たり外れを `IT-MEM-PANTS-STRAY-CAT-001` が実効値付きで引き継ぐ。(2) `IT-CAP-MEMORY-DYNAMIC-PROD-001` は6 Memory を**1件ずつ独立に**編成して見るループで、`IT-MEM-CHAOS-MAIDEN-001`・`IT-MEM-COLORFUL-BOUQUET-001`・`IT-MEM-PYXIS-MA-SOEUR-001`・`IT-MEM-SIRIUS-SUGAR-001`・`IT-MEM-TREBLE-QUINTET-001`・`IT-MEM-TRINITY-JEWEL-001` が同じ「所属メンバーだけ+250／全体へ陣営バフ／敵陣営は無傷」を付与先スロットと `sourceSide` まで含めて引き継ぐ（`IT-MEM-CHAOS-MAIDEN-001` には retire に合わせて実効値の行を足した）。`rule-coverage.ts` の `R-MEM-01` の参照はこれらのIDへ差し替えた。

残置した `IT-CAP-MEMORY-STATIC-PROD-005`・`IT-CAP-MEMORY-DYNAMIC-PROD-006`（複数Memoryを**同時に**編成したときのR-MEM-02跨Memory解決順とStateDelta復元）・`IT-CAP-MEMORY-STATIC-PROD-006`・`IT-CAP-MEMORY-DYNAMIC-PROD-008`（変換台帳としての宣言一致）・`UT-PROD-MEM-003`（同上の跨Memory解決順）は、いずれも単一定義へ帰属しない性質であり基準1では判定できない（`REF-034`／Issue #381 が下記のとおり処理した）。`IT-CAP-MEMORY-STATIC-PROD-002`〜`004` は第8バッチで軸へ載った Memory の単一検証で基準1を満たすが、本バッチの対象定義ではない。これらの最終処理は `REF-025`（Issue #354）が担った。

`REF-025`（Issue #354）は仕上げとして、allowlistが空であることを確認したうえで4件を retire した。`IT-CAP-MEMORY-STATIC-PROD-002`〜`004`（第9バッチからの申し送り分）は `IT-MEM-CHAOS-MAIDEN-TWINTAIL-FEST-001`・`IT-MEM-MOMOZONO-NEW-YEAR-001`・`IT-MEM-EURO-TOWER-DAY-001` が、付与先スロットと効果量を `toEqual` の完全一致で引き継ぐ。残る3ファイル（`stat-mod`／`marker`／`cooldown-manipulation`）は、**期待値をproduction payloadから再計算しているだけ**で独立した期待値を1つも持たない — `APPLY_STAT_MOD` は `before + magnitude`／`before × (1 + magnitude)` を、`APPLY_MARKER` は payload の `markerId` と stack policy を、`COOLDOWN_MANIPULATION` は payload の `targetSkillDefinitionId` を、いずれもテスト側で定義から組み立て直して突き合わせていた。この形が保証できるのは executor の配線だけで、ルール自体は `UT-R-STA-02`〜`04`・`UT-R-EFF-10-*`・`UT-R-SKL-09-*` が持ち、実データでの到達性はユニット効果軸の `-003`（実行ベース網羅監査）が持つ。ユニット効果軸はさらに強く、`IT-UNIT-AOI-ELEGANT-001` はEXの実使用でクールタイムのリセット（`remaining: 0`）と、リセット対象がクールタイム中でないときの `resultKind: "SKIPPED"` の両方を1つの `toEqual` で固定する。

`rule-coverage.ts` の参照は、Memory 3件を上記の `IT-MEM-*-001` へ差し替えた。`IT-MARKER-PROD-001`／`002`（`R-EFF-10`）・`IT-COOLDOWN-MANIP-PROD-001`（`R-SKL-09`）は差し替え先を持たずに削除した — ユニット効果軸で同じ機構を通しているのは `-001` の振る舞い表だが、台帳の照合器（`UT-TRACEABILITY-005`）は静的テーブルを持たない `it.each` を実行対象と見なさないため参照先にできない。両ルールとも `UT-R-*` 側の証跡（`R-EFF-10` は34件、`R-SKL-09` は `UT-COOLDOWN-*` ほか）を保っている。

同PRはCapability廃止（`REF-023`／Issue #352）の残骸も2件片付けた。`memory-affiliation-dynamic` の `MemoryExpectation.gatedBy`（20件すべて `[]` で、どのアサーションからも読まれていなかった）を削除し、`IT-CAP-MEMORY-DYNAMIC-PROD-008`・`IT-CAP-MEMORY-STATIC-PROD-006` のテスト名から、存在しなくなった Capability preflight への言及を外した。

`REF-034`（Issue #381）は機能軸移送の第1バッチとして、Memory 系に残っていた3ファイル7 `it()`（`memory-triggered-effects` の `UT-PROD-MEM-001`〜`003`、`memory-static-stat-mod` の `IT-CAP-MEMORY-STATIC-PROD-005`／`006`、`memory-affiliation-dynamic` の `IT-CAP-MEMORY-DYNAMIC-PROD-006`／`008`）をすべて retire し、`production-catalog/` 直下から Memory 系ファイルを無くした。7件は性質が2つに分かれ、扱いも分けている。

1. **単一Memoryへ帰属する検証**（`UT-PROD-MEM-001`／`002`）は既存の `IT-MEM-HARD-WARMUP-001`・`IT-MEM-TIMID-REINDEER-EVE-002` が同等以上を持っており、追加は要らなかった（前者は行別ATTACKバフを付与先スロットと効果量まで、後者は敵陣営宣言時のミラーと `sourceSide` を固定している）。
2. **変換台帳としての宣言一致**（`IT-CAP-MEMORY-STATIC-PROD-006`／`IT-CAP-MEMORY-DYNAMIC-PROD-008`）は、`-001` の観測が「どのスロットへ・どの効果量で・どの `sourceSide` で」までしか載せておらず、**1回の発現では観測に出ない宣言**だけが被覆されていなかった。標準形どおり観測側へ載せる方針を採り、`MemoryGrant` へ `statMod`（`stat`／`valueType`）・`damageMod`（`direction`／`damageType`）・`timeLimit`・`consumption` を、`MemoryMarkerGrant` へ `stackMax` を追加した。`timeLimit` は「戦闘終了まで（`{ unit: "BATTLE", count: 1 }`）」を既定として**キーごと落とし**、逸脱（`MEM_SOOTHING_SCENT`／`MEM_ALWAYS_PICO_BESIDE_YOU` の `ACTION` 期間、`MEM_OJOSAMA_MANNERS` の `owner` 省略、`MEM_BUSY_DAY_SLUMBER` の期限なし＋`NEXT_INCOMING_ATTACK` 消費）だけが表に現れる。`toEqual` の完全一致なので、既定から外れた瞬間にキーが現れて落ちる。これで全32 Memory・66 EffectAction の宣言が `-001`／`-002` に載り、`RATIO 0.025` と `FIXED 0.025`、「与えるダメージ」（`damageType: null`）と「物理攻撃で与えるダメージ」（`PHYSICAL`）の取り違えが効果量だけでは区別できない問題も解消した。

   **対象集合の宣言も同じ理由で観測へ載せる**（`MemoryObservation.targetSelections`）。当たったスロット（`unitIds`）は「対象となる陣営**全員**」を保証しない — 固定盤面で一致対象が1体だけになる行は66付与のうち24件あり（`ROLE: SUPPORT`以外のロール絞り込み・所属条件・属性条件はどれも1スロットしか引かない）、`count: "ALL"` が `count: 1` へ退行しても `unitIds` は変わらない。同じ理由で `filters` も観測へ載せる — 一致対象が1体だけの行は**別の絞り込みへ差し替えても同じ1体を引く**（`MEM_ABSOLUTE_ORDER` の `ROLE: PHYSICAL_ATTACKER` を `POSITION_ROW: FRONT`＋`POSITION_COLUMN: LEFT` にしても固定盤面では同じ `ally:FRONT_LEFT` が当たる）ため、原文の「物理アタッカーの味方**全員**」という契約の退行が `unitIds` に現れない。`triggeredEffect` ごとの `targetBindings` の `kind`／`side`／`count`／`filters` を定義順に並べた表を `-001` が `toEqual` で固定し、binding が増減した場合も行数が変わって落ちる。空の `filters` は「陣営全体から絞り込まない」という宣言そのものとして表に残す。

   **EffectActionの適用順**（`MemoryObservation.actionOrder`）も同様に載せる。`grants` はEffectAction ID順、`markers` は別配列、`executedActionIds` は集合なので、どれも適用順を表さない。しかしR-MEM-04が委譲するR-SKL-06 #4は同じACTION stepの `actions` を定義順に適用する契約で、入れ替わると `EffectApplied`／`MarkerApplied` の発行順が変わり、各イベントを契機にするPS・Memory連鎖の観測が変わり得る（`MEM_ALWAYS_PICO_BESIDE_YOU#0` の「攻撃力バフ → 三ツ星Marker」）。宣言ではなく**実発行順**（`EffectActionCompleted` の順）を観測するため、定義順どおりに適用されていること自体もここで固定される。`EffectActionCompleted` は対象1体につき1件発行され、対象が複数あるstepでは対象ごとに `actions` を一巡するため、観測はEffectActionごとの初回位置だけを残す（何体へ当たったかは `grants.unitIds` が持つ）。

3. **跨Memory解決順（R-MEM-02）とStateDelta復元**（`UT-PROD-MEM-003`／`IT-CAP-MEMORY-STATIC-PROD-005`／`IT-CAP-MEMORY-DYNAMIC-PROD-006`）はどの単一定義にも帰属しないため、retire基準3に従い**関係する13 Memory のファイルへ複製した**（`IT-MEM-HARD-WARMUP-004`・`IT-MEM-HEART-COLOR-004`・`IT-MEM-STRANGERS-004`・`IT-MEM-PANTS-STRAY-CAT-004`・`IT-MEM-CHAOS-MAIDEN-TWINTAIL-FEST-004`・`IT-MEM-MOMOZONO-NEW-YEAR-004`・`IT-MEM-EURO-TOWER-DAY-004`・`IT-MEM-CHAOS-MAIDEN-005`・`IT-MEM-TREBLE-QUINTET-005`・`IT-MEM-FUUKI-IINKAI-005`・`IT-MEM-INCOGNITO-SISTER-ADVENTURE-005`・`IT-MEM-DISCONTENT-AND-ANXIETY-006`・`IT-MEM-CURIOUS-EQUIPMENT-004`）。1件あたりの形は「自分と他Memoryを同時に宣言したとき、API指定順 → 同一Memory内定義順 → ALLY候補の後にENEMY候補、で解決される」＋「宣言順を入れ替えると解決順も入れ替わる」＋「同じスロットへ両Memoryの補正が重なった実効値」＋「開始前スナップショットへStateDeltaだけを当てると開始後状態を復元できる」で、ハーネスに `observeCoDeclaredMemories`（陣営ごとのMemory ID列を受ける観測）を新設して支えた。実効値の行は `RATIO` が加算で合成されること（`IT-MEM-EURO-TOWER-DAY-004` の `1000 × 1.065 + 1250`）まで固定する。

`rule-coverage.ts` の `R-MEM-01`〜`R-MEM-04` の参照はこれらのIDへ差し替えた。Memory の `-001` は `it.each` ではなく plain `it` なので、ユニット側と違って台帳の参照先にできる。

`REF-036`（Issue #383）は機能軸移送の第3バッチとして、発動条件・step条件の3ファイル17 `it()`（`action-activation-condition` の `IT-CAP-ACTION-ACTIVATION-CONDITION-001`〜`005`、`passive-activation-condition` の `IT-CAP-PASSIVE-001`〜`007`、`effect-step-condition` の `IT-CAP-EFFSTEP-001`〜`005`）をすべて retire した。3ファイルとも「条件が成立する盤面と成立しない盤面を作り分ける」という同じ形で、ユニット効果軸の `-001` は**その作り分けを行ごとに持つ表そのもの**である（AS/EXは `selectAsCandidate`／`isExUsable`、PSは `PassiveActivationRuntime` を実際に通す）。したがって大半は基準1で `-001` が引き継ぎ、`-001` では表せない3点だけを `-004` 以降へ書いた。

1. **`-001` が引き継いだもの**。`activationCondition` の成立／不成立は `IT-UNIT-LILY-HERO-001`（HP20%未満）・`IT-UNIT-MAO-COMMITTEE-001`（HP60%未満）・`IT-UNIT-TATIANA-SAGE-001`（「深慮」所持）・`IT-UNIT-ELENA-MOODMAKER-001`（自身HP40%未満／HP70%未満の味方なし）・`IT-UNIT-LYDIA-GENIUS-001`（右列・左列に敵なし）が、PS側は `IT-UNIT-KOKORO-SPORTSDAY-001`（「ストイック」）・`IT-UNIT-LAURA-MOUNTAIN-001`／`IT-UNIT-MAO-COMMITTEE-001`（`ALIVE_UNIT_COUNT`）・`IT-UNIT-LUCIE-MAID-001`（1ターン目）・`IT-UNIT-MERU-SIRIUS-001`（奇数ターン）・`IT-UNIT-RAMI-NEWYEAR-001`（HP50%以下）・`IT-UNIT-TATIANA-SAGE-001`（「深慮」所持）が、いずれも成立行では**実行結果まで** `toEqual` で固定したうえで持つ。step条件の対象別振り分けは `IT-UNIT-ROSIE-ARTIST-001` が物理型・EN型を混在させた1回のPS2発動で相補条件の両側（`×2` と基本値）を固定しており、これも追加が要らなかった。候補検出（R-PS-01）と発動直前再確認（R-PS-04）の2点評価は `PassiveActivationRuntime` が内部で両方を呼ぶため `-001` の実経路に含まれ、ルール自体は `UT-R-PS-01-*`／`UT-R-PS-04-*` が合成定義で押さえている。
2. **行動選択層の「次候補への送り」**（`R-ACT-02`）は `-001` に現れない。`observeSkillUse` はスキル1件だけを行動選択へ渡すため、条件不成立は常に `activated: false`（＝`WAIT`）にしか見えず、**宣言順の次のASへ送られること**を表せない（retire元も同じ形で、`WAIT` しか見ていなかった）。ハーネスへ `selectedActiveSkill`（実 `UnitDefinition` が宣言する順のASを丸ごと `selectAsCandidate` へ通し、選ばれたスキルIDを返す観測）を新設し、`IT-UNIT-ELENA-MOODMAKER-009`・`IT-UNIT-LYDIA-GENIUS-004`・`IT-UNIT-LILY-HERO-004`・`IT-UNIT-MAO-COMMITTEE-006`・`IT-UNIT-TATIANA-SAGE-007` が「成立時はそのASが選ばれ、不成立時は候補から外れて次のASが選ばれる」を1件ずつ固定する。しきい値は境界（`GTE 0.6` の6000／5999、`LT 0.2` の2000／1999）で挟む。`SKL_TATIANA_SAGE_AS2` は宣言順が先頭ではないため、AS1をクールタイム中（R-ACT-02の別条件）に置いてAS2から評価させる。なお `ELENA`／`LYDIA` の `TARGET_SET_COUNT` はどちらもそのスキル自身のbindingを見るため、集合が空になる不成立はR-TGT-01 #4（空bindingは常に発動不能）とも重なる — retire元と同じく、条件評価パイプラインがそこでスローせず候補除外として扱われることの証跡として残した。
3. **同じ対象集合の中での対象ごとの分かれ方**（`R-SKL-06`）も `-001` の既存行には無かった。`IT-UNIT-LUCIE-MAID-001` は物理型だけ／EN型だけの均質な盤面を別々の行として持つため、**混在した集合で対象ごとに評価される**ことが差として現れない。`IT-UNIT-AOI-ELEGANT-005`（同じ前後列に「浮足」所持・未所持を混ぜ、攻撃は両方・攻撃力低下は所持側だけ）・`IT-UNIT-LUCIE-MAID-006`／`007`（AS1の気絶とPS2のPP削りを物理型・EN型が混ざる列で）・`IT-UNIT-TATIANA-SAGE-008`（「凶兆」0個・1個・2個・3個を1回の使用へ混ぜ、しきい値未満の1個が付与側へ落ちる境界）を追加した。
4. **PS発動の実ライフサイクル**（`IT-CAP-PASSIVE-002` だけが持っていた `PassiveActivated` のPP収支・StateDelta復元・R-PS-07）は `IT-UNIT-LAURA-MOUNTAIN-004` へ移した。`-001` はPP差分を持つが、`PassiveActivated` payload の `ppBefore`／`ppAfter`、開始前スナップショットへ公開差分だけを当てる独立Reducer復元、解決スコープが変われば同じPSが再び発動することは持っていない。

`rule-coverage.ts` の `R-ACT-02`・`R-SKL-06` の参照はこれらのIDへ差し替えた。ユニットの `-001` は `it.each` で台帳の参照先にできないため、参照先はすべて `-004` 以降の plain `it` である。移送の過程で raw原文と実挙動の食い違いは検出されなかった（**是正0件**）。本バッチ後の残数は26ファイル・118 `it()`。

`REF-037`（Issue #384）は機能軸移送の第4バッチとして、対象解決の2ファイル15 `it()`（`target-filter-order` の `IT-CAP-TARGET-FILTER-ORDER-PROD-001`〜`006`、`target-effect-query` の `IT-CAP-TARGET-EFFECT-QUERY-PROD-001`〜`009`）をすべて retire した。2ファイルとも「production の `targetBindings`／`filters`／`order`／`area` を通し、当たる相手と当たらない相手を固定する」「照会条件を満たす対象にだけ条件付きの効果が適用される」形で、これは `-001` 振る舞い表の必須観点2そのものである。したがって大半は基準1で `-001` が引き継ぎ、`-001` では表せない4点だけを `-004` 以降へ書いた。

1. **`-001` が引き継いだもの**。`MARKER_COUNT ASC` 順は `IT-UNIT-DOROTHEA-PIONEER-001`（「気品」2個の敵を飛ばして未所持の敵を選ぶ）、`EXCLUDE_RESOLVED_UNIT` は `IT-UNIT-MIHIME-SNIPER-001`（`LOWEST_HP_RATIO` が2つのbindingで同じ敵を指すのに2体目が選ばれる）、`UNIT_TYPE_PRIORITY`+`SELF_LOWEST_PRIORITY` は `IT-UNIT-SHIRANA-SORA-001`（ENタイプの味方が自身より先に選ばれる）が、いずれも実行結果まで `toEqual` で固定したうえで持つ。効果照会側では `IT-UNIT-CHIYURU-MAZE-001`（毒）・`IT-UNIT-FLUTE-INFLUENCER-001`（デバフ有無で回復量がちょうど2倍・解除の有無）・`IT-UNIT-MAIA-LAZY-001`（バフ有無でシールドの有無）・`IT-UNIT-NOEL-RUMBLE-001`（炎上有無で威力162.24／243.36の腕）・`IT-UNIT-NANAE-COMMANDER-001`（自身が炎上のとき自己デバフ解除）が成立側と不成立側を1行ずつ持つ。
2. **基準敵そのもの**（`R-TGT-09`／`R-TGT-10`）は `-001` に現れない。`SKL_CLARA_SANTA_AS2` の `TGT_BASE` はどのACTION stepの対象でもなく `TGT_COLUMN`（`SAME_COLUMN_AS_BASE`）を導くためだけに解決されるので、振る舞い表からは列全体しか見えず、基準がタグ保持者なのか同じ列の未所持の敵なのかを区別できない。`IT-UNIT-CLARA-SANTA-004` が実 `resolveBindingSelections`（`resolveSkillUse` と同じ関数）で binding 単位に見て、「`MARKER_IN_AREA` は候補自身の列を見るため基準はタグ保持者とは限らない」と「タグを持つ敵が居なければ非空filtersが候補0件になり `fallback` が基準を供給する」を固定する。`SKL_LYDIA_GENIUS_EX` は両bindingがACTION stepの対象になるため振る舞い表から見えるが、台帳の参照先を plain `it` にする必要から `IT-UNIT-LYDIA-GENIUS-005` へ、左右両列を持つ盤面（`OR(POSITION_COLUMN)` が中央列を外す）と左右に敵が居ない盤面（`fallback` が最も近い敵を供給する）の2件として置いた。
3. **状態異常の総称照会の両端**（`R-SKL-06`／`R-STS-01`）は `-001` の既存行には無かった。`IT-UNIT-CHIYURU-MAZE-001` と `IT-UNIT-MERU-FLATSPIN-001` はどちらも「状態異常を持つ／何も持たない」の2行しか持たず、**状態異常ではない単なるデバフ**が不成立であること（`DEBUFF` への近似ではないこと）が差として現れない。`IT-UNIT-CHIYURU-MAZE-004`・`IT-UNIT-MERU-FLATSPIN-004` を追加した。`SKL_SHOUKA_SCHEMER_AS3` の `statKinds: [ATTACK]` も同じ穴で、防御力デバフを持つ対象で腕が切り替わらないことを `IT-UNIT-SHOUKA-SCHEMER-004` が持つ（不成立側の防御力デバフは実 production 定義 `ACT_CHIYURU_MAZE_AS1_DEF_DOWN` で作る）。

   **総称照会の成立側は、R-STS-01が定める2系統（`APPLY_CONTINUOUS_DAMAGE` の炎上・毒と `APPLY_STATUS` の気絶・凍結・暗闇）を同じ1つのスキルへ通して初めて固定される。** 別々のスキルで片系統ずつ確認しても、当のスキルのCatalog条件を `continuousDamageKinds`／`statusKinds` で片方へ絞る誤変更は検出できない。`IT-UNIT-CHIYURU-MAZE-004` は `SKL_CHIYURU_MAZE_EX` に対して毒と気絶の両方を、`IT-UNIT-MERU-FLATSPIN-004` は `SKL_MERU_FLATSPIN_AS2` に対して同じく両方を通す。自身が片系統しか配らないユニットでは、もう片方を実 production 定義で作れるよう供給元ユニットだけをsnapshotへ併読する（ちゆるは気絶を配らないため `UNIT_LUCIE_MAID` を読む）。

4. **Domain Event・StateDelta・独立Reducer復元と、分岐が一度だけ確定すること**は観測の外にある。`IT-UNIT-CHIYURU-MAZE-004` は使用前を基準線にして `stateDelta` だけから再構成した最終状態が集約側と一致することまで見る。`IT-UNIT-NOEL-RUMBLE-004` は `onFactEventForPassiveChain` で「強化ダメージが着弾した瞬間にPS連鎖が炎上を解除する」状況を作り、単一 `BRANCH` では通常版が走らないことを固定する — retire元は連鎖が実際に炎上を剥がしたことを見ていなかったためフック未呼び出しでも通ってしまう形だったので、剥がれた事実の確認を足した。

`rule-coverage.ts` の `R-TGT-09`・`R-TGT-10`・`R-SKL-06`・`R-EFF-02`・`R-STS-01` の参照はこれらのIDへ差し替えた。移送の過程で raw原文と実挙動の食い違いは検出されなかった（**是正0件**）。本バッチ後の残数は24ファイル・103 `it()`。

なお `SCN-BTL-*`（シナリオ層）は従来どおり production 定義IDを参照してはならない。

[`15_Unit_Memory変換台帳.md`](./15_Unit_Memory変換台帳.md)の全不完全変換行は、[`17_残作業対応表.json`](./17_残作業対応表.json)で解消責任を持つTaskへ割り当て、対応する production-catalog テストで近似なしの実行結果を確認してから台帳の該当行を除去する（`COOLDOWN_MANIPULATION`／Issue #129と同じ運用）。Issue #143により`RUNTIME_COUNTER_MODULO`（9行）・`CUMULATIVE_DAMAGE_THRESHOLD_TRIGGER`（3行）の計12行は解消済み。`TRIGGER_POSITION_RELATION`（1行）・`TRIGGER_EXCLUSION_TIMING`（3行）はCondition APIをIssue #144で実装済みだが、イベント発行と`TRIGGER_TARGET`/`TRIGGER_SOURCE`解決を含むproduction完走を`RES-005`（Issue #172）が担当する。`RESOURCE_GAIN_MOD`（2行）は`M7-002`（Issue #185）が担当する。`TARGET_STATE_QUERY_BUFF_DEBUFF`（5行）は`M7-001E`（Issue #248）が`TARGET_HAS_EFFECT`／`TARGET_STATE.field: HAS_STATUS`で解消し、`IT-CAP-TARGET-STATE-FIELD-PROD-002`と、`REF-037`（Issue #384）でユニット効果軸へ移送した5スキルの証跡（`IT-UNIT-CHIYURU-MAZE-001`／`IT-UNIT-FLUTE-INFLUENCER-001`／`IT-UNIT-MAIA-LAZY-001`／`IT-UNIT-NOEL-RUMBLE-001`の振る舞い表と`IT-UNIT-SHOUKA-SCHEMER-004`）で近似なしの実行結果を確認した。`AOE_PER_TARGET_CONDITION`（1行）は`RES-004-STATUS-CONDITION`（Issue #224）が`STATUS`カテゴリへ炎上・毒を含める分類統一で解消し、`IT-UNIT-CHIYURU-MAZE-004`が`SKL_CHIYURU_MAZE_EX`のAOE対象別適用（Domain Event・StateDelta・独立Reducer復元を含む）を、`IT-UNIT-MERU-FLATSPIN-004`・`IT-UNIT-NANAE-COMMANDER-001`が同じ総称照会へ寄せた`SKL_MERU_FLATSPIN_AS2`・`SKL_NANAE_COMMANDER_PS1`を確認した。同Issueは`HAS_STATUS`の総称利用を解消したため`IT-CAP-TARGET-STATE-FIELD-PROD-001`を撤去し、`HAS_STATUS`自体の証跡はUnitテスト（`UT-R-SKL-06-064`/`065`）へ残した。

対象が保持する効果を照会する条件（`TARGET_HAS_EFFECT`／`HAS_STATUS`）のテストは、条件evaluatorが**評価時点の最新`BattleUnit`だけ**を読む契約を固定するため、次を必ず含める: 成立・不成立に加えて、(1) 絞り込み（`continuousDamageKinds`／`statKinds`）が別種別・別statへ一致しないこと、(2) 複数対象が混在する集合で対象ごとに判定が分かれること、(3) 直前に効果が解除・失効した対象では成立しなくなること、(4) step全体スコープ（BRANCH）で対象が0体なら不成立・2体以上なら例外になること。

### 必要なテスト種別

各ルールについて、適用可能な種別を選ぶ。

| ルール性質 | 必須テスト                                  |
| ---------- | ------------------------------------------- |
| 値域・上限 | 下限、上限、下限未満、上限超過              |
| 優先順     | 各優先キーが異なる例、完全同値の扱い        |
| 計算式     | 基本例、0、負方向補正、端数、最終丸め       |
| 発動条件   | 成立、不成立、成立後に再確認で不成立        |
| リソース   | 丁度消費、不足、0、最大値超過               |
| 期間       | 付与スコープ、次回、残1、残0、複数個別期間  |
| 連鎖       | 子なし、1段、複数段、循環候補、発動済み除外 |
| 状態遷移   | 正常遷移、不許可遷移、完了後更新            |
| エラー     | code、対象path／ruleId、Battle未開始        |

## ルール領域別方針

| 領域   | ルール数 | 主なテストレベル         | 重点境界・不変条件                                                                |
| ------ | -------: | ------------------------ | --------------------------------------------------------------------------------- |
| `NUM`  |        4 | Unit／Property           | 割合表現、最終切り捨て、確率0・100、Formula評価                                   |
| `FRM`  |        5 | Unit／Application        | 1・5体、0・6体、配置重複、同一ユニット、1・99ターン                               |
| `POS`  |        3 | Unit／Property           | 全12座標、前方向、マンハッタン距離の対称性                                        |
| `BON`  |        3 | Unit／Table              | 各役、コミカル候補比較、クレバー段階累積                                          |
| `STA`  |        4 | Unit／Scenario           | 補正順、重複あり総和、重複なし最強、失効後再計算                                  |
| `ORD`  |        4 | Unit／Property／Scenario | 速度、陣営、前列、左列、予約保持、未行動者のみ再計算                              |
| `ACT`  |        4 | Unit／Scenario           | チャージ優先、AS探索、AP／EX待機、PP消費によるEX増加、変更イベント                |
| `TGT`  |       10 | Unit／Property           | 近い・遠い・隣接・正面・列・不足・ステルス代替、binding                           |
| `SKL`  |        9 | Unit／Scenario           | 効果順、対象順、ヒット順、中断、クールタイム、他スキルCT操作、チャージ2行動、分岐 |
| `PS`   |        8 | Unit／Scenario           | 柔軟なイベント照合、先制攻撃、速度順、再確認、即時ネスト、1回制限                 |
| `MEM`  |        4 | Unit／Scenario           | Memory候補順、BattleStarted、PSとの順序、source side                              |
| `ACTN` |        3 | Unit／Scenario           | EffectAction共通、即時効果、継続状態付与                                          |
| `HIT`  |        3 | Unit                     | 通常命中、特別回避、複数暗闇の定義順判定                                          |
| `CRT`  |        2 | Unit／Boundary           | 会心率補正、会心あり・なし倍率                                                    |
| `ATR`  |        2 | Unit／Table              | 全属性組み合わせ、最終倍率                                                        |
| `DMG`  |        5 | Unit／Property           | 攻防差0、最低1、無効時1、最終切り捨て、防御貫通、補正、イベント順                 |
| `HEAL` |        4 | Unit／Scenario／Boundary | 即時回復、回復量補正、継続回復、回復リンク（転送・自己リンク恒等・再リンク禁止）  |
| `SHD`  |        3 | Unit／Property           | 同タイプ加算、タイプあり→なし→サブユニット→HP、保存則                             |
| `SUB`  |        2 | Unit／Scenario           | 耐久吸収、追加効果、特殊減衰式                                                    |
| `INT`  |        3 | Unit／Scenario           | 防御介入順、肩代わり、反射                                                        |
| `LNK`  |        3 | Unit／Scenario           | 全対象同量、対象ごとのシールド、再リンク禁止                                      |
| `DOT`  |        4 | Unit／Scenario           | 付与時攻撃力、固定、通常炎上、3回時各炎上2倍、毒上限                              |
| `STS`  |        4 | Unit／Scenario           | 状態異常分類、気絶、凍結、暗闇                                                    |
| `EFF`  |       11 | Unit／Scenario／Property | 解除・無効、期間、個別保持、次点繰り上げ、Marker、RuntimeCounter                  |
| `END`  |        2 | Unit／Scenario           | 1行動後、同時全滅、敵全滅、味方全滅、ターン上限優先順                             |

ルール数の合計は110件とする。設計書のルール追加時にはこの表と台帳を更新する。

## 決定的なテスト

### SequenceRandomSource

テスト用 `RandomSource` は、指定した値を順に返す。

```text
SequenceRandomSource {
  values: number[]
  consumedValues: number[]
}
```

規則：

- 値はDomainのRandomSource契約範囲内とする。
- 値が不足した場合は即時にテストを失敗させる。
- 余った値も原則としてテスト失敗にし、想定外に乱数消費が減ったことを検出する。
- 呼び出し回数と順序をassertできるようにする。
- 値不足時にランタイム乱数へfallbackしない。

確率境界では「判定値が閾値の直前」「閾値と同値」「直後」をテーブル化する。会心率ステータスが0未満または100超でも、判定時の実効値だけが0～100へ補正されることを確認する。

### ID生成器

`BattleId`、`ActionId`、`SkillUseId`、`EffectInstanceId` などを予測可能に生成するテスト実装を使用する。

```text
battle-1
action-1
skill-use-1
effect-1
```

ID文字列そのものを検証目的にしないテストでは、IDの一意性と参照整合性だけをassertする。

### Clockと期限

実時間のsleepで期限テストを行わない。手動で進められるClockまたは期限判定fakeを使用する。

- 期限直前は継続する。
- 期限到達で `EXECUTION_TIMEOUT` になる。
- タイムアウトを `TURN_LIMIT_REACHED` へ変換しない。
- HTTPの強制キャンセルはWorker結合テストで別に扱う。

## テストデータ設計

### Builder

次のBuilderを用意する。

- `UnitDefinitionBuilder`
- `SkillDefinitionBuilder`
- `EffectDefinitionBuilder`
- `MemoryDefinitionBuilder`
- `FormationInputBuilder`
- `BattleScenarioBuilder`
- `CatalogBuilder`

Builderの既定値は「最小で妥当、特殊効果なし」とし、テスト対象以外の要因を減らす。ただし重要な既定値を隠さないよう、生成結果を読みやすいfixture名で表す。

例：

```text
unit({ hp: 1000, attack: 200, defense: 100, actionSpeed: 50 })
activeSkill({ apCost: 1, effects: [physicalDamage({ power: 100 })] })
```

### テストCatalog

用途別に分ける。

| Catalog            | 用途                                   |
| ------------------ | -------------------------------------- |
| `minimal`          | 1対1の最小戦闘と単体シナリオ           |
| `scenarios`        | PS、チャージ、状態異常など複合シナリオ |
| `invalid`          | 参照切れ、重複ID、不正期間             |
| production Catalog | 実データ全体の構造・参照検証           |

ドメインルールテストをproduction Catalogの具体的なユニットへ依存させない。production定義の変更でルールテストの意図が変わることを防ぐ。

### 数値fixture

- 整数化前の端数が分かる値を使う。
- 0、1、最大値、最大値直前・直後を明示する。
- 浮動小数の途中値は必要に応じて許容誤差で比較する。
- 最終ダメージ、HP、AP、PP、EX、期間など整数値は完全一致で比較する。
- テスト期待値へproductionの計算関数を呼び出さない。

期待値の式をテストへ実装コードと同じ形で複製しすぎない。代表的な手計算例と、別形式の不変条件テストを組み合わせる。

## Domain Unitテスト

### 値オブジェクト

- 境界内の生成成功
- 境界外の生成失敗
- 等価性
- 不変性
- JSONやprimitiveへの変換境界

### 比較・選択ポリシー

行動順、PS順、ターゲット順は比較キーごとに独立したテーブルテストを作る。

```text
行動速度 desc
→ 陣営 ALLY before ENEMY
→ 前列 before 後列
→ column asc
```

同じ入力集合を並べ替えても結果順が同じになることをPropertyテストで確認する。入力配列順を暗黙のtie-breakerにしない。

### 計算サービス

- 各補正を一つずつ変えた例
- 複数補正を組み合わせた例
- 途中計算を丸めない例
- 最終値だけ切り捨てる例
- 最低値・上限値を適用する例
- 適用対象外の補正が混ざらない例

## Battle Scenarioテスト

### シナリオHarness

```text
runScenario({
  catalog,
  command,
  randomValues,
  expectedRules
}) -> SimulateBattleResult
```

Harnessは実際のFormation、Battle、Observationを使用し、HTTPとWorkerだけを通さない。次をまとめてassertできるようにする。

- 勝敗と終了理由
- 最終状態
- 特定イベントの順序とpayload
- 行動ID・SkillUse ID・親子関係
- 状態バージョンと差分
- RandomSource消費回数
- ルールIDの関連づけ

### 基準シナリオ

| ID            | シナリオ                   | 重点確認                                                                   |
| ------------- | -------------------------- | -------------------------------------------------------------------------- |
| `SCN-BTL-001` | 1対1の基本戦闘             | 開始、AS、ダメージ、勝敗、完了                                             |
| `SCN-BTL-002` | 全員同速                   | 味方→敵、前列→後列、左列順                                                 |
| `SCN-BTL-003` | キュー途中の速度変化       | 未行動者だけ再計算し予約種別維持                                           |
| `SCN-BTL-004` | キュー決定後にEX満タン     | 現キューはAS、次キューはEX                                                 |
| `SCN-BTL-005` | AP0・EX満タン・行動不能    | EX全量消費と待機                                                           |
| `SCN-BTL-006` | AS候補の順次判定           | CT、AP不足、条件不成立、対象不在を飛ばす                                   |
| `SCN-BTL-007` | PSからPSへの多段連鎖       | 新規候補優先、親復帰、発動済み除外                                         |
| `SCN-BTL-008` | PSのPP消費                 | 消費PPと同量のEX増加、最大超過切り捨て                                     |
| `SCN-BTL-009` | スキル途中で使用者戦闘不能 | 未解決効果の中断                                                           |
| `SCN-BTL-010` | チャージ開始と発動         | 2行動、気絶キャンセル、凍結保持                                            |
| `SCN-BTL-011` | 行動単位効果期間           | 付与行動を数えず対象自身の次回から減算                                     |
| `SCN-BTL-012` | ターン単位効果期間         | 付与ターンを数えず次ターンから減算                                         |
| `SCN-BTL-013` | 重複あり効果               | 個別期間と個別失効                                                         |
| `SCN-BTL-014` | 重複なし効果               | 個別保持、最強失効後の次点繰り上げ                                         |
| `SCN-BTL-015` | シールドとサブユニット     | 物理／EN→無属性→サブユニット→HP                                            |
| `SCN-BTL-016` | リンクダメージ             | 全対象同量、個別吸収、再リンクなし                                         |
| `SCN-BTL-017` | 継続ダメージ               | 付与時攻撃力のsnapshotを継続使用                                           |
| `SCN-BTL-018` | 気絶・凍結・暗闇           | 各固有処理と行動・命中への影響                                             |
| `SCN-BTL-019` | 同時全滅                   | 味方勝利と `SIMULTANEOUS_DEFEAT`                                           |
| `SCN-BTL-020` | 最終ターン境界             | 途中敵全滅は勝利、生存終了は敗北                                           |
| `SCN-BTL-021` | 状態履歴復元               | 初期状態＋全差分＝最終状態                                                 |
| `SCN-BTL-023` | 戦闘不能予約除去へのPS連鎖 | 同サイクル予約除去起点のPS連鎖でも親子・決定性・差分再適用の不変条件が成立 |

各シナリオを巨大な一つのテストにまとめず、主要な分岐ごとにケースを分ける。

**コード実在シナリオとの差分**: `SCN-BTL-004`・`005`・`009`〜`014` の8件と `SCN-BTL-018` はテストコード側に未実装である。これらの実装は `REL-003`（Issue #200、M9）のスコープであり、同Issueの主要シナリオ全経路検証で追加する。それ以外の14件（`001`〜`003`・`006`〜`008`・`015`〜`017`・`019`〜`021`・`023`）はコード側に実在する。`SCN-BTL-022`（未実装Capabilityの拒否）はREF-023（Issue #352）のCapability廃止で退役し、IDは欠番のままとする。

## PS連鎖テスト

PSは循環と順序の不具合が起きやすいため、専用のテスト群を持つ。

### 候補順

- 行動速度が異なる所有者
- 同速の味方と敵
- 同陣営同速の前列・後列
- 同じ列条件で絶対左列
- 同じユニットの複数PS定義順
- 先制攻撃候補を通常候補より優先し、先制攻撃同士へ全tie-breakerを適用すること
- 同時発動制限による1件選択

### 再確認

候補検出後、先行PSによって次を変化させる。

- 所有者が戦闘不能になる。
- PP不足になる。
- クールタイムが設定される。
- 発動条件が不成立になる。
- チャージ状態になる。

発動直前確認で候補が除外され、適切な診断理由を観測できることを確認する。

### RuntimeCounter条件（M6最小範囲・SkillRuntimeスコープ）

`R-EFF-11`のうち`SkillRuntime`スコープをM6で検証する。`Battle`／`BattleUnit`スコープは利用するproduction定義が存在しないためFeature Complete必須対象に含めず、Catalogロード時点で明示的に拒否する。`AppliedEffect`スコープは下記「RuntimeCounter条件（EFF-005・AppliedEffectスコープ）」で検証する。

- N回ごと発動（`modulo`）の直前・一致・次周期境界（Issue #143）
- 累計ダメージ閾値の直前・一致・超過、1回の被ダメージで複数閾値を超える場合、端数繰越（Issue #143）
- Counter更新イベントとPS候補検出の順序（Counter更新 → 候補抽出）。`RuntimeCounterChanged`のsequenceが原因イベントより大きく、かつ原因イベント自身の候補抽出より前に発行されること（[08\_ドメインイベント.md](./08_ドメインイベント.md)「複合処理と状態差分の所有」の例外）。`RuntimeCounterChanged`がcounterの`stateDelta`を単独で所有し、原因イベント自身の`stateDelta`にcounter変化を含まないこと
- `RuntimeCounterReset`が`RuntimeCounterChanged`のpre-matching例外に含まれず、PS/Memory候補スタック全体が空になった後（解決スコープ終了時）にのみ発行され、そのスコープの`rootEventId`を`parentEventId`とすること。reset分の`stateDelta`を単独で所有すること
- `TriggerDefinition.eventType: RuntimeCounterChanged`／`RuntimeCounterReset`を持つPS/MemoryがCatalog検証を通過し、それぞれの発行直後に候補化・解決されること（原因イベント自身の候補抽出より先に`RuntimeCounterChanged`由来の候補が解決を終えること、`RuntimeCounterReset`由来の新規候補が生じた場合に解決スコープの終了を遅延すること）
- `RuntimeCounterReset`が、counterを破棄し`stateVersion`を進めた後に発行されること（FACTイベントは状態変更確定後に発行する一般規則どおりで、`RuntimeCounterChanged`のpre-matching例外とは順序が異なること）。`RuntimeCounterReset`由来の候補解決が同じスコープへcounterを再生成・更新した場合、そのcounterが残らなくなるまで破棄・発行・候補解決を繰り返してから解決スコープを終了すること

Issue #143実装メモ: 上記のうち`modulo`境界・累計ダメージ閾値の直前/一致/超過・複数閾値/端数繰越・Counter更新→候補抽出の順序（`RuntimeCounterChanged`のpre-matching例外含む）はUnit/Scenarioテスト（`runtime-counter-state.test.ts`／`runtime-counter-matcher.test.ts`／`trigger-condition-evaluator.test.ts`／`passive-activation-service.test.ts`のUT-R-EFF-11-001）で検証済み。`value`不変でも`carry`が変化した更新は`RuntimeCounterChanged`を発行する（`runtime-counter-matcher.test.ts`のUT-RCOUNTER-M-006）。

対象12行はいずれも戦闘終了までcounterを保持する設計（`resetScope`省略）だが、`resetScope: RESOLUTION_SCOPE`自体は実装・配線済み。`RuntimeCounterReset`のイベント種別・payload・`StateDelta`復元（`state-delta-reducer.test.ts`のUT-STATE-REDUCER-021〜026、`after: undefined`によるキー削除を含む）、pure関数（`resetRuntimeCounter`）に加え、「解決スコープ終了時にリセットする」呼び出し元（`PassiveActivationRuntime.finalizeResolutionScope`、discard→発行→候補解決の反復ループ）を`battle.ts`（TurnStarted）／`action-skill-use-resolver.ts`（AS/EX）／`action-charge-resolver.ts`（チャージ解放）の3つの解決スコープ終了処理全てへ配線し、`ActionCompleting`／Cooldown更新／`ActionCompleted`の発行後（`06_戦闘状態遷移.md`のCOMPLETING順序どおり）に呼び出すことを`passive-activation-service.test.ts`のUT-R-EFF-11-002で検証した。自身の`RuntimeCounterReset`を再生契機にする誤ったCatalog定義に対する反復回数上限は、UT-R-EFF-11-003（`finalizeResolutionScope`が決定的なエラーで停止すること）で検証した。`Battle`／`BattleUnit`スコープは利用するproduction Catalog定義が現れるまでCatalogロード時に拒否する（`UT-CAT-RCU-011`）。

### RuntimeCounter条件（EFF-005・AppliedEffectスコープ）

`R-EFF-11`の`AppliedEffect`スコープ（EFF-005、Issue #162）を検証する。利用するproduction Catalog定義は存在しないため、明示的Scenario（Issue #162本文が許容する代替受け入れ条件）として検証した。`EffectSequence`スコープ（EFF-006、Issue #212）は次節「RuntimeCounter条件（EFF-006・EffectSequenceスコープ）」で検証する — `13_実装計画.md`のM7完了条件が要求する両スコープが揃い、`R-EFF-11`はルールCoverage台帳（`rule-coverage.ts`）上で完了した。

- Catalogスキーマ: `DurationDefinition.counterUpdates`（`scope: APPLIED_EFFECT`のみ許可、他スコープ拒否）のマッピング・バリデーション、`expiration.conditions`の`RUNTIME_COUNTER`参照が同じ`counterUpdates`に宣言されていることの検証（`duration-definition.test.ts`のUT-CAT-DUR-015〜020）。`RUNTIME_COUNTER_SCOPES`への`APPLIED_EFFECT`/`EFFECT_SEQUENCE`追加と、`APPLIED_EFFECT`だけを受理・`EFFECT_SEQUENCE`は`BATTLE`/`BATTLE_UNIT`と同じくCatalogロード時に拒否すること（`runtime-counter-update-definition.test.ts`のUT-CAT-RCU-011/015）
- Catalog整合性: `APPLY_MARKER.duration.counterUpdates`の明示的拒否（Marker自身のconsumption/expiration機構が未実装のため）、`APPLY_STAT_MOD`等duration保持kindの`duration.counterUpdates`の受理（`catalog-integrity.test.ts`のUT-CAT-IDX-038・040）
- ドメインモデル: `EffectDurationState.counters`が`counterUpdates`を持つ場合だけ空マップから始まること（`applied-effect.test.ts`のUT-R-EFF-11-005/006）
- Condition評価: `RUNTIME_COUNTER` Conditionが`context.effectCounters`（`AppliedEffect`自身のcounter）から値を読み、`skillDefinitionId`のSkillRuntimeスコープと分離されていること、両方が渡された場合は`effectCounters`を優先すること（`trigger-condition-evaluator.test.ts`のUT-R-EFF-11-007〜010）。R-EFF-08（`expiration.conditions`）評価が`effect.duration.counters`を`effectCounters`として渡し、効果インスタンスごとに独立していること（`effect-expiration-condition-service.test.ts`のUT-R-EFF-11-011〜013）
- マッチング・更新: 効果インスタンス自身の`counterUpdates`検出（defeated holderのスキップ、インスタンスごとの独立マッチング含む）、`INCREMENT`/`CUMULATIVE_DAMAGE_THRESHOLD`適用（累計ダメージ閾値・carryのみの変化の追跡を含む）、非`APPLIED_EFFECT`スコープが紛れ込んだ場合の防御的拒否（`runtime-counter-effect-matcher.test.ts`のUT-RCOUNTER-EFF-001〜010）
- 実ライフサイクル配線: `PassiveActivationRuntime.onFactEvent`のトップレベル呼び出しが原因イベント直後・R-EFF-08評価前にAppliedEffectスコープのcounterを更新し、`RuntimeCounterChanged`（`scope: APPLIED_EFFECT`、`effectInstanceId`、`skillDefinitionId`を持たない）を発行すること、更新後のcounterがR-EFF-08の`expiration.conditions`を成立させ効果インスタンスを失効させること、`RuntimeCounterChanged`の`stateDelta`（`effects[instanceId]`の完全なbefore/afterスナップショット差し替え、`counters`を含む）が独立Reducer（`applyStateDelta`）で正しく復元されること（`passive-activation-service.test.ts`のUT-R-EFF-11-014〜016）
- PS連鎖内部での実ライフサイクル配線: `resolvePassiveChain`（`resolve-passive-chain.ts`）へ`deps.applyEffectRuntimeCounterUpdates`として注入し、`onFactEvent`を経由しないPS連鎖内部イベント（PS自身がyieldする`PassiveActivated`・`EffectActionStarting`、PS効果由来の`DamageApplied`等）でもcounter更新・`RuntimeCounterChanged`発行・R-EFF-08評価が届くこと。同じトップレベルeventが`onFactEvent`と`resolvePassiveChain`の両経路から到達しても`processedEffectRuntimeCounterEventIds`ガードにより二重加算されないこと（`passive-activation-service.test.ts`のUT-R-EFF-11-014の`RuntimeCounterChanged`件数アサーション、およびPS自身のEffectSequenceが発行した`DamageApplied`から効果失効まで検証するUT-R-EFF-11-017、`RuntimeCounterChanged`の`skillUseId`がPSの原因イベントと一致することを検証するUT-R-EFF-11-017の追加アサーション）
- 自己再誘発の再帰depth上限: `RuntimeCounterChanged`自身をtriggerに再度`counterUpdates`が発火する自己再誘発定義に対し、`ChainState.effectRuntimeCounterDepth`（`resolve-passive-chain.ts`）がPS連鎖内の再帰的イベント解決全体を通して保持され、`PassiveChainLimits.maxEffectRuntimeCounterDepth`超過時に決定的な`ExecutionGuardExceededError`で停止すること（stack overflowにならないこと）を検証する（`passive-activation-service.test.ts`のUT-R-EFF-11-018）
- AppliedEffect counter変更の逐次解決: 同一イベントに複数のAppliedEffect counter更新がマッチする場合、`SkillRuntime`側のgeneratorと同様に1件ずつ`record()`直後にその候補連鎖を完了させてから次のmatched entryを適用すること。先行entryの候補解決による状態変化を後続entryが（stale値ではなく）観測できることを検証する（`passive-activation-service.test.ts`のUT-R-EFF-11-019）

### RuntimeCounter条件（EFF-006・EffectSequenceスコープ）

`R-EFF-11`の`EffectSequence`スコープ（EFF-006、Issue #212）を検証する。`EffectSequence`自身は状態を持たないため、実行時識別子には既存の`SkillUseId`（1回の解決を一意に識別する）を再利用し、`BattleUnit.effectSequenceCounters`（`SkillUseId`→`RuntimeCounter`の2段キー）を保持先とする。利用するproduction Catalog定義は存在しないため、`AppliedEffect`スコープと同じく明示的Scenarioとして検証した。

- Catalogスキーマ: `EffectSequence.counterUpdates`（`scope: EFFECT_SEQUENCE`のみ許可、他スコープ拒否）のマッピング・バリデーション（`effect-sequence.test.ts`のUT-CAT-SEQ-027〜030）。`RUNTIME_COUNTER_SCOPES`の`EFFECT_SEQUENCE`を受理し、`BATTLE`/`BATTLE_UNIT`だけを引き続き拒否すること（`runtime-counter-update-definition.test.ts`のUT-CAT-RCU-011/016）
- マッチング・更新: 呼び出し側が維持する「現在進行中の解決」レジストリ（`ActiveEffectSequenceResolution`、`SkillUseId`キー）からの検出（actorが戦闘不能・`units`から消失した場合のスキップ含む）、複数の同時進行中解決が互いに独立してマッチすること、`INCREMENT`適用、非`EFFECT_SEQUENCE`スコープが紛れ込んだ場合の防御的拒否（`effect-sequence-runtime-counter-matcher.test.ts`のUT-RCOUNTER-SEQ-001〜009）
- 実ライフサイクル配線: `PassiveActivationRuntime.beginEffectSequenceResolution`でこの解決の`counterUpdates`を登録し、`onFactEvent`のトップレベル呼び出しがcounterを更新して`RuntimeCounterChanged`（`scope: EFFECT_SEQUENCE`、`skillDefinitionId`を持つ。`SkillUseId`はイベントエンベロープの`skillUseId`が既に持つためpayloadには重複させない）を発行すること、`finalizeEffectSequenceResolution`が解決完了時に必ずcounterを破棄し`RuntimeCounterReset`を発行すること、両イベントの`stateDelta`（`effectSequenceCounters`/`effectSequenceCounterCarry`、`skillCounters`と同じ2段キー形式）が独立Reducer（`applyStateDelta`）で正しく復元されること（`passive-activation-service.test.ts`のUT-R-EFF-11-020/021、`state-delta-reducer.test.ts`のUT-STATE-REDUCER-029〜031）
- 自己再誘発の防止: `finalizeEffectSequenceResolutionSteps`が`this.activeEffectSequenceResolutions`からエントリを破棄・`RuntimeCounterReset`発行より先に削除するため、`RuntimeCounterReset`自身を再誘発契機にする誤ったCatalog定義があっても対象が見つからず無限ループが原理的に起こらないこと（`SkillRuntime`スコープの反復回数上限とは異なる安全策、`passive-activation-service.test.ts`のUT-R-EFF-11-022）
- PS連鎖内部での実ライフサイクル配線とPS/Memory候補解決パリティ: `resolvePassiveChain`（`resolve-passive-chain.ts`）へ`deps.applyEffectSequenceRuntimeCounterUpdates`として注入し、PS自身のEffectSequence解決（`activatePassiveCandidate`）が`beginEffectSequenceResolution`/`finalizeEffectSequenceResolutionSteps`を`yield*`委譲すること、`RuntimeCounterChanged`（`scope: EFFECT_SEQUENCE`）自身が`AppliedEffect`スコープと同じくPS/Memory候補として検出・発動されること（`RuntimeCounterChanged`をtriggerに持つ別PSが、親PSのEffectSequence解決がまだ進行中の状態で実際に発動する、R-PS-06「親A→子PS→親B」順序を含む）を検証する（`passive-activation-service.test.ts`のUT-R-EFF-11-023）
- AS/EX・チャージ解放からの実ライフサイクル配線: `action-skill-use-resolver.ts`（AS/EX、`resolveSkillUse`）と`action-charge-resolver.ts`（チャージ解放、`resolveChargeRelease`）が、それぞれの`skillUseId`採番直後に`beginEffectSequenceResolution`を、`applyEffectActionGroups`完了直後に`finalizeEffectSequenceResolution`を呼ぶことを、実際の生成関数を通した統合的なテストで検証する（`action-skill-use-resolver.test.ts`のUT-R-EFF-11-025、`action-charge-resolver.test.ts`のUT-R-EFF-11-024）

- 位置関係Trigger（目の前）のtable test、対象不在・戦闘不能時の候補破棄（Issue #144）
- Battle開始・Turn開始・Turn終了での発動除外と、同じeventTypeが通常解決中に発生した場合の候補化（Issue #144）
- nested PS連鎖でもroot/parent文脈を失わず除外判定できること（Issue #144）

### ネスト

```text
元イベント
├─ PS-A候補
├─ PS-C候補
└─ PS-A発動
   └─ 子イベント
      └─ PS-B発動
         └─ 完全解決
   └─ PS-Aの残効果
└─ PS-Cへ復帰
```

イベントの `sequence`、`parentEventId`、`rootEventId`、候補スタック順を検証する。同じ解決スコープでPS-Aを再候補化しても2回目は発動しない。

## 効果期間テスト

### 行動単位

- 付与された当該行動終了では減らない。
- 他ユニットの行動では減らない。
- 対象自身の次回行動終了で1減る。
- 残1から0になった直後に失効する。
- 待機も対象自身の行動終了として減算し、戦闘不能で予約を除去されて行動しなかった場合は減算しない。
- 同じ種別の複数インスタンスが別々の残回数を持つ。

### ターン単位

- 付与ターン終了では減らない。
- 次のターン終了から1減る。
- 残1から0になった直後に失効する。
- 同時に複数失効した場合のイベント順が決定的である。

### 重複なし

- 効果量が異なる複数インスタンスを保持する。
- 最大効果だけ `isEffective` になる。
- 最大効果失効後、次点が同じ終了処理内で有効になる。
- `EffectExpired` → `EffectiveEffectChanged` → `CombatStatChanged` の相対順を確認する。各イベントから誘発されたPSイベントの割り込みは許容し、隣接だけを要求しない。

### リソース獲得量Modifier（M7）

`RuntimeCounter`の`AppliedEffect`／`EffectSequence`スコープと同じM7効果期間モデルで検証する（`M7-002`／Issue #185）。

- EX獲得量+50%/-50%の単体テスト。
- 複数Modifier（増加と減少、同値効果）の重複／非重複合成。
- 行動単位Durationの適用開始・減算・失効境界。
- 基礎量確定 → Modifier適用 → capacity打ち止め → イベント発行、の順序。`ResourceChanged.baseDelta`（基礎量）と`delta`（Modifier適用後・capacity適用後の最終量）が両方とも観測できること。Modifier不在かつcapacity打ち止めなしの場合だけ両者が一致し、capacity打ち止めのみが発生する場合（Modifier不在）でも一致しないこと。
- capacity超過時のModifier適用後の破棄量と`ExtraGaugeOverflowDiscarded`payload（要求増加量がModifier適用後・capacity適用前の量と一致すること）。
- EffectActionを経由しない`EX_GAIN`（AP/PP消費起因）でも`baseDelta`/`delta`が観測できること。
- EXゲージが既に上限で正の`baseDelta`が全量破棄され`delta`が0になる境界。この場合`ResourceChanged`は発行されないが、`ExtraGaugeOverflowDiscarded`が`delta`が0でも発行され、payloadの`baseDelta`から基礎量を復元できること。

## イベントと状態差分

### イベント不変条件

- `sequence` が重複せず昇順である。
- 子イベントのsequenceが親より大きい。
- 同じ解決スコープでrootが一致する。
- 同じ行動・スキル解決でIDが一致する。
- 状態変更を所有するイベントが一つだけである。
- 内訳イベントに同じ差分を重複させない。
- TIMINGイベント後に親処理の前提を再検証する。
- DETAILEDからDIAGNOSTICを除いても状態履歴が変わらない。

### 独立した差分Reducer

テスト用に `StateTransitionResponse` を適用する小さなReducerを作る。productionのBattleObservationやResultAssemblerをそのまま呼び出さない。同じ不具合を期待値側でも再現することを防ぐ。

確認内容：

1. 初期 `stateVersion` が0。
2. 各差分でversionが1ずつ進む。
3. 前後versionが連続する。
4. `causedBySequence` が正規の内部イベント列に存在する主イベントを参照する。公開レベルでそのイベントが省略されることは許容する。
5. イベントの `stateTransitionIndex` が対応差分を指す。
6. 全差分適用後に最終状態と深い等価になる。
7. 差分を一件抜く、逆順にする、重複させると検証が失敗する。

### Snapshotの使用範囲

Snapshotを許容する対象：

- OpenAPI文書
- 小さく安定したイベント列の補助確認
- エラーレスポンスの代表例

Snapshotだけにしない対象：

- ダメージ計算
- 勝敗
- リソース消費
- PS順序
- 効果期間
- 状態差分復元

Snapshot更新時は仕様変更と単なる出力変化をレビューで区別する。

## Property／Modelテスト

ランダム生成にはテスト用seedを固定し、失敗時にseedと最小反例を記録する。再実行可能な形式でCIログへ残す。

### 主なProperty

- 任意の有効編成でHP、AP、PP、EX、シールド、期間が負数にならない。
- EXゲージがユニット固有最大値を超えない。
- 行動順比較器が反対称性、推移律、決定性を満たす。
- 入力配列順を変えても行動順結果が変わらない。
- 速度再計算でキューから行動済み者が復活しない。
- 速度再計算で予約済みAS／EXが変化しない。
- ターゲット距離が負にならず、マンハッタン距離の対称性を満たす。
- シールド吸収＋サブユニット吸収＋HPダメージが適用ダメージと一致する。
- リンク対象数を変えても各対象のリンクダメージ量が変わらない。
- 効果インスタンス追加・失効後も同一IDが重複しない。
- 初期状態へ全差分を適用すると最終状態になる。
- `COMPLETED` 後に状態が変化しない。

生成器はドメイン上有効な入力を基本とし、無効入力生成器は値オブジェクト・Command検証用に分ける。

## Applicationテスト

### Portのテストダブルと実行保護

実際に注入するPortは次で、テストダブルへ差し替える。

- `BattleCatalog`（Port）→ `CatalogBuilder`（`testing/scenario/catalog-builder.ts`）が生成する `TestBattleCatalog`。合成した定義グラフを返し `loadSnapshot` 呼び出し回数を記録する。UseCase単体テストで従来インライン定義していた `FakeBattleCatalog` はこれへ一本化する。
- `RandomSource`（Port）→ `SequenceRandomSource`（枯渇throw・余剰検出・消費列追跡）。
- `BattleIdGenerator`（Port）→ `FixedBattleIdGenerator`。
- `Clock`（Port）→ `ManualClock`。

一方、次は当初「Port」を想定していたが実アーキテクチャではPortではないため、テストダブルではなく実物＋決定的入力で検証する。

- **実行保護（ExecutionGuard）はPortではなくドメイン設定**。PS連鎖などの上限は `PassiveChainLimits`（`domain/battle/triggering/passive-chain-limits.ts`）とイベント上限で表現し、超過を `EXECUTION_LIMIT_EXCEEDED` として検証する。協調的な期限は `SimulationExecutionContext.deadlineEpochMs`＋`ManualClock` で与え、期限超過を `EXECUTION_TIMEOUT` として検証する（`ManualExecutionGuard` という差し替えPortは実装しない）。
- **Observationはポートではなく純関数** `buildBattleObservation`（`application/observation/battle-observation.ts`）。`InMemoryBattleObservation` という差し替えPortは存在せず、`SimulationResultAssembler` 経由で観測結果（イベント・状態差分）を検証する。

UseCaseの高速な単体テストではCatalogとClockを差し替える。Battleルール自体をmockして期待結果を捏造せず、正常系の主要ケースでは実Battleを使用する。

### 事前検証

- 複数のCommand違反を一度に収集する。
- Command違反時はCatalogやBattleを呼ばない。
- 不明なユニット・メモリーIDをpath付きで返す。
- 推移的参照切れを検出する。
- Catalogを戦闘中に再取得しない。
- 同一ユニット定義の参加枠へ別BattleUnitIdを付ける。

### エラー変換

- 既知の入力違反を `INVALID_COMMAND` にする。
- 期限切れを `EXECUTION_TIMEOUT` にする。
- 実行保護上限を `EXECUTION_LIMIT_EXCEEDED` にする。
- 内部不変条件違反を成功結果や敗北にしない。

## 未実装ルールのテスト

次をしてはならない。

- 仮の計算結果を期待値として固定する。
- 実際に分岐へ到達しなければ実行可能とする。
- production Catalogの説明文から未実装の挙動を推測する。

## Catalogテスト

### 構造

- manifestと各JSONがSchemaに適合する。
- ID、列挙値、整数・割合、期間が許容形式である。
- ファイルハッシュがmanifestと一致する。
- 未知プロパティを拒否する。

### 意味

- IDが種類内で一意である。
- すべての参照先が存在し期待する型である。
- 定義順が一意に決まる。
- PSトリガーイベントと条件式を解釈できる。
- 不許可の循環参照がない。

production Catalogの全定義を起動時検証と同じvalidatorで検査する。エラーfixtureでは一つの意図した違反だけを入れ、診断箇所までassertする。

## API契約テスト

### Catalog一覧

- 全Unit・Memoryをdefinition ID昇順で返す。
- Skill、EffectAction、Formula、Condition、triggeredEffectsの完全定義をresponseへ含めない。
- 200でCatalog revision由来ETagとCatalog用Cache-Controlを返す。
- ETag一致の `If-None-Match`で304、本文なしを返す。
- 一覧read modelをHTTP requestごとに再構築しない。

### リクエスト

- 1～5体、0～6メモリー、1～99ターンの境界
- 数値文字列、小数、`null`、未知プロパティの拒否
- 配置のcolumn・rowと共通座標変換
- 同一ユニット定義の重複許可
- Content-Type、Accept、本文上限
- `options` 省略時のDETAILED既定値

### レスポンス

- `schemaVersion`、`catalogRevision`、Battle ID
- 勝敗と終了理由の組み合わせ
- 初期・最終状態のスキーマ
- 公開イベントの連番と因果参照
- `stateTransitionIndex` と差分配列
- SUMMARYでも全状態差分を保持
- エラーのcode、path、ruleId、diagnosticId
- スタックトレースや内部パスがない
- `Cache-Control: no-store` とRequest ID
- Catalog一覧だけはETagと短いpublic cacheを返し、戦闘POSTの `no-store`と混同しない。

### CORS

- 許可したGitHub Pages originのCatalog GET、戦闘POST、preflightを受理する。
- 未許可originにCORS response headerを返さない。
- `Origin`なしの既存requestを拒否しない。
- browserから `X-Request-Id`、`Retry-After`、`ETag`を参照できる。
- 不正な `CORS_ALLOWED_ORIGINS`を起動時に拒否する。

### OpenAPI

- OpenAPI 3.0.3として検証できる。
- 全ルートと全ステータスにSchemaがある。
- 実際の代表レスポンスが生成Schemaへ適合する。
- 保存された文書を採用する場合は再生成差分がない。
- 互換性検査で意図しない破壊的変更がない。

#### 実装（REL-004 / Issue #203）

3.0.3適合は、生成文書のSchema Objectが仕様「Schema Object」の採用keyword部分集合に収まっているかを走査して確かめる（`testing/openapi/openapi-303-conformance.ts`）。後発のJSON Schema（`const`・`prefixItems`など、OpenAPI 3.1系）が混ざると厳格なclient generatorが落ちるため、`openapi: "3.0.3"`と宣言している以上ここを固定する。走査器自体が素通りしないことは`API-OPENAPI-021`が既知の不適合入力で確かめる。

互換性検査は、凍結した`apps/api/openapi/v1-baseline.json`と生成文書を比較し、`10_API設計.md`「バージョニング」が破壊的と定めるものだけを失敗させる（`testing/openapi/openapi-breaking-change.ts`）。後方互換な追加（任意プロパティ・新イベント種別・新エラーコード・新列挙値）は通るので、追加のたびのbaseline更新は要らない。文書全体のsnapshot比較にしないのはこのためで、毎回更新を強いると「意図的な追加」と「うっかりの破壊」がレビューで区別できなくなる。

比較は`paths`と`components.schemas`の両方を見る。`$ref`は解決せず（自己参照する`ConditionDefinition`で無限再帰になる）、参照先の中身はcomponentsを名前どうしで突き合わせ、`paths`側では参照先の名前が変わっていないかだけを見る。

`oneOf`のvariantは配列indexではなく判別子（`type`／`kind`の単一値`enum`）で対応付ける。判別子は一意とは限らないため（`ConditionDefinition`の`kind=TARGET_STATE`は`field`と`value`の型の組み合わせごとに複数variantある）、同じ判別子のグループ内ではまず内容が完全に一致するものを消し込み、残りだけを出現順で対応付ける。indexや出現順だけで対応付けると、variantが1つ挿入されただけで以降が全部ずれ、後方互換な追加を大量の破壊的変更として誤報する。

レスポンス側の`required`縮小（プロパティは残す）は失敗させない。「バージョニング」がこれを条件付き（「緩めた後も従来から公開されていた変種では常に存在することを併せて示す」）でしか後方互換と認めておらず、その条件は文書だけからは判定できないため、緩和する側が設計書へ根拠を書く責任を負う。

baselineの更新は意図的な操作として扱う。`apps/api/scripts/write-openapi-baseline.mjs`で再生成し（スクリプトがPrettierを通すので`format:check`は追加操作なしで通る）、その差分をレビュー対象にする（本節「Snapshotの使用範囲」の「仕様変更と単なる出力変化をレビューで区別する」と同じ扱い）。

## Worker・インフラテスト

### Worker境界

- DTOと結果が構造化クローンできる。
- HTTPオブジェクトやDomain Entityを渡さない。
- コンパイル済みESMワーカーをproductionと同じパスで起動できる。
- ワーカーごとにCatalogを一度だけ読み込む。
- 期待Catalogリビジョン不一致を拒否する。
- 複数リクエストのBattle状態が混ざらない。

### 容量と障害

- `concurrentTasksPerWorker = 1` を維持する。
- Pool満杯を `503 CAPACITY_EXCEEDED` にする。
- 利用者別上限を `429 RATE_LIMIT_EXCEEDED` にする。
- 協調期限を `504 EXECUTION_TIMEOUT` にする。
- クライアント切断で外側のタスクをキャンセルする。
- ワーカー異常終了後にタスクを自動再実行しない。
- 障害ワーカーが補充される。
- Graceful Shutdownの受付停止、待機、強制終了順を守る。

Worker結合テストは共有状態と実時間依存を避け、各テスト専用Poolを生成して必ずcloseする。

### Container・Cloud Run境界

- production containerをLinux amd64向けにbuildできる。
- containerがnon-rootで起動し、注入した `PORT`へ `0.0.0.0`でlistenする。
- image内のCatalog、コンパイル済みWorker、OpenAPIを解決できる。
- SIGTERM受信後8秒以内に受付停止、Pool終了、process終了を完了する。
- Cloud Run設定のminimum 0、maximum 1、concurrency 2、timeout 40秒が宣言値と一致する。
- GitHub ActionsのCloud Run deployが長期service account keyを使用しない。

## End-to-Endテスト

最小限の代表経路に絞る。

1. HTTPから最小Catalogの基本戦闘を完了する。
2. GitHub Pages相当の別originから一覧GETと戦闘POSTを完了する。
3. PS連鎖と状態差分を含む戦闘を完了する。
4. 同一ユニット重複と最大編成人数を受理する。
5. Worker Pool容量不足をHTTP `503` で返す。
6. タイムアウトをHTTP `504` で返す。
7. 初期状態からレスポンス差分を復元して最終状態と一致する。
8. Cloud Run revisionの公開HTTPS URLでlive、ready、Catalog GET、最小simulationを完了する。
9. deployed GitHub Pages originからCloud RunへのCORS preflightと実requestを完了する。
10. 新revisionのsmoke test失敗時に直前の正常revisionへtrafficを戻せる。

End-to-Endでは全110ルールを個別に再検証しない。失敗時に原因を切り分けられる小さな下位テストを正本とする。

## 負荷・耐久テスト

### シナリオ

- 1対1短時間戦闘の高並行実行
- 5対5・99ターン・DETAILED
- 5対5・99ターン・DIAGNOSTIC
- 深いPS連鎖
- 多数の効果インスタンスと個別期間
- 大きなイベント・状態差分
- Pool満杯の継続負荷
- クライアント切断とタイムアウトの連続
- ローリング終了中の実行
- 長時間soak後のメモリー推移

### 測定値

- HTTP p50／p95／p99
- Pool待機時間と実行時間
- イベントループ遅延
- CPU使用率
- Workerごとのheap
- 戦闘1件あたりの最大メモリー
- イベント数、差分数、レスポンスbyte数
- JSONシリアライズ時間
- 圧縮時間と圧縮率
- Worker再起動数、timeout数、容量拒否数

固定の合格値は、配備CPU・メモリーとサービスSLOが決まった時点で定義する。それまでは基準環境のbaselineを保存し、有意な回帰をレビュー対象にする。

## Mutationテスト

優先度の高い計算・順序ロジックへ限定してMutationテストを導入できる。

候補：

- `>` と `>=` の境界
- 速度sortの昇順・降順
- 最低1ダメージ
- 最終切り捨て
- シールド消費順
- 効果期間の付与スコープ除外
- PS発動済み除外
- 勝敗優先順

Mutation scoreだけを目標にせず、生き残ったmutationが不足テストまたは同値変異かをレビューする。

## Mock・Stub方針

### 差し替えてよいもの

- Catalog Port
- RandomSource
- ID Generator
- Clock／Execution Guard
- Worker Pool Port
- ロガー、メトリクス

### 原則としてmockしないもの

- Battle集約内部のEntity
- 行動順、ターゲット、ダメージなどのDomain Policy同士
- Domain Eventの発行そのもの
- StateDeltaの生成と適用

呼び出し回数だけを確認するinteraction testより、結果状態とイベント事実を確認するstate-based testを優先する。ただしCatalog取得1回、Battle未開始などアプリケーション調整の責務ではinteractionをassertする。

## テストの独立性

- グローバルな乱数、時計、ID連番を共有しない。
- production Catalogを変更しない。
- テストごとに新しいBattleとObservationを生成する。
- Worker Pool、HTTPサーバー、timerを必ず終了する。
- 実行順に依存しない。
- 並列実行時に同じ一時ファイルやポートを共有しない。
- 失敗時にseed、fixture、RandomSource消費列を出力する。

Flaky testを単純に再試行して成功扱いにしない。隔離する場合は担当・原因・期限を記録し、決定的な再現へ修正する。

## CIパイプライン

### Pull Request

1. Prettier check
2. TypeScript typecheck
3. ESLint
4. Domain／Application unit test
5. 小規模Property test
6. ルールカバレッジ台帳検証
7. Catalog構造・意味検証
8. API契約・OpenAPI検証
9. 主要Battle Scenario

### Main branch

Pull Request項目に加えて次を実行する。

- production build
- コンパイル済みWorker結合テスト
- HTTP＋WorkerのEnd-to-End
- 全Property test
- OpenAPI互換性検査
- test coverage report
- production container build・local smoke test
- Workload Identity FederationによるArtifact Registry pushとCloud Run deploy
- 新Cloud Run revisionのlive／ready／Catalog／simulation smoke test

### Nightly／Release

- 負荷試験
- soak test
- 最大規模シナリオ
- Mutation test
- 依存関係・脆弱性検査
- コンテナまたは配備成果物のsmoke test

## 品質ゲート

マージには次を必須とする。

- 全自動テスト成功
- 全ルールIDのテスト対応率100%（件数は`17_残作業対応表.json`の`current.rules.total`を正本とする）
- 新規・変更ルールに正常、境界、不成立の必要ケースがある
- 状態差分復元テスト成功
- OpenAPI契約に意図しない破壊的変更がない
- Catalog全体検証成功
- TypeScript、ESLint、Prettier成功
- 既知のflaky testが増えていない

行カバレッジ率だけでマージ可否を決めない。重要な分岐が未検証の場合、数値上のカバレッジが高くても不合格とする。現在のVitest設定にあるlines、functions、branches、statements各80%を初期下限として維持し、実装後のbaselineと重要度を確認しながら段階的に引き上げる。

UIワークスペース（`apps/ui`）もAPIと対称に`@vitest/coverage-v8`でカバレッジを計測し、同じ4指標80%を下限としてPR CIで検証する（`mise run ui:test:coverage`）。UI側の対象範囲・除外・引き上げ方針は[06_UIテスト戦略.md](../ui-design/06_UIテスト戦略.md)のCoverage方針を正とする。

## 不具合修正手順

1. 最も低い適切なテストレベルで失敗を再現する。
2. 関連ルールIDまたは仕様確認IDを特定する。
3. 仕様が不明なら実装を推測せず、[02\_仕様確認事項.md](./02_仕様確認事項.md) へ論点を追加する。
4. 再現テストが失敗することを確認する。
5. 実装を修正する。
6. 関連するProperty・Scenario・APIテストを実行する。
7. トレーサビリティ台帳を更新する。

仕様変更の場合は、古い期待値をただ書き換えず、設計書、ルールID、イベント契約、API互換性への影響を先に確認する。

## テスト完了条件

初回実装がテスト上完了したと判断する条件：

- 全ルールに実行可能なテストが対応している（件数は`17_残作業対応表.json`の`current.rules.total`を正本とする）。
- 主要22シナリオ（基準シナリオ表の`SCN-BTL-001`〜`023`のうち、REF-023で退役した`SCN-BTL-022`を除く22件）が成功する。
- 全イベント不変条件が成功する。
- 初期状態＋全差分が最終状態と一致する。
- APIの正常・入力エラー・障害レスポンスが契約に適合する。
- Catalog一覧APIの選択可否が戦闘事前検証と一致し、ETag／304契約が成立する。
- GitHub Pages許可originのCORSと未許可originの非許可が契約テストで固定される。
- production Catalogが構造・意味検証を通過する。
- コンパイル済みWorkerでEnd-to-Endが成功する。
- 実行保護、timeout、Pool満杯、キャンセルを勝敗へ誤変換しない。
- 基準環境で最大規模シナリオを完了し、容量設定に必要な測定値を取得している。

## モジュール境界のリファクタリング（`#132`）

M6〜M8に備えたDomain内部モジュール境界の再編（`#132`、PR1〜PR5）は、既存挙動を一切変更しない前提のArchitecture refactoringである。次のテスト方針で非回帰を保証する。

### Module境界テスト

- Layer間の禁止依存を検証している既存のESLint `no-restricted-imports` テスト（`layer-boundary.test.ts`）と同様の方式で、[04\_境界づけられたコンテキスト.md](./04_境界づけられたコンテキスト.md)「モジュールの依存順序」「モジュール依存規則」の禁止importをテストした（PR5で追加、`src/__tests__/architecture/module-boundary.test.ts`のUT-MOD-001〜028）。
  - `catalog` から `formation`／`battle` への import がないこと。
  - `formation` から `battle/model` 以外の `battle/*` への import がないこと。
  - `battle/model` から他のどの `battle/*` サブモジュールへも import がないこと。
  - `battle/events` から `battle/combat`・`battle/lifecycle`・`battle/triggering`・`battle/effects` への import がないこと。
  - `battle/effects` と `battle/combat` が互いを import していないこと。
  - `battle/*` の下位モジュールから `battle/lifecycle` への逆依存がないこと。
  - `presentation` から `domain` への直接importがないこと（既存規則の維持確認）。
  - 上記に加え、`battle/model`・`battle/lifecycle`を含む`battle/*`全サブモジュールから`formation`への逆依存がないこと（`formation`は`battle/model`にのみ依存される一方向）。
  - モジュール別の禁止import設定は、同一ファイルにマッチする既存のDomain Layer共通ルール（`application`／`infrastructure`／`presentation`／`bootstrap`およびNode.js組み込みモジュールの禁止）をFlat configの後勝ち置換で上書きしないこと（`domainRestrictedImports`ヘルパーで両方を常に合成し、`catalog`・`formation`・`battle/*`の全12モジュールパスで確認、UT-MOD-021・022）。
  - `battle/triggering`（M6で新設）と`battle/effects`（M7で新設）は`lifecycle`直下の同格ノードとして、互いを import していないこと、および`triggering`から`combat`への import がないこと（UT-MOD-023〜026）。
  - `domain/battle`・`application`直下に、認識済みサブモジュール以外のファイル・ディレクトリが存在しないこと（実ディレクトリを走査するarchitecture test。`no-restricted-imports`はimport文のみを検査しファイル配置自体は検査できないため、`#132`が解消した平坦化の再発を別の仕組みで防ぐ。UT-MOD-027・028）。
- 循環依存検査（`mise run check:circular` / `madge --circular`）を、移動後の新しいpathに対して実行した（PR5）。PR1時点で現行パスに対して実行済みであり、[04\_境界づけられたコンテキスト.md](./04_境界づけられたコンテキスト.md)「依存方向の検証結果」に記載のとおり、移行表は実際のimportグラフを1件ずつ確認して矛盾がないことを確認している。

### 移動時のcharacterization

機械的移動（PR2: Domain、PR4: Application/Presentation）と責務分割（PR3: Domain、PR4後半のSchema/Route分割）は、それぞれ次を満たしてからマージする。

- 移動前後で、Unit／Scenario／Contract／Integration／E2E／Loadの各テストレベルの実行件数（`it`/`test`の件数）が減っていないことを確認する。テストファイルの移動に伴うimport更新だけを行い、アサーション内容は変更しない。
- 移動対象のファイルと、対応する `*.test.ts` は同一コミット内で一緒に移動し、実装内容の変更（責務分割・ロジック変更）は別コミットへ分離する。
- 責務分割PR（PR3、PR4後半）では、分割前の1ファイルに対する既存テストが、分割後も（importパスの変更以外は）そのまま通ることを確認する。新しい公開境界を追加する場合は、境界を検証する最小限のテストを追加してよい。

### 乱数・イベント順序の非回帰条件

- 固定 `SequenceRandomSource` を用いた既存の `SCN-BTL-*` シナリオが、移動前後で同じ乱数消費回数・消費順を維持することを確認する。
- 既存イベントの `type`・`sequence`・`parentEventId`・`rootEventId` の順序が、移動前後で変化しないことを確認する（スナップショットまたは既存アサーションで検証）。
- `StateTransition`（`stateVersionBefore`/`stateVersionAfter`/`stateDelta`）の内容が変化しないことを確認する。
- `initialState + stateTransitions = finalState` の不変条件が、移動後も成立することを確認する。
- OpenAPI文書、Catalogレスポンス、CORS許可設定、ETag挙動、エラーマッピングが移動前後で意味的に同一であることを契約テストで確認する（PR4対象）。

### PRごとの実行内容

PR2〜PR5の各PRで、少なくとも次を実行する。

1. `mise run typecheck`
2. `mise run lint`
3. `mise run format:check`
4. `mise run test`（Unit／Scenario／Contract）
5. `mise run check:circular`
6. 移動対象のテストレベルに応じて `mise run test:integration`／`mise run test:e2e` を実行する。
7. PR3・PR4の責務分割PRでは、分割前後のスキーマ・レスポンス・イベント順を上記「非回帰条件」で確認する。

PR1（本ドキュメント更新）自体はコード変更を伴わないため、上記の実行内容は対象外とする。

## テスト基盤の整備状況（2026-07 更新）

シミュレート可能な production Unit が現れた段階で、本戦略書と実装の乖離を点検し、次を整備した。

### ルールカバレッジ台帳の実在照合

`rule-coverage.ts` の正例 testCaseId が「実在する実行対象テスト」に対応することを機械照合する（`rule-coverage.test.ts` の `UT-TRACEABILITY-004`／`005`）。照合ロジックは `remaining-work.test.ts` から `testing/traceability/test-case-definitions.ts` へ抽出して共有し、`it.skip`/`todo`/条件付き無効化・コメント内・文字列内を証跡から除外する。`it.each([...] as const)("%s: ...", ...)` のように ID がテーブル行に存在するパラメタライズドテストも収集対象に含める。

- `UT-TRACEABILITY-004`: 同一ルール内で testCaseId が重複しないこと（ルールを跨いだ共有は複数ルール確認テストとして許容）。
- `UT-TRACEABILITY-005`: 各正例 testCaseId が**ちょうど1件**の実行対象テストに対応すること（phantom ID＝実体0件と、採番衝突＝2件以上の双方を不合格とする）。あわせて、台帳掲載の有無によらず**リポジトリ内で収集された全 testCaseId に衝突がない**ことを検査する — 台帳外で生まれた重複が、後から台帳へ持ち込まれるのを防ぐ。

**合格条件は「ちょうど1件」**（REF-011／Issue #314 で引き上げ済み）。導入時に残っていた採番衝突（同一 ID を複数の別テストが使用）は同 Issue で一括リナンバーして解消した。意図的に1つの ID を複数テストで共有する場合は、`rule-coverage.test.ts` の許可リスト `INTENTIONALLY_SHARED_TEST_CASE_IDS` へ理由コメント付きで登録する — 現在の登録は `SCN-BTL-001` のみ（基準シナリオ「1対1の基本戦闘」1件を、決定性・lethal 経路・use-case lifecycle の複数テストで分担して検証するため）。`UT-R-STA-01-014〜017` の phantom ID は本整備で台帳から除去済み。

### Property テスト（fast-check）

Property/Model テストは fast-check（devDependency）で実装する。共通設定・arbitrary は `testing/property/`（`PROPERTY_ASSERT_CONFIG` は seed 固定・`numRuns` 指定、失敗時に fast-check が seed と最小反例を出力）。配置はこの project の規約（プレーンでないサフィックスは別 config・別 CI ステージを意味する）に合わせ、**専用サフィックスは使わず、対象モジュールの既存プレーン `<module>.test.ts` へ property の `describe` を統合**する（PR default suite で実行。§CIパイプライン「小規模Property test」）。識別は `PROP-*` ID と `describe(... properties (R-...))` タイトルで確保する。初期実装は次を検証し、台帳の該当ルールへ `PROP-*` ID で登録した（`kinds` の `PROPERTY` ラベルを実体化）。

- `R-POS-03`（`PROP-POS-03-*`）: マンハッタン距離の非負性・対称性・同一性・三角不等式。
- `R-ORD-02`（`PROP-ORD-02-*`）: 行動順比較器の決定性・反対称性・推移律・入力配列順非依存。
- `R-NUM-02`／`R-ACT-03`（`PROP-NUM-02-*`／`PROP-ACT-03-*`）: 最終切り捨ての冪等性、EXゲージ上限打ち止めと要求量保存則。
- `R-DMG-01`（`PROP-DMG-01-*`）: `calculateDamage` の finalDamage が整数・最低1・非負、攻撃力に対し単調非減少、実効防御=防御力×(1−無視率)（`damage-calculator.test.ts` へ統合）。
- `R-SHD-03`（`PROP-SHD-03-*`、`damage-application-service.test.ts` へ統合）: 任意のダメージ・`shieldIgnoreRate`・複数のタイプあり／なしプール・HPについて、`DamageApplied` の内訳が `typedShieldAbsorbed + untypedShieldAbsorbed + hitPointDamage + discardedDamage === calculatedDamage`（保存則、`08_ドメインイベント.md` 不変条件 #6）を満たし、各項が非負でHPが0未満にならないこと。あわせて各プールの吸収量が `min(プール合計, 残ダメージ)` に一致し、対応しないタイプありシールドが常に無傷であること（R-SHD-02末尾）。
- 全戦闘の不変条件（`PROP-BATTLE-001`、`testing/scenario/run-scenario.test.ts`）: 任意の有効編成（1〜5体・座標/速度/stats 可変・PREVENTED会心で乱数非消費）を `runScenario` で完走させ、HP/AP/PP/EX 非負・`COMPLETED`・stateVersion 連続・イベント単調を満たす。特定ルールに紐づかない model property のため台帳未登録（横断テスト）。

**Defer（別基盤要）**: シールド側の保存則 R-SHD-03 は `DMG-004`／Issue #194 で `PROP-SHD-03-*` として追加済み。サブユニット吸収を含む5項の保存則と、リンク量不変（R-LNK）・DoT（R-DOT）を含む全ダメージ経路の通し検証は、`DMG-011`（Issue #186）が `IT-AUDIT-M8-001`〜`003` として実 `catalog/` の全production戦闘に対して追加した（生成 property ではなく実データ全数を母集団にする形。合成入力の property 版への拡張は継続作業とする）。状態全復元（初期+全差分=最終）の property 版は StateDelta→snapshot の独立Reducer新規実装が必要で、現状は `state-restoration.test.ts` が response-level で example ベース検証済み。残る `PROPERTY` ラベル付きルールへの拡充も継続作業とする。

### Scenario harness と Builder

`testing/scenario/` に合成 Catalog 用の `CatalogBuilder`／定義Builder（`unitDefinition`・`attackSkill`・`damageEffectAction`・`exSkillDefinition`・`formationSlot`・`battleCommand`）と `runScenario` harness（実 Formation/Battle/Observation を通し HTTP/Worker を通さない）＋不変条件アサーション（`assertBattleInvariants` 等）を実装した。

**harness 採用（実運用）**: `simulate-battle-use-case.test.ts` にインライン重複していた `attackSkill`／`damageEffectAction`／`exSkillDefinition`／`ENEMY_ALL`／`slot`（＝`formationSlot`）を共有 Builder へ集約した（アサーション不変・約90行削減）。同ファイルの `unitDefinition`（`SKL_EX` 参照）と `FakeBattleCatalog`（多引数API）はbespokeなため据え置く。新規の harness ベースシナリオは `__tests__/scenario/battle-scenarios.test.ts` に集約する（全戦闘シナリオ `SCN-BTL-001`（#10 決定性/lethal）・`SCN-BTL-008` を集約済み。`SCN-BTL-022` もここにあったが `REF-023`／Issue #352 で退役した）。既存の散在する `SCN-BTL-*`（下位 Battle 直接構築を含む）の全面移行は、回帰リスクを避けるため段階的フォローアップとする。`SCN-BTL-015`〜`017`（シールド＋サブユニット／リンク／DoT）は `DMG-011`（Issue #186、M8完了監査）が該当ルールの実装完了を確認して追加した。`SCN-BTL-018`（状態異常）は根拠だった「ルール未実装」が失効しており（`R-STS-01`〜`04` は完了済み）、実装可能である。`REL-003`（Issue #200、M9）で追加する。

### Golden battle 回帰層

全 production Unit について、味方・敵に据えた1対1の決定的戦闘を完走させ、基本不変条件と「勝敗・終了理由・公開イベントの type 列」の小さな安定 snapshot を固定する（`__tests__/golden/production-unit-golden-battle.test.ts`、実行部は `testing/scenario/run-production-battle.ts`）。新たに追加された Unit は自動的にケースが増える（snapshot 未登録なら失敗する強制関数）。

`REF-024`（Issue #353）で**混成編成戦**を追加した（`production-party-golden-battle.test.ts`、`E2E-GOLDEN-PARTY-*`）。1対1ミラー戦は同一ユニットを味方・敵に据えるため、「異なる production ユニットの定義同士が同じ盤面で噛み合うか」をどの層も見ていなかった（機能軸が押さえているのはエンジンのルールであって編成の組み合わせではなく、ユニット効果軸は1定義のスコープに閉じている）。N×N を避けるため、全ユニットIDをsortして5体ずつ（編成上限）のpartyへ分割し `party[k]` 対 `party[k+1]` を回す。**全ユニットが味方として1回・敵として1回**、必ず混成編成の中で実戦闘を経験し、ユニット追加時も自動的にケースが増える。行適性（`positionAptitudes`）は配置制限ではなく統計ペナルティ（`R-STA-01`）なので、任意の組み合わせを任意の位置へ置ける。

snapshotはイベント型ごとの件数マップに落とす（イベントの**順序**は全 production ユニットに対して `IT-AUDIT-M8-001` が既に検査しているため重複させない）。混成編成ではミラー戦では起こらないPS連鎖の重なりが実行ガード（`maxEffectsPerScope: 50`）へ届く組み合わせが実在する。**完走ケースで例外を成功として飲み込んではならない**（golden の契約は「完走・不変条件」であるため）。ガードへ到達する組み合わせは `E2E-GOLDEN-PARTY-GUARD-*` として明示的な既知テストへ分離し、到達しなくなったら失敗させて一覧の更新を強制する。`REF-026`（Issue #356）で `DamageApplied` に `skillType` を載せた結果、それまで一度も発動しなかったPSが発動するようになり、matchup 1 はガードへ届かず完走するようになったため完走ケースへ移した（残るガード到達は matchup 3 のみ）。

retire の先例として、`UNIT_KEI_JACKKNIFE` の実戦闘完走を検証していた機能軸の `kei-jackknife-selectable-battle-production-catalog.test.ts`（`IT-CAP-BRANCH-REPEAT-PROD-005〜006`）は retire した。KEI は selectable でありこの golden 層が完走を包含し、maximumHp のゲージ境界整数化は domain 単体（`UT-BATTLE-UNIT-010`／`UT-R-STA-01-013`）が守るため。`CAP_RESOLUTION_BRANCH_REPEAT` の BRANCH 機構検証（`IT-CAP-BRANCH-REPEAT-PROD-001〜004`）は機能軸として `production-catalog/` に残る。

### Load/Soak baseline

空白だった Load/Soak レベルへ最小1本（`LOAD-SOAK-001`、`__tests__/load/battle-simulation.load.test.ts`）を追加した。catalog を一度だけロードし（`createProductionBattleRunner`）、production Unit の DIAGNOSTIC 戦闘を反復実行して、実行時間 p50/p95/p99、イベント数、`stateTransitions` 数、レスポンス相当 byte 数、heap 増加を測定し **baseline を CI ログへ JSON 出力**する。固定合格閾値は配備CPU・メモリとSLO確定まで設けず（戦略「baselineを保存し有意な回帰をレビュー対象にする」）、暴走検出のサニティ上限のみ assert する。`mise run test:load` で nightly／リリース前に実行。5対5・深いPS連鎖・複数効果インスタンス等の追加シナリオと固定閾値は継続作業。

## 実装計画への申し送り

[13\_実装計画.md](./13_実装計画.md) では、次を実行可能な作業へ分解する。

- 実装フェーズと依存順
- 各フェーズで作成するモジュール・テスト・Catalog
- 最小縦切りから全ルールへ拡張する順序
- OpenAPI、Worker Pool、観測基盤の導入時期
- 各マイルストーンの完了条件
