"""ギア配分の分析。1手の限界効用（`lab gear-sensitivity`）と、その部品。

探索変数は**ギア配分だけ**である。ユニットの選抜と配置・メモリー・敵・現在レベル・
学園レベルはすべて基点編成の値で固定する。`lab optimize`（編成探索）とは探索変数が
相補的で、あちらはギアを `--player-data` から固定して編成を探す。したがって両者を
交互に回さないと同時最適には届かない。

**基点編成を変えたら分析結果は無効になる。** とくにメモリーは順位を決める量を直接
動かす（Catalog の `APPLY_STAT_MOD` に攻撃力・行動速度の加算がある）ため、入れ替えると
順位セレクタの当て先が変わり、最適なギア配分も変わる。
"""
