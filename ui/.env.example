# Copy to .env.local for local development against a locally running API
# (see CLAUDE.md `mise run dev` at the repository root).
# Production values are public (no secrets) and are injected by the GitHub
# Pages deploy workflow via the GitHub Environment (Issue #99).
VITE_API_BASE_URL=http://localhost:8080
